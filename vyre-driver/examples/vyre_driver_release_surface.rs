//! Minimal release-surface example proving `vyre-driver` ships a runnable examples target.

fn main() {
    println!("vyre-driver release surface example for Vyre 0.4.1");
    println!("This example is intentionally small: it proves the crate ships a runnable examples/*.rs target.");
}

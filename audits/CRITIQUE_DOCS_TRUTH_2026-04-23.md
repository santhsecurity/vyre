# CRITIQUE_DOCS_TRUTH_2026-04-23

Scope: `libs/tools/surgec/{README.md,docs/*.md}` and `libs/performance/matching/vyre/{README.md,docs/*.md}`.

| Severity | file:line | description | suggested fix |
| --- | --- | --- | --- |
| HIGH | `libs/tools/surgec/docs/cvss-calibration.md:3` | The doc says every shipped rule carries `#[cvss]` and `#[owasp]`, but repo grep shows 84 shipped `.srg` rules missing one or both attributes (for example `rules/auth/hardcoded_credential.srg`, `rules/crypto/static_iv_reuse.srg`, `rules/ai/prompt_injection.srg`). | Either add the missing attributes to every shipped rule or narrow the claim to the subset that is actually annotated. |
| HIGH | `libs/tools/surgec/docs/cvss-calibration.md:61` | The gate cites `scripts/check_cvss_calibration.sh`, but no such script exists under `libs/tools/surgec/scripts/` or elsewhere in the repo. | Point the doc at the real enforcement artifact, or add the missing gate before claiming CI enforcement. |
| HIGH | `libs/tools/surgec/docs/SUBSYSTEM_STANDARD.md:94` | The doc says per-subsystem READMEs are "landed for vyre-libs dialects" and cites `index/README.md`, but `**/index/README.md` does not exist and `vyre-libs` only has root/fixtures READMEs. | Replace the claim with the actual README coverage, or land the missing dialect READMEs. |
| HIGH | `libs/tools/surgec/docs/TOOLING_STATUS.md:12` | The table marks `surgec bench <target>` as shipped, but `src/main.rs` has no `bench` subcommand at all; only Criterion bench files exist under `benches/`. | Reword this row to "bench harnesses exist" or add a real `surgec bench` CLI verb. |
| HIGH | `libs/tools/surgec/docs/TOOLING_STATUS.md:15` | The table marks `surgec distribute <target>` as shipped, but `src/main.rs` exposes distribution as `surgec scan --distributed <workers.toml>`, not as a `distribute` verb. | Document the shipped surface as the `--distributed` scan mode, or add the missing verb. |
| HIGH | `libs/tools/surgec/docs/TOOLING_STATUS.md:18` | The doc says the crate ships `fuzz/` corpora and structure-aware generators, but there is no `libs/tools/surgec/fuzz/` tree. | Remove the shipped-asset claim or land the fuzz corpus/generator tree. |
| HIGH | `libs/performance/matching/vyre/docs/PER_OP_SURFACE.md:34` | The doc cites parity test `cat_a_conform::bitset_and_cpu_gpu_agrees`, but grep of `vyre-driver-wgpu/tests/cat_a_conform.rs` finds no such test name. | Rename the doc to the real parity test, or add the named test. |
| HIGH | `libs/performance/matching/vyre/docs/PER_OP_SURFACE.md:37` | The doc says `bitset::proptest::and_commutative` landed, but grep finds no `and_commutative` symbol anywhere in vyre Rust sources. | Point to the real property test, or add the missing proptest before calling it landed. |
| HIGH | `libs/performance/matching/vyre/docs/POST_LANDING_STATUS.md:18` | The doc says exploit-graph tombstones were closed via `exploit_graph::verify_chain_consistency`, but no such symbol exists in `libs/tools/surgec`. | Cite the real implementation/test that closed the issue, or reopen the item in docs. |
| HIGH | `libs/performance/matching/vyre/docs/POST_LANDING_STATUS.md:31` | The doc says moved-file diff replay was closed in `diff_replay::plan`, but `libs/tools/surgec/src/scan/diff_replay.rs` exports `run_diff_replay`, `provenance_set`, and `scan_findings`; there is no `plan`. | Update the doc to the actual function that implements rename handling, or drop the closed claim. |
| HIGH | `libs/performance/matching/vyre/docs/POST_LANDING_STATUS.md:54` | The doc says the central `Finding` struct lives in `surgec::finding`, but no `surgec::finding` module exists; `Finding` is defined in `libs/tools/surgec/src/scan/dispatch.rs`. | Fix the module path in the doc to the real owning module. |
| HIGH | `libs/performance/matching/vyre/docs/ops-catalog.md:118` | The doc says `fnv1a32` is shipped in `vyre-libs/src/crypto/fnv/`, but that path does not exist; the shipped op lives in `vyre-libs/src/hash/fnv1a32.rs`. | Update the catalog path and migration note to `vyre-libs/src/hash/`. |
| HIGH | `libs/performance/matching/vyre/docs/ops-catalog.md:125` | The doc says `blake3_compress` is shipped in `vyre-libs/src/crypto/blake3/`, but that path does not exist; the shipped op lives in `vyre-libs/src/hash/blake3_compress.rs`. | Update the catalog path and migration note to `vyre-libs/src/hash/`. |
| MEDIUM | `libs/performance/matching/vyre/docs/primitives-tier.md:137` | "CI green at zero cost" is a green-status claim with no workflow URL, run URL, or badge in the file. | Link the exact CI workflow/run that proves green, or rewrite the sentence as a goal instead of a status statement. |
| HIGH | `libs/performance/matching/vyre/README.md:11` | The README claims first-class shipped primitives include `union-find` structures, but grep over `libs/performance/matching/vyre/**/*.rs` finds no union-find/disjoint-set code artifact. | Narrow the README to implemented primitives, or land the missing union-find surface before claiming it. |
| HIGH | `libs/performance/matching/vyre/README.md:181` | The README claims first-class shipped workgroup coordination includes a `priority queue`, but grep over `libs/performance/matching/vyre/**/*.rs` finds no priority-queue primitive/module. | Remove the shipped claim or add the missing priority-queue primitive and tests. |

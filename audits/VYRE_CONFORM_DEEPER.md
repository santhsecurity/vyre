# VYRE_CONFORM_DEEPER Audit

Format: | SEV | file:line | defect | fix |

| CRITICAL | libs/performance/matching/vyre/conform/vyre-conform-runner/src/lens.rs:170-187 | cpu_vs_backend performs a raw bytewise comparison (cpu != gpu) and completely ignores fp_parity::compare_output_buffers / ULP tolerance. This will mark valid F32-ULP-divergent-but-acceptable results as failures or, conversely, miss accepted/expected ULP windows when callers expect them. | Replace bytewise equality with a call to fp_parity::compare_output_buffers(&program, &gpu, &cpu) and map BufferParity::Mismatch to LensOutcome::Fail with the mismatch detail. Add tests covering F32 tolerance and strict-fp behavior. |

| CRITICAL | libs/performance/matching/vyre/conform/vyre-conform-runner/src/lens.rs:366-378 | merge_rw iterates `program.buffers.iter()` (looks like a field access) while other modules call `program.buffers()`. This inconsistency likely causes compile errors or semantic divergence across implementations. | Normalize to the canonical API (use program.buffers() everywhere) or document the struct field; run cargo test + clippy to validate compilation. Add a unit test that compiles and exercises merge_rw. |

| HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/fp_parity.rs:110-116 | f32_ulp_tolerance detects transcendental presence via program_has_transcendental which treats Expr::Call by only inspecting args (line ~193). If a Call refers to a transcendental intrinsic by name, the heuristic can miss it and under-report tolerance. This leaks ULP policy and may cause false positives. | Extend node/expr scanning to inspect Call target identity (function/op id) for known transcendental intrinsics, or annotate lowered nodes with a transcendental flag during IR lowering. Add unit tests where Expr::Call represents an intrinsic. |

| HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/fp_parity.rs:81-97 | f32_buffer_matches treats NaN only as bitwise-equal (line ~80). However CPU vs backend NaN semantics (signaling vs quiet, payloads) differ across implementations; treating only bitwise-equal NaNs as equal risks spurious mismatches. | Document NaN policy. If intended, keep but add explicit tests for signaling vs quiet NaNs and an option to allow NaN-equality-by-NaNness rather than bitwise. Consider allowing configurable NaN-policy (bitwise vs any-NaN) per-program. |

| HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/main.rs:68-76,121-136 | dispatch_pairs / main CLI: argument parsing is ad-hoc and will silently accept subtle misuses; usage messages inconsistent. Also backend acquisition message suggests only wgpu/spirv though registered_backends is used  -  error strings are not exhaustive. | Replace argument parsing with clap/structopt to get strict parsing, better help text, and consistent error codes. Enumerate available backends in error messages using registered_backends() to avoid inaccurate hints. Add CLI unit tests. |

| HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/prover.rs:63-82 | LawProver uses a tiny Xorshift32 RNG seeded with fixed constants (0x1337_BEEF / 0xBEEF_1337) for stochastic law checks and deterministic sampling. While determinism helps reproducibility, the PRNG has limited quality and the sampling counts (samples = witnesses.len()*4 max 64; / *8 max 128) are likely insufficient for small witness sets and provide no statistical guarantees of coverage. | Replace Xorshift32 with a higher-quality deterministic RNG (e.g., ChaCha20 with fixed seed when determinism desired) and expose a tunable sampling policy. Add a non-deterministic mode for fuzz runs (seed from OS RNG) and document statistical failure probabilities. Add tests measuring collision/coverage on small witness sets. |

| HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/prover.rs:69-76 | Sampling uses modulo indexing into witnesses: witnesses[(rng.next() as usize) % witnesses.len()]. This skews when witnesses.len() is not a power-of-two but is generally acceptable; however combined with small sample counts it can bias pair/triple selection and miss counterexamples. | Use unbiased sampling (e.g., generate u64 and reduce with rejection sampling) or shuffle witness indexes and take prefix to ensure uniform selection without modulo bias. Add tests for distribution uniformity for various lengths. |

| HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/convergence_lens.rs:164-203 | infer_fixpoint_buffers selects the last ReadOnly as `current` and last ReadWrite as `next`. This positional heuristic is fragile and undocumented; malformed buffer ordering or additional RW/RO buffers can silently break the contract, causing wrong buffers to be driven and incorrect convergence checks. | Require explicit buffer naming via FixpointContract or validate naming conventions; add structural checks: ensure selected buffers have expected counts and types, and fail early with actionable errors when ambiguity exists. Add tests with multiple RO/RW buffers. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-runner/src/convergence_lens.rs:231-237 | flag_word returns 0 when the changed buffer is shorter than 4 bytes (line ~231-236) silently treating it as converged. This masks mis-sized buffers and makes convergence succeed incorrectly. | Treat short/missing flag words as an IncompatibleLayout or Dispatch error and fail fast with a clear message. Add tests for malformed buffers. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-runner/src/convergence_lens.rs:74-99 | run_fixpoint_to_convergence swaps state with state.swap(current_idx, next_idx) unconditionally, but does not guard against programs where current_idx==next_idx (e.g., malformed declarations). This can be a no-op or worse depending on merge behavior. | Validate that current_idx != next_idx and fail with IncompatibleLayout if equal. Add defensive assertions and tests. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-runner/src/cert.rs:160-179 | verify_structural checks fingerprint length==64 and hex chars but does not validate hex decodeability (non-ASCII hex will pass is_ascii_hexdigit check). | Attempt a hex decode (hex::decode) and return BadFingerprint on decode failure; add targeted tests. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-runner/src/main.rs:417-444 | Prove artifact signing uses rand_core::OsRng to generate a fresh key per run (good for preventing brute-force forging), but this makes certificates non-reproducible and may break reproducible CI auditing that expects deterministic cert artifacts. | Add an opt-in reproducible mode that accepts a high-entropy secret via env var (HKDF) to derive the seed deterministically; document tradeoffs and reject weak seeds. Keep OS RNG as default. Add tests for reproducible and non-reproducible modes. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-runner/src/main.rs:417-456 | The signing process includes `pairs` (messages with runtime errors) into the signed blob. If messages contain large runtime traces or sensitive debug data, the signature manifests them verbatim. | Canonicalize and truncate non-essential textual data included in certificate body. Document what is signed and ensure no secrets or PII can appear. Add a size cap on pair.message before inclusion. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-generate/src/minimizer.rs:25-49 | CounterexampleMinimizer::shrink_u32 uses a binary-search shrink, but the mid computation and loop invariants rely on caller providing a failing `failing` and predicate monotonicity. If predicate is non-monotonic, algorithm misbehaves silently. | Document monotonicity requirement in public API, and add an assert/debug-check mode that detects non-monotonic predicates (e.g., verify that predicate(mid) implies predicate(higher) in sampled points) or provide a generic shrinker that handles non-monotonic predicates. Add tests demonstrating failure modes. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-spec/src/witness.rs:24-40 | U32Witness.enumerate seeded from blake3("u32-witness-v1") and then splitmix64  -  deterministic but fixed corpus could fail to exercise platform-specific corner cases (e.g., NaNs, denormals for FP counterparts) and bias towards certain bit-patterns. | Consider expanding witness sets, or providing a configurable/semi-random mode for fuzz runs to augment deterministic canonical sets. Add metadata describing why each sentinel value was chosen. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-spec/src/lib.rs:11-18 | LawProver::verify_associative/commutative sample sizes are fixed multipliers of witness.len and use hard caps (64/128). No rationale or statistical confidence levels documented. | Document sampling strategy and confidence guarantees; expose config for sample multiplier and caps; include unit tests that show failure detection probability for contrived laws. |

| MEDIUM | libs/performance/matching/vyre/conform/vyre-conform-spec/src/cert/fingerprint.rs:68-82 | BackendFingerprint canonicalization uses length-prefixed fields (good), but no versioning is embedded beyond the literal string `vyre.conform.fingerprint.v2`. Missing an explicit compat check may confuse consumers. | Keep the version tag (good) but add a typed version constant and a compatibility policy in docs. Add tests asserting byte-canonicality when fields include NUL or binary. |

| LOW | libs/performance/matching/vyre/conform/vyre-conform-runner/src/bundle_cert.rs:1-1 | bundle_cert.rs uses case-insensitive pubkey comparison (eq_ignore_ascii_case) on hex strings (grep hit). Hex should be normalized; eq_ignore_ascii_case is harmless but makes assumptions about ASCII-only inputs. | Explicitly normalize hex to lowercase via hex crate before comparisons and assert expected length. Add unit test. |

| LOW | libs/performance/matching/vyre/conform/vyre-conform-runner/src/prover.rs:119-138 | Tests in LawProver rely on small witness vector (0..8). These unit tests validate XOR but do not stress the stochastic sampler or pathological witness distributions. | Add property tests (proptest) or fuzz harnesses that exercise the sampling layer and edge cases (single-element witness, repeated values). |

| LOW | libs/performance/matching/vyre/conform/vyre-conform-spec/src/cert.rs:79-87 | Certificate::to_json uses serde_json::to_string_pretty which may reorder or add whitespace; canonical JSON for signatures should be strict canonicalization (the runner uses a separate signable body already, but docstring should call out canonical form). | Document canonicalization used for signatures and add tests that ensure issue_certificate + verify_structural pipeline uses exact canonical bytes for signing. |

| LOW | libs/performance/matching/vyre/conform/vyre-conform-runner/src/main.rs:146-156 | acquire_backend enumerates registered_backends() and gives a custom hint for 'wgpu' feature missing, but other backends may also be optional  -  the help message could mislead. | Improve hint text to list registered_backends() and suggest `--features` that will register backends. |

| LOW | libs/performance/matching/vyre/conform/vyre-conform-spec/src/witness.rs:43-51 | fingerprint_canonical returns raw 32 bytes; higher-level code expects hex string fingerprint elsewhere. Inconsistent representation risks mixups. | Standardize witness fingerprint representation across modules (bytes vs hex) and document the stable canonical encoding. Add tests asserting round-trip. |

| INFO | libs/performance/matching/vyre/conform/**/* | No tests were found with #[ignore] in scanned files; no ignored tests hiding known bugs. | N/A: continue monitoring test suite for newly ignored tests; encourage minimal use of #[ignore] with clear issue references. |


Summary: The most urgent defects are the missing ULP-aware comparison in the cpu_vs_backend lens (CRITICAL) and multiple API inconsistencies that could break compilation or semantics (CRITICAL/HIGH). RNG/sampling quality, fixpoint buffer inference, and silent fallbacks merit fixes to avoid correctness holes under fuzzing or rare inputs. Follow-up: implement the critical fixes first, then harden sampling, add property/fuzz tests, and run `cargo test` + `cargo clippy -- -D warnings` across the workspace.

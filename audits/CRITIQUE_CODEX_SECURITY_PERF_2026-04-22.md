1. CRITICAL | `libs/tools/surgec/src/compile/predicate_registry.rs:169` | `PredicateDef::lower` defaults to `Ok(())`, so third-party predicates can register as “implemented” while performing no lowering at all once registry parsing is enabled, which is a silent false-negative sink. Fix: remove the default body and require every predicate registration to provide executable lowering or fail registration/build.
2. HIGH | `libs/tools/surgec/src/compile/predicate_registry.rs:208` | Duplicate predicate names panic the process during registry initialization, so a malicious or simply conflicting extension crate can turn rule loading into a startup-time DoS. Fix: surface duplicate registrations as structured `Result` errors and keep registry construction non-panicking.
3. HIGH | `libs/tools/surgec/src/compile/signal_registry.rs:87` | Duplicate signal-type registrations also panic at init time, which makes extension loading hostile-input fragile instead of recoverable. Fix: return a structured duplicate-registration error that includes both providers and abort loading cleanly.
4. HIGH | `libs/tools/surgec/src/compile/signals/regex_family.rs:32` | `regex_family` validates patterns by compiling them eagerly with no state/size budget, so a hostile rule file can force pathological regex compile time and memory before any scanner budget applies. Fix: validate against an explicit regex complexity/state budget and reject oversized NFAs/DFAs up front.
5. MEDIUM | `libs/tools/surgec/src/compile/signals/literal_family.rs:21` | `literal_family` ignores the authored `variants` list entirely during validation, so malformed or unsupported variant declarations survive to expansion time with no actionable diagnostic. Fix: validate every declared variant token against the supported encodex variant set and reject unknown entries.
6. MEDIUM | `libs/tools/surgec/src/compile/signals/exact.rs:19` | `exact` enforces cardinality but places no upper bound on literal length, so a single hostile rule can inject multi-megabyte needles that explode scan-time buffer packing and cache churn. Fix: add a hard maximum literal length with an explicit compile error that instructs authors to shard or externalize oversized blobs.
7. HIGH | `libs/tools/surgec/src/compile/expand.rs:41` | Expansion treats `signal.variants` as a boolean switch and then calls `expand_pattern(value.as_bytes())` without consulting the actual variant list, so authored variant policy is ignored and the scanner may generate unintended encodings. Fix: pass the explicit variant set into expansion and emit only the declared transforms.
8. HIGH | `libs/tools/surgec/src/compile/expand.rs:47` | `String::from_utf8_lossy` collapses invalid-byte expansions into replacement characters, which destroys byte identity and can merge distinct shellcode/malware variants into the same scanner literal. Fix: keep expanded patterns as raw bytes plus encoding metadata instead of lossy UTF-8 strings.
9. HIGH | `libs/tools/surgec/src/compile/expand.rs:58` | Unknown `SignalType` variants silently expand to `Vec::new()`, which drops scanner coverage the moment the AST grows and guarantees false negatives instead of a loud compiler failure. Fix: replace the wildcard-empty path with an explicit error naming the unsupported signal type.
10. HIGH | `libs/tools/surgec/src/compile/predicate.rs:61` | `extract_file_selector` overwrites `selector` on every matching node and returns only the last `in file(...)` location it encounters, so mixed selectors in one predicate tree silently lose earlier file constraints. Fix: detect multiple file selectors and fail compilation unless they are merged explicitly by a defined combinator.
11. HIGH | `libs/tools/surgec/src/compile/applicability.rs:194` | Unknown `FileSelector` variants fall back to `Applicability::any()`, which lets new selector shapes silently bypass host-side gating and wastes scan budget while hiding selector support gaps. Fix: make unhandled selectors a compile error until a conservative executable interpretation exists.
12. CRITICAL | `libs/tools/surgec/src/compile/applicability.rs:236` | `applicability_for_rule` intersects every clause’s applicability mask even though rule semantics are additive across clauses, so a rule with one `.rs` clause and one `.py` clause becomes applicable to neither file type. Fix: compute per-clause applicability independently and combine them with OR semantics at the rule level.
13. MEDIUM | `libs/tools/surgec/src/compile/applicability.rs:165` | Magic-prefix intersection keeps only byte-identical prefixes and drops compatible prefix refinements, so intersecting `PK` with `PK\x03\x04` incorrectly yields “no magic gate” instead of the tighter prefix. Fix: intersect prefixes by prefix-compatibility and keep the most specific common prefix set.
14. MEDIUM | `libs/tools/surgec/src/compile/applicability.rs:207` | `extensions_from_glob` is a string hack that treats the last `.` as the extension boundary, so patterns like `*.tar.gz` or `foo.[ch]` are misparsed into the wrong gate. Fix: parse globs structurally and derive extensions from the AST rather than ad hoc slicing.
15. MEDIUM | `libs/tools/surgec/src/compile/applicability.rs:277` | `extensions_from_regex` only recognizes a narrow suffix idiom and silently degrades everything else to “match all,” which absorbs security-relevant path-filter detail. Fix: either compile a real regex-to-extension extractor for recognized forms or reject regex selectors that cannot be lowered conservatively.
16. CRITICAL | `libs/tools/surgec/src/compile/types.rs:52` | `CompiledDocument::program_by_name` returns the first matching rule name across all indexes, so duplicate scanner rule names resolve nondeterministically to the wrong `Program`. Fix: make scanner rule names globally unique and store a direct name→(index,offset) map built at compile time.
17. CRITICAL | `libs/tools/surgec/src/compile/compile.rs:139` | Scanner rule names are derived only from `rule.name` and `clause_index`, with no artifact/package namespace, so two artifacts that both define `rule sample { ... }` produce colliding internal names and corrupt dispatch lookup. Fix: include artifact identity (or a stable rule UUID) in every generated scanner rule name.
18. HIGH | `libs/tools/surgec/src/compile/bundle.rs:76` | Bundle writes always use `path.with_extension("srgb.tmp")`, so concurrent writers to the same target can clobber each other’s temp file and publish mixed output. Fix: use a unique temp name per write (PID + counter/random suffix) and rename atomically after fsync.
19. HIGH | `libs/tools/surgec/src/compile/bundle.rs:103` | `CompiledDocument::load` reads the entire bundle into memory before validating its header, so a hostile bundle path can force unbounded allocation before basic sanity checks run. Fix: stream-read and validate the fixed header first, enforce payload caps, then map or bounded-read the remainder.
20. CRITICAL | `libs/tools/surgec/src/compile/bundle.rs:159` | `payload_size = payload_size.saturating_add(...)` masks size overflow by pinning at `u64::MAX`, which can yield a self-inconsistent bundle header instead of failing loudly. Fix: use checked addition and reject oversized bundles with a structured compile error.
21. HIGH | `libs/tools/surgec/src/index/serialize.rs:34` | `CompiledRuleIndex::load` also reads the whole file before checking the magic header or declared lengths, leaving index loading open to the same hostile-file OOM pattern. Fix: parse the header and bounded lengths from a streaming reader before allocating the full payload.
22. HIGH | `libs/tools/surgec/src/index/serialize.rs:233` | `read_u32_vec` allocates `count` entries directly from attacker-controlled length fields with no index-specific cap, so a corrupted index can request arbitrarily large vectors before semantic validation runs. Fix: enforce maximum counts for every vector field based on compiled-index invariants before allocating.
23. MEDIUM | `libs/tools/surgec/src/index/serialize.rs:263` | `read_string` copies every string payload into a fresh `Vec<u8>` before UTF-8 decode, doubling allocation churn on large rule packs. Fix: decode from borrowed slices when possible and reserve/copy only once into the target `String`.
24. MEDIUM | `libs/tools/surgec/src/index/builder.rs:154` | `program_by_rule_name` linearly scans `rules` on every lookup, turning repeated dispatch-time lookups into O(rules) name resolution instead of O(1). Fix: precompute a `HashMap<&str, usize>` or interned-name index inside `CompiledRuleIndex`.
25. MEDIUM | `libs/tools/surgec/src/index/builder.rs:167` | `materialize_matches` clones `rule_name` and `tags` for every hit, even though they are immutable index metadata. Fix: return borrowed metadata or interned handles so hot-path result materialization stops reallocating strings and vectors.
26. HIGH | `libs/tools/surgec/src/output/vyre.rs:25` | `build_clause_rule_index` discards the first build error and immediately retries via recursive splitting, which can hide non-budget compiler bugs behind a batching fallback. Fix: inspect the failure reason and split only on explicit over-budget diagnostics; otherwise surface the original error unchanged.
27. HIGH | `libs/tools/surgec/src/output/vyre.rs:103` | `rule_id: rule_id as u32` and `rule_list.push(rule_id as u32)` truncate `usize` to `u32` without a checked conversion, so very large clause batches wrap rule ids and corrupt scatter mappings. Fix: use `u32::try_from(rule_id)` and fail loudly when the batch exceeds the wire-format range.
28. MEDIUM | `libs/tools/surgec/src/scan/collector.rs:421` | `clause_program_and_patterns` clones the `Program` and every matched `CompiledPattern` into each `ClauseDispatchPlan`, so the collector duplicates immutable compile artifacts before scanning a single file. Fix: store shared references/Arcs into the plan instead of cloning rule programs and pattern vectors.
29. CRITICAL | `libs/tools/surgec/src/scan/collector.rs:462` | `usize::try_from(*max_id).unwrap_or(0)` silently maps an oversized `string_id` to slot 0, which aliases unrelated signals rather than failing. Fix: convert with `try_from` and return a structured error when `string_id` exceeds the host index range.
30. HIGH | `libs/tools/surgec/src/scan/collector.rs:484` | `counts[signal_index]` records the full hit count even though offsets/lengths are truncated to `MAX_CACHED_POSITIONS`, so downstream predicates can observe a hit cardinality larger than the number of cached offsets they can safely read. Fix: either cap `counts` to the cached-position budget or carry an explicit overflow flag and matching contract.
31. HIGH | `libs/tools/surgec/src/scan/collector.rs:523` | Any regex-backed pattern aborts the entire scan path with a host error, which directly contradicts the universal-engine claim that regex families are first-class scan inputs. Fix: route regex patterns through a GPU regex backend in this path or pre-partition literal and regex signals instead of hard-failing the rule.
32. HIGH | `libs/tools/surgec/src/scan/collector.rs:571` | `dfa_compile_with_budget` runs per file and per signal family, so identical literal sets are recompiled repeatedly instead of being cached once per clause index. Fix: cache compiled DFAs by literal-set fingerprint and reuse them across files and layers.
33. MEDIUM | `libs/tools/surgec/src/scan/decode.rs:132` | Hitting `max_total_bytes` returns from `decode_recursive` immediately, which aborts every remaining region and encoding in the current layer with no overflow marker and hands attackers an evasion knob. Fix: stop adding new layers for the offending branch but continue scanning siblings while emitting a “decode budget exhausted” signal.
34. MEDIUM | `libs/tools/surgec/src/scan/decode.rs:173` | Base64 extraction uses one permissive character predicate for both standard and URL-safe alphabets, so the `Base64` and `Base64Url` passes both accept mixed runs and mislabel provenance. Fix: validate run membership against the selected alphabet before decoding and reject mixed-alphabet blobs.
35. HIGH | `libs/tools/surgec/src/scan/decode.rs:212` | Hex-region extraction bails out on any non-UTF-8 input, even though `\\xHH` escapes are byte-oriented and commonly appear inside binary malware/config blobs. Fix: scan raw bytes for `\\xHH` sequences without requiring the whole layer to be valid UTF-8.
36. HIGH | `libs/tools/surgec/src/scan/decode.rs:266` | URL-decoding also requires the entire layer to be valid UTF-8, which means percent-encoded payloads embedded in arbitrary binary/network captures never enter the decode pipeline. Fix: parse `%HH` runs directly from bytes and only UTF-8 decode the extracted region if a later frontend needs text.
37. CRITICAL | `libs/tools/surgec/src/scan/decode.rs:314` | `decoder.take(16 * 1024 * 1024).read_to_end(&mut decoded)` treats a stream that hits the 16 MiB limit as success, so truncated zip bombs are accepted as valid decoded layers and handed to the scanner with incomplete content. Fix: distinguish “fully decoded” from “limit hit” and reject or mark truncated gzip outputs explicitly.
38. MEDIUM | `libs/tools/surgec/src/scan/dispatch.rs:143` | `dispatch_rules` reads only `outputs.first()` and silently ignores additional output buffers, which means any backend/program that emits structured side channels (lengths, evidence ids, overflow flags) loses them without error. Fix: validate the exact expected output arity per compiled clause and reject mismatches instead of truncating to the first buffer.
39. HIGH | `libs/tools/surgec/src/output/sarif.rs:210` | Unknown severities collapse to SARIF level `"none"`, which silently downgrades new severity classes instead of preserving review visibility. Fix: treat unknown severities as a serialization error or map them conservatively to `"warning"` with explicit metadata.
40. HIGH | `libs/tools/surgec/src/output/sarif.rs:236` | Primary SARIF results always emit `region: None`, so byte-level evidence from `result_slots` never reaches downstream code-scanning consumers. Fix: materialize concrete byte offsets/lengths into the primary SARIF location for every finding that has scanner coordinates.
41. HIGH | `libs/tools/surgec/src/output/sarif.rs:304` | `attach_chains` keys chain attachment only on `(rule_id, file URI)`, so multiple same-rule findings in one file all inherit the same chain whether or not they correspond to the same slot/path. Fix: include clause index and concrete byte region in the join key before attaching exploit flows.
42. HIGH | `libs/tools/surgec/src/compile/chains/rules.rs:145` | `RuleSet::load_dir` walks every TOML file under the directory with no cap on file count or file size, so a hostile rule bundle can force unbounded parse work and memory. Fix: enforce maximum file count, maximum file size, and maximum aggregate bytes before parsing.
43. MEDIUM | `libs/tools/surgec/src/compile/chains/rules.rs:168` | Each TOML file is parsed into `toml::Value` for probing and then reparsed into one or more concrete structs, which multiplies CPU and allocation cost on large rule packs. Fix: parse once into a tagged schema or inspect the raw table after a single deserialize pass.
44. HIGH | `libs/tools/surgec/src/compile/chains/rules.rs:277` | Composition validation allows duplicate primitives in `rule.inputs`, which inflates tuple products and can synthesize self-joins the author never intended. Fix: reject duplicate input primitive ids inside one composition rule.
45. CRITICAL | `libs/tools/surgec/src/compile/chains/engine.rs:174` | `rule_tuples` builds the full Cartesian product of provider findings before evaluating constraints, which is exponential in rule arity and trivial to weaponize with large primitive fan-outs. Fix: push constraint evaluation into tuple generation and prune incrementally instead of materializing the full product.
46. MEDIUM | `libs/tools/surgec/src/compile/chains/engine.rs:244` | `collect_tuples` clones `current` into a new `Vec` at every leaf, amplifying allocation churn exactly where tuple growth is already worst. Fix: stream tuples to the evaluator or reuse a scratch arena rather than cloning per leaf.
47. MEDIUM | `libs/tools/surgec/src/compile/chains/engine.rs:255` | `compose_chain` clones every prior step and finding id from every input chain on every successful tuple, turning deep-chain composition into repeated whole-chain copying. Fix: store chain DAG nodes behind shared ownership and build composed chains by reference.
48. HIGH | `libs/tools/surgec/src/compile/chains/engine.rs:286` | `composed_finding` copies `file_path`, `offset`, and `subsystem` from only the first tuple element, so cross-file/cross-subsystem chains are emitted with misleading provenance. Fix: carry the full contributing finding set (or a structured proof graph) into composed findings instead of collapsing to the first record.
49. CRITICAL | `libs/tools/surgec/src/scan/exploit_graph.rs:295` | `build_directed_edges` compares every node against every later node, making exploit-graph construction O(n²) in findings before any GPU primitive runs. Fix: index candidate transitions by primitive and order bucket so edge generation is proportional to reachable successor classes, not all pairs.
50. HIGH | `libs/tools/surgec/src/scan/exploit_graph.rs:343` | `bucket.dedup_by_key(|(to, _)| *to)` discards duplicate edges solely by destination id, so if two compositions produce the same `to` with different weights the surviving confidence is arbitrary. Fix: coalesce duplicates by a defined reducer (for example max confidence) instead of dropping by key.
51. HIGH | `libs/tools/surgec/src/scan/exploit_graph.rs:388` | `gpu_components` launches forward closure, backward closure, and SCC decomposition for every pivot node, which is O(V) GPU dispatch rounds for one connected-components pass. Fix: batch SCC/CC computation per component or run one graph-wide decomposition primitive instead of per-pivot traversals.
52. HIGH | `libs/tools/surgec/src/scan/exploit_graph.rs:467` | `best_path` recomputes a GPU closure for every candidate source and then runs CPU BFS for every reachable source-sink pair, which compounds into a dispatch storm on dense components. Fix: precompute reachability once per component and derive parent trees with one shortest-path pass per source or sink set.
53. MEDIUM | `libs/tools/surgec/src/scan/exploit_graph.rs:575` | `gpu_closure` allocates a fresh `new_frontier` vector on every graph step, creating avoidable allocator churn in a tight iterative traversal loop. Fix: reuse two frontier buffers and swap them in place.
54. HIGH | `libs/tools/surgec/src/scan/exploit_graph.rs:651` | `decode_u32_buffer` silently truncates any excess words with `.take(expected_words)`, which hides output-size mismatches and wire-format drift in graph primitives. Fix: require exact word counts unless an explicit trailing-layout contract exists.
55. HIGH | `libs/tools/surgec/src/scan/exploit_graph.rs:673` | `chain_id` hashes only path, rule name, and clause index, omitting offsets, decode-layer provenance, and primitive weights, so distinct chains in the same file can collapse to the same id. Fix: include concrete locations and ordered edge metadata in the chain fingerprint.
56. HIGH | `libs/performance/matching/vyre/vyre-driver-wgpu/src/shaders/aho_corasick_scan.wgsl:28` | `emit_match` increments `match_count` before checking the cap, so overflowed scans still advance the counter and report a larger match cardinality than the number of materialized records. Fix: clamp with a compare/exchange loop or maintain a separate dropped-match counter while preserving an exact stored count.
57. HIGH | `libs/performance/matching/vyre/vyre-driver-wgpu/src/shaders/aho_corasick_scan.wgsl:38` | The Aho-Corasick kernel runs with `@workgroup_size(1,1,1)` and copies a 256-word transition row plus a `workgroupBarrier()` on every byte, guaranteeing catastrophic underutilization and needless synchronization on the hottest scan path in the engine. Fix: parallelize across haystack partitions/subgroups and keep transition data in read-only storage/cache-friendly form instead of copying it byte-by-byte through workgroup memory.
58. CRITICAL | `libs/performance/matching/vyre/vyre-driver-wgpu/src/lowering/subgroup_intrinsics.rs:137` | The subgroup broadcast fallback always returns `shared[0u]`, ignoring the source-lane argument entirely and producing wrong results whenever the broadcast source is not lane 0. Fix: thread the requested source lane through the fallback and read the corresponding shared slot.
59. CRITICAL | `libs/performance/matching/vyre/vyre-driver-wgpu/src/lowering/subgroup_intrinsics.rs:144` | The shuffle-xor fallback hardcodes `^ 1u` instead of using the caller’s XOR mask, so every non-`1` butterfly stage is wrong on adapters without native subgroup support. Fix: parameterize the fallback by the actual mask and preserve subgroup shuffle semantics exactly.
60. HIGH | `libs/performance/matching/vyre/vyre-driver-wgpu/src/engine/graph.rs:17` | `GraphOp` can carry only a single `Vec<u8>` input, which leaks a misleading API surface for “dispatch graphs” even though real vyre pipelines routinely require multiple buffers and params. Fix: model graph nodes as the same multi-buffer binding contract used by `WgpuPipeline` rather than a single opaque byte blob.
61. MEDIUM | `libs/performance/matching/vyre/vyre-driver-wgpu/src/lib.rs:75` | The public docs still claim “If no GPU is available, construction returns `None`” even though `WgpuBackend::acquire/new` return `Result`, which is an API-surface lie at exactly the point callers need reliable recovery semantics. Fix: update the docs to reflect the actual `Result` contract and remove all stale `None` wording.
62. MEDIUM | `libs/performance/matching/vyre/vyre-runtime/src/pipeline_cache.rs:30` | `PipelineFingerprint::of` clones the full `Program` just to canonicalize it, so cache lookup on large IR pays an avoidable deep copy before hashing. Fix: canonicalize through a borrowed or copy-on-write transform so hashing does not duplicate the whole program graph.
63. MEDIUM | `libs/performance/matching/vyre/vyre-runtime/src/pipeline_cache.rs:59` | The default `PipelineCacheStore::get` clones the cached artifact payload on every hit, which reintroduces allocation/copy overhead into the exact hot path the cache is supposed to remove. Fix: make `Arc<Vec<u8>>` the primary retrieval API and remove the cloning default from hot consumers.
64. MEDIUM | `libs/performance/matching/vyre/vyre-runtime/src/pipeline_cache.rs:137` | `LayeredPipelineCache` returns the first lower-layer hit but never warms the faster front layer, so repeated lookups keep missing the RAM tier and pay the slower backend every time. Fix: promote lower-layer hits into the first writable layer on read.
65. MEDIUM | `libs/performance/matching/vyre/vyre-driver-wgpu/src/pipeline_persistent.rs:216` | `legacy_handles_from_inputs` does a `.position(...)` scan over `input_bindings` for every binding, turning binding resolution into O(n²) work in a dispatch helper that already runs on every legacy submission. Fix: precompute binding→input-index once per pipeline and reuse it for all dispatches.

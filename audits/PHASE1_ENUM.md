CRITICAL | libs/performance/io/walkkit/src/walk_common.rs:15 | blocking sync stat syscall per file instead of io_uring-batched statx | Replace std::fs::metadata with io_uring sqe multipolling or tokio-uring::statx batching.
CRITICAL | libs/performance/io/walkkit/src/walk_common.rs:165 | blocking sync read_dir syscall per directory instead of io_uring getdents batch | Replace std::fs::read_dir with io_uring-batched directory enumeration via tokio-uring or rustix+io_uring.
CRITICAL | libs/tools/surgec/src/scan/collector.rs:203 | blocking sync WalkDir iteration with per-entry stat instead of io_uring | Replace walkdir with io_uring-batched directory walker that coalesces statx and read operations.
CRITICAL | libs/tools/surgec/src/scan/collector.rs:1280 | fs::read copies entire file into host DRAM Vec with zero NVMe→GPU bypass | Implement GPUDirect Storage zero-copy path via vyre-runtime io_uring/gpudirect so file pages map straight into VRAM.
CRITICAL | libs/tools/surgec/src/scan/collector.rs:1221 | pack_bytes_as_u32_words expands host DRAM 4× instead of zero-copy GPU packing | Replace host-side 4× byte expansion with a vyre Program that packs u32 words in VRAM at dispatch time.
HIGH | libs/performance/io/walkkit/src/gitignore_ctx.rs:229 | gitignore matching walks a CPU linked-list of ancestors per path instead of a compiled vyre Program | Flatten gitignore rules into a single vyre Program and run path matching on the GPU in one batched dispatch.
HIGH | libs/performance/io/walkkit/src/codewalker/mod.rs:262 | UTF-8 validation is single-shot host-CPU String::from_utf8 with no SIMD or GPU | Compile a vyre utf8_validate Program and run it on the GPU against batched file chunks.
HIGH | libs/performance/io/walkkit/src/codewalker/mod.rs:386 | per-chunk heap allocation of vec![0_u8; 64 KiB] for every file read | Replace Vec::with_capacity with a thread-local or slab-allocated reusable buffer pool.
HIGH | libs/tools/surgec/src/scan/collector.rs:751 | build_clause_inputs allocates fresh counts/offsets/lengths Vecs per file per clause | Pre-allocate clause input buffers in a Collector-scoped arena and reuse them across files.
HIGH | libs/scanner/polyglot/src/detect.rs:87 | scan() allocates a new HashSet and Vec for every file instead of reusing scratch buffers | Replace per-call HashSet/Vec with a thread-local reused bitset and pre-sorted output slice.
HIGH | libs/tools/surgec/src/scan/collector.rs:1406 | FileCollector validates UTF-8 with per-byte host-CPU std::str::from_utf8 instead of GPU | Replace std::str::from_utf8 with a batched vyre utf8_validate Program dispatch across collected files.
HIGH | libs/performance/io/walkkit/src/walk_common.rs:171 | read_dir_sorted pushes a new heap-allocated PathBuf for every directory entry | Store directory entries as inline path slices inside a pre-allocated bump buffer or reuse a Vec with clear().
MEDIUM | libs/performance/io/walkkit/src/worker.rs:329 | walk_root PathBuf is cloned for every child directory pushed to the work queue | Store walk_root as an Arc<Path> or index into a shared path arena to eliminate per-child clones.
MEDIUM | libs/performance/io/walkkit/src/worker.rs:352 | visited_files HashSet inserts clone the full PathBuf into the heap on every file | Replace HashSet<PathBuf> with a HashSet of inode/dev tuples or path arena indices.
MEDIUM | libs/tools/surgec/src/scan/collector.rs:754 | packed_haystack and per-clause inputs allocate fresh host Vecs per file instead of GPU staging buffers | Bind file bytes directly to GPU buffers and let the vyre backend handle alignment; remove host-side packing.
MEDIUM | libs/performance/matching/vyre/vyre-driver/src/registry/io.rs:9 | zero-copy NVMe→GPU ops declared but no backend opts in, forcing host DRAM bounce | Implement the vyre-dialect-io crate with io_uring+GPUDirect Storage lowerings and register it in the driver.

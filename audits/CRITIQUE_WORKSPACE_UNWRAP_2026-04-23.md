# Workspace unsafe/unwrap/expect audit - 2026-04-23

Scope: `libs/` non-test Rust code. Pattern hunt: `unsafe {`, `.unwrap()`, `.expect(`, `todo!`, `unimplemented!`, `panic!` outside `#[cfg(test)]`.

Skipped crates: `vyre-foundation`, `surgec`, `vyre-driver`, `vyre-driver-wgpu`, `vyre-runtime`, `pocgen`, `jsir`, `polyglot`, `procjail`, `authjar`, `deobfuscate`, `safecheckpoint`, `santh-error`.

Noise filtered from this report: test directories/files, `#[cfg(test)]` blocks inside `src/`, fuzz harnesses, `future_work/`, `corpus/`, comment-only hits, and Surge parser `self.expect(...)` calls that are parser API methods rather than panic-prone `Result::expect(...)`.

## Summary

- Total findings: **1005**
- CRITICAL: **300**
- HIGH: **705**

## Focus crates

| Crate | Findings |
| --- | ---: |
| `karyx` | 0 |
| `soleno` | 0 |
| `keyhog` | 0 |
| `gossan` | 0 |
| `wafrift` | 0 |
| `attackstr` | 0 |
| `scanwire` | 0 |
| `gpumatch` | 0 |
| `interactsh` | 3 |
| `stealthreq` | 0 |
| `codewalk` | 4 |

## Crate counts

| Crate | Findings |
| --- | ---: |
| `warpstate` | 219 |
| `warpgrep` | 75 |
| `dfajit` | 67 |
| `kernelkit` | 56 |
| `vyre-libs` | 47 |
| `simdsieve` | 44 |
| `wireshift` | 39 |
| `mempool` | 37 |
| `warpscan-ingest` | 35 |
| `walkkit` | 31 |
| `tenshift-core` | 28 |
| `warpscan` | 27 |
| `cudagrep` | 24 |
| `flashsieve` | 22 |
| `wireshift-uring` | 21 |
| `ebpfkit` | 21 |
| `dedup` | 18 |
| `secmatch` | 16 |
| `vyre-reference` | 14 |
| `xtask` | 13 |
| `secir` | 11 |
| `secreport` | 11 |
| `scanstate` | 10 |
| `surgec-grammar-gen` | 8 |
| `ebpfsieve` | 7 |
| `vyre-conform-runner` | 7 |
| `mlgraph` | 6 |
| `blockscan` | 6 |
| `openpack` | 6 |
| `trigramkit` | 5 |
| `vyre-intrinsics` | 5 |
| `scanclient` | 5 |
| `secfinding` | 5 |
| `blindtime` | 4 |
| `pyrograph` | 4 |
| `encodex` | 4 |
| `gpudeflate` | 4 |
| `codewalk` | 4 |
| `interactsh` | 3 |
| `ziftsieve` | 3 |
| `manifestkit` | 3 |
| `rulefire` | 3 |
| `secjit` | 3 |
| `prospect` | 2 |
| `gpudecode` | 2 |
| `ratelimit` | 2 |
| `libs-template` | 2 |
| `faultkit` | 2 |
| `respredict` | 2 |
| `matchcorr` | 1 |
| `rulekit` | 1 |
| `wireshift-fallback` | 1 |
| `matchkit` | 1 |
| `vyre-conform-spec` | 1 |
| `vyre-test-harness` | 1 |
| `vyre-frontend-c` | 1 |
| `yaragpu` | 1 |
| `proxywire` | 1 |
| `vulnir` | 1 |
| `surge` | 1 |
| `warpscan-coord` | 1 |

## Findings

- HIGH | libs/general/mlgraph/examples/vit_optimize.rs:33 | `let graph = build_vit(&config).expect("failed to build ViT graph");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/general/mlgraph/examples/vit_optimize.rs:49 | `.expect("analysis failed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/general/mlgraph/examples/vit_optimize.rs:99 | `let mut fused_graph = build_vit(&config).expect("failed to rebuild graph");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/general/mlgraph/examples/vit_optimize.rs:103 | `.expect("attention fusion failed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/general/mlgraph/examples/vit_optimize.rs:108 | `.expect("FFN fusion failed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/general/mlgraph/examples/vit_optimize.rs:126 | `.expect("analysis failed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/general/prospect/src/bandit/mod.rs:612 | `Err(error) => panic!("failed to seed ChaCha8Rng from system RNG: {error}"),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/general/prospect/src/game_search/mod.rs:875 | `Err(error) => panic!("failed to seed ChaCha8Rng from system RNG: {error}"),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/offensive/blindtime/examples/blind_sqli.rs:13 | `.expect("valid timing config");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/offensive/blindtime/examples/debug_bootstrap.rs:7 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/offensive/blindtime/examples/permutation_debug.rs:7 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/offensive/blindtime/src/statistics/bootstrap.rs:161 | `Err(error) => panic!("failed to seed SmallRng from system RNG: {error}"),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/offensive/interactsh/examples/basic.rs:24 | `let (mut socket, _) = listener.accept().await.expect("accept register");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/offensive/interactsh/examples/basic.rs:26 | `let _n = socket.read(&mut buf).await.expect("read request");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/offensive/interactsh/examples/basic.rs:30 | `.expect("write response");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:35 | `let hasher = MinHasher::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:43 | `black_box(hasher.compute_str(black_box(doc_short), 0).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:49 | `black_box(hasher.compute_str(black_box(doc_medium), 0).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:55 | `black_box(hasher.compute_str(black_box(&doc_long), 0).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:69 | `let hasher = MinHasher::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:73 | `black_box(hasher.compute_str(black_box(&doc), 0).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:85 | `let hasher = MinHasher::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:90 | `.map(|(i, doc)| hasher.compute_str(doc, i).unwrap())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:95 | `let mut index = LshIndex::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:109 | `let hasher = MinHasher::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:114 | `let sig1 = hasher.compute_str(doc1, 0).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:115 | `let sig2 = hasher.compute_str(doc2, 1).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:138 | `let hasher = MinHasher::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:139 | `let mut index = LshIndex::new(&config).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/benches/minhash.rs:142 | `let sig = hasher.compute_str(doc, i).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/dedup/examples/basic.rs:23 | `let mut transformer = DedupTransformer::new(config).expect("Failed to create transformer");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/analysis/dedup/examples/basic_dedup.rs:23 | `let mut transformer = DedupTransformer::new(config).expect("Failed to create transformer");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/analysis/dedup/examples/streaming_dedup.rs:21 | `.expect("Failed to create transform")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/analysis/matchcorr/src/bloom.rs:53 | `panic!(` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/analysis/pyrograph/src/parse/go/visitor.rs:82 | `.expect("scope stack not empty")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/analysis/pyrograph/src/parse/js/mod.rs:428 | `let graph = parse_js(js, "test.js").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/pyrograph/src/parse/js/scope.rs:34 | `self.scopes.last_mut().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/analysis/pyrograph/src/parse/python/scope.rs:88 | `self.scopes.last_mut().expect("scope stack is initialized")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/compression/encodex/src/content_type.rs:331 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/compression/encodex/src/content_type.rs:345 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/compression/encodex/src/content_type.rs:353 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/compression/encodex/src/hex.rs:69 | `if let Ok(byte) = hashkit::hex::decode(std::str::from_utf8(&[c1, c2]).unwrap()) {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/compression/gpudeflate/benches/throughput.rs:8 | `encoder.write_all(payload).expect("gzip write");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/compression/gpudeflate/benches/throughput.rs:9 | `encoder.finish().expect("gzip finish")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/compression/gpudeflate/benches/throughput.rs:13 | `let decompressor = GpuDecompressor::new().expect("decompressor");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/compression/gpudeflate/benches/throughput.rs:25 | `.expect("decompress")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/compression/ziftsieve/benches/ziftsieve_bench.rs:80 | `ziftsieve::lz4::extract_literals(black_box(&compressed), 4 * 1024 * 1024).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/compression/ziftsieve/benches/ziftsieve_bench.rs:89 | `lz4_flex::block::decompress(black_box(&compressed), data.len()).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/compression/ziftsieve/benches/ziftsieve_bench.rs:107 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/gpu/cudagrep/benches/release_bench.rs:314 | `device_pointer: unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/gpu/cudagrep/benches/release_bench.rs:321 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/gpu/cudagrep/benches/release_bench.rs:333 | `let ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/gpu/cudagrep/benches/release_bench.rs:340 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/gpu/cudagrep/src/availability.rs:219 | `let cuda_lib = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/availability.rs:228 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/batch.rs:25 | `let result = unsafe { (self.api.batch_io_setup)(batch_params, count) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/batch.rs:50 | `let result = unsafe { (self.api.batch_io_submit)(batch_params, count, handle, flags) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/batch.rs:86 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/batch.rs:109 | `let result = unsafe { (self.api.batch_io_cancel)(batch_id) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:34 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:71 | `let result = unsafe { (self.api.driver_open)() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:86 | `let result = unsafe { (self.api.driver_close)() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:101 | `let result = unsafe { (self.api.driver_set_max_pinned_mem_size)(size) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:116 | `let result = unsafe { (self.api.driver_set_max_cache_size)(size) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:131 | `let symbol = unsafe { library.get::<T>(symbol)? };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:141 | `let symbol = unsafe { library.get::<T>(symbol).ok()? };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/init.rs:183 | `let status = unsafe { get_version(std::ptr::addr_of_mut!(raw_version)) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/read.rs:51 | `unsafe { (self.api.buf_register)(device_ptr.as_mut_ptr().cast_const(), size, 0) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/read.rs:67 | `let result = unsafe { (self.api.buf_deregister)(device_ptr.as_mut_ptr().cast_const()) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/read.rs:90 | `let result = unsafe { (self.api.handle_register)(&raw mut handle, &raw mut descriptor) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/read.rs:109 | `let result = unsafe { (self.api.handle_deregister)(handle) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/cufile/library/read.rs:142 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/gpu/cudagrep/src/hardware/dma.rs:305 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/gpu/gpudecode/src/cpu.rs:130 | `let hi = hex_value(input[read_cursor]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/gpu/gpudecode/src/cpu.rs:131 | `let lo = hex_value(input[read_cursor + 1]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/indexing/flashsieve/benches/filter_throughput.rs:40 | `let mut bloom = NgramBloom::new(BLOOM_BITS).unwrap_or_else(|error| panic!("{error}"));` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/indexing/flashsieve/benches/filter_throughput.rs:49 | `NgramBloom::from_block(&block, BLOOM_BITS).unwrap_or_else(|error| panic!("{error}"));` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/indexing/flashsieve/benches/filter_throughput.rs:73 | `.unwrap_or_else(|error| panic!("{error}"));` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/indexing/flashsieve/benches/filter_throughput.rs:102 | `.unwrap_or_else(|error| panic!("{error}"));` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:51 | `let mut bloom = NgramBloom::new(size).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:68 | `let mut bloom = NgramBloom::new(size).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:93 | `let mut bloom = NgramBloom::new(size).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:116 | `let mut bloom = NgramBloom::new(size).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:180 | `let bloom = NgramBloom::from_block(data, 8192).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:250 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:279 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:302 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:332 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:361 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:396 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:422 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:430 | `let index = BlockIndex::from_bytes(bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:449 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:457 | `let recovered = BlockIndex::from_bytes(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:479 | `let mut bloom = NgramBloom::new(size).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:528 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/flashsieve/benches/release_bench.rs:531 | `let bloom = NgramBloom::from_block(&data[..256], 1024).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/manifestkit/debug_insert.rs:5 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/manifestkit/debug_insert.rs:6 | `let mut manifest = Manifest::open(dir.path().join("test.db")).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/manifestkit/debug_insert.rs:21 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/indexing/rulekit/benches/parse_rules.rs:19 | `let file = rulekit::RuleFile::parse_str(black_box(&input)).expect("benchmark parse");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/indexing/trigramkit/src/builder.rs:330 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/indexing/trigramkit/src/builder.rs:371 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/indexing/trigramkit/src/persistent.rs:293 | `let entries: &'static [IndexEntry] = unsafe { std::mem::transmute(entries_slice) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/indexing/trigramkit/src/persistent.rs:346 | `let slice = unsafe { std::slice::from_raw_parts(self.data.add(start), len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/indexing/trigramkit/src/persistent.rs:421 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/io/ratelimit/benches/token_bucket.rs:7 | `let bucket = TokenBucket::new(1_000_000.0, 1_000_000).expect("benchmark bucket");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/ratelimit/benches/token_bucket.rs:18 | `let throttle = BandwidthThrottle::new(10_000_000).expect("benchmark throttle");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/walkkit/src/lib.rs:202 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:204 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:210 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:211 | `std::fs::write(dir.path().join("test.txt"), "content").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:213 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:220 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:222 | `std::fs::write(dir.path().join(format!("f{i}.js")), "content").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:225 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:231 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:233 | `std::fs::create_dir_all(&nested).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:234 | `std::fs::write(nested.join("deep.js"), "deep content").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:235 | `std::fs::write(dir.path().join("top.js"), "top content").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:237 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:243 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:244 | `std::fs::write(dir.path().join("sized.txt"), "12345").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:246 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:253 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:254 | `std::fs::write(dir.path().join(".hidden"), "secret").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:255 | `std::fs::write(dir.path().join("visible"), "open").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:257 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:263 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:264 | `std::fs::write(dir.path().join("a.txt"), "a").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:266 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:273 | `let items: Vec<_> = walker.walk().unwrap().collect();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:287 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:288 | `let target = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:289 | `std::fs::write(target.path().join("target.txt"), "data").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:293 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:299 | `let dir = tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:301 | `std::fs::write(dir.path().join(format!("file{i:04}.txt")), "x").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/walkkit/src/lib.rs:304 | `let files: Vec<_> = files_only(walker.walk().unwrap().collect());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:23 | `.expect("clock")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:26 | `fs::create_dir_all(&root).expect("dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:30 | `fs::write(&path, "payload-data-here-0123456789".repeat(100)).expect("write");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:41 | `.expect("clock")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:45 | `.map(|i| u8::try_from(i % 256).expect("value is reduced modulo 256"))` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:47 | `fs::write(&path, &data).expect("write");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:62 | `.expect("ring creation");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:71 | `let ring = Ring::new(RingConfig::default()).expect("ring creation");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:90 | `.expect("ring");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:93 | `let req = ring.submit(Nop).expect("submit");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:94 | `req.wait(None).expect("wait");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:107 | `let req = ring.submit(Nop).expect("submit");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:108 | `let _: () = req.wait(None).expect("wait");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:128 | `.expect("ring");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:131 | `u64::try_from(*batch_size).expect("batch size fits in u64"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:136 | `let results = ring.batch(ops).expect("batch");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:157 | `u64::try_from(size_bytes).expect("benchmark size fits in u64"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:163 | `.expect("ring");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:167 | `let file = File::open(&path).expect("open");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:169 | `.submit(Read::new(file, 0, size_bytes).expect("read creation"))` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:170 | `.expect("submit");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:171 | `let result = req.wait(None).expect("wait");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:185 | `let file = File::open(&path).expect("open");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:187 | `.submit(Read::new(file, 0, size_bytes).expect("read creation"))` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:188 | `.expect("submit");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:189 | `let result = req.wait(None).expect("wait");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:202 | `let file_size = fs::metadata(&paths[0]).expect("meta").len() as u64;` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:210 | `.expect("ring");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:217 | `File::open(path).expect("open"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:219 | `usize::try_from(file_size).expect("file size fits in usize"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:221 | `.expect("op")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:223 | `.expect("batch");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:240 | `File::open(path).expect("open"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:242 | `usize::try_from(file_size).expect("file size fits in usize"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:244 | `.expect("op")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:246 | `.expect("batch");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:296 | `let pool = wireshift::BufferPool::new(4096, 100).expect("pool creation");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:300 | `let buf = pool.acquire().expect("acquire");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/io/wireshift/benches/wireshift_bench.rs:302 | `let completed = submitted.into_completed(100).expect("complete");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/io/wireshift/fallback/src/ops/read.rs:203 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/buffer_registry.rs:27 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/capabilities.rs:54 | `let mut probe = unsafe { std::mem::zeroed::<Self>() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/capabilities.rs:69 | `let rc = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/helpers.rs:39 | `let mut storage: libc::sockaddr_storage = unsafe { std::mem::zeroed() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/helpers.rs:50 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/helpers.rs:69 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/helpers.rs:263 | `let file = unsafe { std::fs::File::from_raw_fd(fd) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/helpers.rs:299 | `let stream = unsafe { std::net::TcpStream::from_raw_fd(socket_fd) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/helpers.rs:305 | `let stream = unsafe { std::net::TcpStream::from_raw_fd(fd) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/lib.rs:158 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/io/wireshift/uring/src/lib.rs:174 | `std::alloc::Layout::from_size_align(size, 4096).unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/io/wireshift/uring/src/lib.rs:251 | `let written = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/lib.rs:310 | `let limit_result = unsafe { libc::getrlimit(libc::RLIMIT_MEMLOCK, &mut memlock_limit) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/lib.rs:368 | `let written = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/manager.rs:39 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/manager.rs:281 | `let fd = unsafe { std::os::fd::BorrowedFd::borrow_raw(wakeup_fd) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/sqe_builder/file_ops.rs:127 | `let statx_buf: Box<libc::statx> = Box::new(unsafe { std::mem::zeroed() });` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/sqe_builder/file_ops.rs:142 | `let statx_buf = unsafe { Box::from_raw(statx_ptr) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/sqe_builder/net_ops.rs:26 | `let socket_fd = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/types.rs:62 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/io/wireshift/uring/src/uring_sys.rs:12 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/core_detect.rs:34 | `let euid = unsafe { libc::geteuid() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/prog.rs:74 | `let fd = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/prog.rs:96 | `let res = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:87 | `let fd = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:131 | `let value = unsafe { libc::sysconf(libc::_SC_PAGESIZE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:152 | `let rc = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:166 | `Ok(unsafe { info.assume_init() })` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:216 | `let fd = unsafe { libc::epoll_create1(libc::EPOLL_CLOEXEC) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:228 | `let rc = unsafe { libc::epoll_ctl(self.0, libc::EPOLL_CTL_ADD, map_fd, &mut event) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:237 | `let rc = unsafe { libc::epoll_wait(self.0, &mut event, 1, -1) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:247 | `let _ = unsafe { libc::close(self.0) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:262 | `let consumer_mapping = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:290 | `let producer_mapping = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:302 | `let _ = unsafe { libc::munmap(consumer_mapping, page_size) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:306 | `let data_ptr = unsafe { (producer_mapping as *const u8).add(page_size) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:318 | `unsafe { &*(self.consumer_mapping as *const AtomicU64) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:322 | `unsafe { &*(self.producer_mapping as *const AtomicU64) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:326 | `unsafe { &*(self.data_ptr.add(offset) as *const RingbufHeader) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:330 | `unsafe { std::slice::from_raw_parts(self.data_ptr.add(offset), len) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:336 | `let _ = unsafe { libc::munmap(self.consumer_mapping, self.page_size) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfkit/src/loader/ringbuf.rs:337 | `let _ = unsafe { libc::munmap(self.producer_mapping, self.mapping_len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfsieve/src/kernel.rs:170 | `let euid = unsafe { libc::geteuid() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/kernel/ebpfsieve/src/kernel.rs:179 | `let prog_fd = unsafe { std::os::fd::FromRawFd::from_raw_fd(raw_fd) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/kernel/ebpfsieve/src/kernel.rs:217 | `assert!(result.unwrap().is_none());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/kernel/ebpfsieve/src/kernel.rs:234 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/kernel/ebpfsieve/src/kernel.rs:235 | `let pat = byte_frequency_filter_to_literal_pattern(&filter).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/kernel/ebpfsieve/src/kernel.rs:242 | `let filter = crate::ByteFrequencyFilter::new([ByteThreshold::new(b'x', 2)]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/kernel/ebpfsieve/src/kernel.rs:243 | `let insns = compile_socket_filter_program(&filter).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/benches/jit_bench.rs:5 | `let mut table = TransitionTable::new(state_count, 256).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/examples/debug.rs:10 | `let jit = JitDfa::from_patterns(&pattern_refs).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/buffer.rs:44 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/buffer.rs:68 | `let func: JitFn = unsafe { std::mem::transmute(self.ptr) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/buffer.rs:69 | `let count = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/buffer.rs:92 | `let func: JitFn = unsafe { std::mem::transmute(self.ptr) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/buffer.rs:93 | `let count = unsafe { func(input.as_ptr(), input.len() as u64, std::ptr::null_mut(), 0) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:161 | `let page_size = unsafe { libc::sysconf(libc::_SC_PAGESIZE) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:167 | `let ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:187 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:197 | `let prot = unsafe { libc::mprotect(ptr, alloc_size, libc::PROT_READ | libc::PROT_EXEC) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:199 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:234 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:246 | `let ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:264 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:272 | `let prot = unsafe { libc::mprotect(ptr, page_size, libc::PROT_READ | libc::PROT_EXEC) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/dfajit/src/codegen/compile.rs:274 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:5 | `TransitionTable::new(state_count, class_count).unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:55 | `let jit = JitDfa::compile(&minimized).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:83 | `let original = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:89 | `let min_jit = JitDfa::compile(&minimized).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:138 | `let jit = JitDfa::from_regex_patterns(&["hello", "world"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:164 | `state0.iter().find(|r| r.0 == b'a').unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:168 | `state0.iter().find(|r| r.0 == b'A').unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:172 | `state0.iter().find(|r| r.0 == b'0').unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:224 | `let jit = JitDfa::compile(&minimized).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:245 | `let minimized = table.minimize().expect("should minimize");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:247 | `assert_eq!(JitDfa::compile(&minimized).unwrap().scan_count(b"xxx"), 3);` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:258 | `let minimized = table.minimize().expect("should minimize");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:260 | `assert_eq!(JitDfa::compile(&minimized).unwrap().scan_count(b"xxx"), 0);` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:272 | `assert_eq!(JitDfa::compile(&table).unwrap().scan_count(b"abc"), 3);` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:287 | `let restored = TransitionTable::from_bytes(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:294 | `let jit_orig = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:295 | `let jit_restored = JitDfa::compile(&restored).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:312 | `let large_jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:313 | `let minimized = table.minimize().expect("should minimize redundant states");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:316 | `let small_jit = JitDfa::compile(&minimized).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:334 | `let jit = Arc::new(JitDfa::from_patterns(&pattern_refs).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_advanced.rs:349 | `handle.join().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:5 | `TransitionTable::new(state_count, class_count).unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:25 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:33 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:46 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:54 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:62 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:70 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:78 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:79 | `let first = jit.scan_first(b"zzabzzab").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:87 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:123 | `let jit = JitDfa::compile(&table).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:133 | `let jit = JitDfa::from_patterns(&[b"foo", b"bar"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:148 | `let jit = JitDfa::from_patterns(&[b"", b"x"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:158 | `let restored = TransitionTable::from_bytes(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:165 | `let jit = JitDfa::compile(&restored).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:195 | `let jit = JitDfa::from_patterns(&[b"a", b"aa"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:204 | `let jit = JitDfa::from_patterns(&[b"a", b"aa"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:216 | `let jit = JitDfa::from_regex_patterns(&["a"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:236 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:239 | `let start = dfa.start_state_forward(&input).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:266 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:269 | `let start = dfa.start_state_forward(&input).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:306 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:308 | `let jit = JitDfa::from_regex_patterns(&[*pattern]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:316 | `let mut state = dfa.start_state_forward(&Input::new(input)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:342 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:345 | `let start = dfa.start_state_forward(&input).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/dfajit/src/tests_core.rs:358 | `let jit = JitDfa::from_regex_patterns(&["hello"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/matchkit/benches/matchset_bench.rs:37 | `let n_u32 = u32::try_from(n).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/rulefire/benches/compile_10k_rules.rs:13 | `let rules = RuleSet::parse(&source).expect("generated rules must parse");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/rulefire/benches/compile_10k_rules.rs:16 | `let program = black_box(rules.compile_to_vyre().expect("valid rules must compile"));` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/rulefire/benches/compile_10k_rules.rs:19 | `vyre::validate(&program).expect("compiled program must validate");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:42 | `let sieve = SimdSieve::new(&haystack, &[b"test"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:66 | `let sieve = SimdSieve::new(&haystack, &patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:92 | `let sieve = SimdSieve::new(&haystack, &patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:113 | `let sieve = SimdSieve::new(&haystack, &[b"test"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:121 | `let sieve = SimdSieve::new_case_insensitive(&haystack, &[b"test"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:144 | `let sieve = SimdSieve::new(&haystack, &pattern_refs).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:169 | `let sieve = SimdSieve::new(&haystack, &[&pattern]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:190 | `let sieve = SimdSieve::new(&haystack, &[b"test"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:216 | `let sieve = SimdSieve::new(&no_match_haystack, &[b"test"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/release_bench.rs:226 | `let sieve = SimdSieve::new(&all_match_haystack, &[b"a"]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/throughput.rs:39 | `let sieve = SimdSieve::new(black_box(&haystack), black_box(patterns)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/throughput.rs:66 | `let sieve = SimdSieve::new(black_box(&haystack), black_box(patterns)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/throughput.rs:83 | `let sieve = SimdSieve::new(black_box(&haystack), black_box(patterns)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/simdsieve/benches/throughput.rs:96 | `SimdSieve::new_case_insensitive(black_box(&haystack), black_box(patterns)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/simdsieve/src/avx2/mod.rs:96 | `patterns: unsafe { core::mem::zeroed() },` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx2/mod.rs:150 | `let mut patterns: [Avx2Pattern; 16] = unsafe { core::mem::zeroed() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx2/mod.rs:169 | `let bcast = unsafe { Self::build_broadcasts(arr) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx2/mod.rs:246 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx2/mod.rs:365 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx512.rs:102 | `let mut patterns: [Avx512Pattern; 16] = unsafe { core::mem::zeroed() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx512.rs:204 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx512.rs:225 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx512.rs:238 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx512.rs:251 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/avx512.rs:317 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/neon.rs:105 | `let mut patterns: [NeonPattern; 16] = unsafe { core::mem::zeroed() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/neon.rs:122 | `let bcast = unsafe { Self::build_broadcasts(arr) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/neon.rs:205 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/neon.rs:338 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/scalar.rs:210 | `let loaded0 = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/scalar.rs:218 | `let loaded1 = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/scalar.rs:226 | `let loaded2 = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/scalar.rs:234 | `let loaded3 = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/compiler.rs:81 | `return HardwareTier::Avx512(Box::new(unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/compiler.rs:86 | `return HardwareTier::Avx2(Box::new(unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/compiler.rs:93 | `return HardwareTier::Neon(Box::new(unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:109 | `dual_pump_main!(self, |chunk| unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:113 | `single_pump_block!(self, |chunk| unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:120 | `let (a, b) = unsafe { filter.check_64byte_block(chunk) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:124 | `single_pump_block!(self, |chunk| u64::from(unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:131 | `let (a, b) = unsafe { filter.check_64byte_block(chunk) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:135 | `single_pump_block!(self, |chunk| u64::from(unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:173 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/simdsieve/src/sieve/dispatch.rs:181 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/convergence_lens.rs:193 | `let current_count = ro_buffers.last().unwrap().count();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/convergence_lens.rs:194 | `let next_count = rw_buffers.last().unwrap().count();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/convergence_lens.rs:234 | `.map(|b| u32::from_le_bytes(b.try_into().expect("4-byte prefix")))` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/fp_parity.rs:92 | `let left = f32::from_bits(u32::from_le_bytes(left.try_into().expect("4 bytes")));` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/fp_parity.rs:93 | `let right = f32::from_bits(u32::from_le_bytes(right.try_into().expect("4 bytes")));` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-runner/src/lens.rs:385 | `.map(|buffer| u32::from_le_bytes(buffer[0..4].try_into().expect("4-byte prefix")))` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/conform/vyre-conform-runner/src/main.rs:106 | `panic!("Fix: dispatch result must stay serializable: {error}")` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/conform/vyre-conform-spec/src/witness.rs:29 | `.expect("Fix: blake3 produces >=8 bytes"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/conform/vyre-test-harness/src/lens.rs:412 | `.map(|buffer| u32::from_le_bytes(buffer[0..4].try_into().expect("4-byte prefix")))` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/examples/libs-template/src/lib.rs:67 | `let n = self.input.element_count().expect("checked above");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/examples/libs-template/src/lib.rs:105 | `.unwrap_or_else(|err| panic!("Fix: example_op build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-frontend-c/src/object_format.rs:37 | `.expect("section length fits u32")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-intrinsics/benches/intrinsic_perf.rs:26 | `.expect("Fix: intrinsic reference benchmark must execute");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-intrinsics/build.rs:20 | `let workspace = manifest.parent().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/vyre/vyre-intrinsics/build.rs:41 | `panic!("cannot read {} for category scan: {e}", path.display())` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-intrinsics/build.rs:67 | `panic!(` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-intrinsics/src/category_check.rs:23 | `panic!(` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:24 | `let _ = vyre_reference::reference_eval(program, &values).expect("execute");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:30 | `b.iter(|| black_box(dot("a", "b", "c", 4).unwrap()));` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:36 | `let program = dot("a", "b", "c", 4).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:104 | `let wire = p.to_wire().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:105 | `let decoded = Program::from_wire(&wire).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:114 | `.unwrap_or_else(|| panic!("Fix: missing benchmark OpEntry for {id}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cat_a_bench.rs:120 | `let cases = (entry.test_inputs.expect("Fix: benchmark fixture required"))();` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cooperative_dfa_vs_classic.rs:73 | `.expect("Fix: classic DFA benchmark must dispatch on the local GPU backend");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/cooperative_dfa_vs_classic.rs:90 | `.expect("Fix: cooperative DFA benchmark must dispatch on the local GPU backend");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/decode_scan.rs:139 | `.expect("Fix: CPU decode + GPU scan baseline must dispatch");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/decode_scan.rs:161 | `.expect("Fix: fused decode + scan benchmark must dispatch");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/inventory_driven.rs:10 | `vyre_reference::reference_eval(program, &values).expect("Fix: inventory-driven bench must execute");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/inventory_driven.rs:29 | `.expect("Fix: bench wire encode must succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/benches/inventory_driven.rs:31 | `.expect("Fix: bench wire decode must succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/builder.rs:124 | `let a_count = a.element_count().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/builder.rs:125 | `let out_count = out.element_count().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/builder.rs:135 | `let n = a.element_count().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/builder.rs:204 | `let n = a.element_count().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/decode/inflate.rs:188 | `.expect("stored block fixture must decode");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/graph/ast_walk_preorder.rs:76 | `let order = vyre_foundation::vast::walk_preorder_indices(&node_region, 4, 128).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/logical/and.rs:18 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/logical/nand.rs:18 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/logical/nor.rs:18 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/logical/or.rs:18 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/logical/xor.rs:18 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/matching/dfa/dfa_compile.rs:72 | `.expect("Fix: default DFA budget exhausted; call dfa_compile_with_budget and handle DfaCompileError::TooLarge")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/math/avg_floor.rs:23 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/dot.rs:65 | `build: || dot("lhs", "rhs", "out", 3).unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul.rs:110 | `let a_count = m.checked_mul(k).expect("matmul: m*k overflows u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul.rs:111 | `let b_count = k.checked_mul(n).expect("matmul: k*n overflows u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul.rs:112 | `let out_count = m.checked_mul(n).expect("matmul: m*n overflows u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul.rs:149 | `.unwrap_or_else(|err| panic!("Fix: matmul build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul_tiled.rs:137 | `.unwrap_or_else(|err| panic!("Fix: matmul_tiled build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul_tiled.rs:154 | `let a_count = m.checked_mul(k).expect("matmul_tiled: m*k overflows u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul_tiled.rs:155 | `let b_count = k.checked_mul(n).expect("matmul_tiled: k*n overflows u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/math/linalg/matmul_tiled.rs:156 | `let out_count = m.checked_mul(n).expect("matmul_tiled: m*n overflows u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/math/lzcnt_u32.rs:27 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/math/tzcnt_u32.rs:26 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/math/wrapping_neg.rs:17 | `.unwrap_or_else(|err| panic!("Fix: {OP_ID} build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/nn/attention/attention.rs:144 | `.unwrap_or_else(|err| panic!("Fix: attention build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/nn/attention/attention.rs:202 | `.expect("attention: s*d overflows u32  -  reduce the seq_len × head_dim product");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/nn/attention/softmax.rs:110 | `.expect("element_count checked above");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/nn/attention/softmax.rs:221 | `.unwrap_or_else(|err| panic!("Fix: softmax build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/nn/linear/linear.rs:73 | `.expect("linear: in_dim * out_dim overflows u32  -  see V7-CORR-007"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/nn/linear/linear.rs:86 | `build: || linear("x", "w", "b", "out", 4, 4).unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-libs/src/nn/norm/layer_norm.rs:100 | `.expect("element_count checked above");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/vyre-libs/src/nn/norm/layer_norm.rs:226 | `.unwrap_or_else(|err| panic!("Fix: layer_norm build failed: {err}"))` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-reference/benches/eval_throughput.rs:17 | `b.iter(|| reference_eval(&program, &inputs).unwrap())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/eval_node.rs:243 | `if u64::try_from(buffer.bytes.read().unwrap().len()).unwrap_or(u64::MAX) < required_end {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:38 | `let bytes_guard = self.bytes.read().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:52 | `.map(|rw| rw.into_inner().unwrap())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:53 | `.unwrap_or_else(|a| a.read().unwrap().clone());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:59 | `let bytes_guard = buffer.bytes.read().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:80 | `let mut bytes_guard = buffer.bytes.write().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:107 | `let bytes_guard = buffer.bytes.read().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/oob.rs:118 | `let mut bytes_guard = buffer.bytes.write().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/vyre/vyre-reference/src/primitives/common.rs:113 | `_ => panic!("combine: unsupported CombineOp variant {op:?}"),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/workgroup.rs:171 | `.map_or_else(Vec::new, |buffer| buffer.bytes.read().unwrap().clone())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/workgroup.rs:182 | `.map(|rw| rw.into_inner().unwrap())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/workgroup.rs:183 | `.unwrap_or_else(|a| a.read().unwrap().clone())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/vyre/vyre-reference/src/workgroup.rs:720 | `let slot = self.slots.slot(name).expect("Fix: slot exists after bind");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/xtask/src/check_cat_a.rs:23 | `std::env::current_dir().expect("xtask must run in a cwd")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/xtask/src/check_cat_a.rs:30 | `panic!("Fix: \`{label}\` could not launch: {err}");` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/xtask/src/compile.rs:147 | `write!(&mut fp_hex, "{b:02x}").expect("format to String never fails");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/xtask/src/dep_drift.rs:10 | `let workspace_root = std::env::current_dir().expect("xtask must run from the vyre workspace");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/vyre/xtask/src/dep_drift.rs:15 | `.expect("Fix: vyre workspace must remain nested under libs/performance/matching/vyre.");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/xtask/src/dep_drift.rs:19 | `panic!(` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/xtask/src/dep_drift.rs:25 | `panic!(` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/xtask/src/dep_drift.rs:41 | `panic!("Fix: failed to read {}: {error}", manifest.display());` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/xtask/src/dep_drift.rs:44 | `panic!("Fix: failed to parse {}: {error}", manifest.display());` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/vyre/xtask/src/paths/workspace_root.rs:9 | `.expect("Fix: vyre/xtask must live two levels below the workspace root.")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/vyre/xtask/src/quick/eval_xor.rs:4 | `_ => panic!("Fix: eval_xor expected exactly 2 args, got {}", args.len()),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/xtask/src/quick_cache/eval_and.rs:13 | `_ => panic!("Fix: eval_and expected exactly 2 args, got {}", args.len()),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/matching/vyre/xtask/src/quick_cache/eval_or.rs:13 | `_ => panic!("Fix: eval_or expected exactly 2 args, got {}", args.len()),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/benches/compact_dfa.rs:18 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/compact_dfa.rs:26 | `black_box(patterns.scan(&payload).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:29 | `builder.build().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:45 | `black_box(patterns.scan(data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:63 | `black_box(patterns.scan(data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:78 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:111 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:117 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:157 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:164 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:188 | `let pattern_set = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:204 | `let ptr = unsafe { table.as_ptr().add(idx) }.cast::<u8>();` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:207 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/warpstate/core/benches/cpu_scan.rs:212 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_vs_gpu.rs:17 | `builder.build().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_vs_gpu.rs:30 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/cpu_vs_gpu.rs:43 | `black_box(pollster::block_on(gpu.scan(&data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:175 | `.unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:189 | `let ac = AhoCorasick::new(&aho_patterns[..]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:197 | `black_box(AhoCorasick::new(patterns.as_slice()).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:211 | `black_box(scan_aho_corasick(ac, &data, &mut out_matches).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:302 | `black_box(PatternSet::builder().literal("password").build().unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:322 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:330 | `black_box(PatternSet::builder().regex("abc").build().unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:341 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:356 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:371 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:379 | `black_box(pollster::block_on(gpu.scan(b"test data")).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:390 | `black_box(pollster::block_on(gpu.scan(data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:401 | `let patterns = PatternSet::builder().literal("needle").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:410 | `black_box(patterns.scan(data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:420 | `let patterns = PatternSet::builder().literal("needle").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:430 | `black_box(pollster::block_on(gpu.scan(data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:453 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/criterion_benches.rs:458 | `black_box(set.scan(data.as_bytes()).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/dfa_huge_pages.rs:32 | `.expect("transition table byte length overflowed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/warpstate/core/benches/dfa_huge_pages.rs:33 | `let ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/matching/warpstate/core/benches/dfa_huge_pages.rs:48 | `let ptr = std::ptr::NonNull::new(ptr.cast::<u32>()).expect("mmap returned a null pointer");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/performance/matching/warpstate/core/benches/dfa_huge_pages.rs:49 | `let values = unsafe { std::slice::from_raw_parts_mut(ptr.as_ptr(), count) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/warpstate/core/benches/dfa_huge_pages.rs:62 | `Self::HugePages { ptr, len, .. } => unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/warpstate/core/benches/dfa_huge_pages.rs:73 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_batching.rs:19 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_batching.rs:47 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu.rs:43 | `builder.build().expect("valid pattern set")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu.rs:75 | `b.iter(|| black_box(ps.scan(data).unwrap()));` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:29 | `builder.build().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:45 | `black_box(patterns.scan(data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:63 | `black_box(patterns.scan(data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:78 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:111 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:117 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:157 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:164 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:188 | `let pattern_set = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:204 | `let ptr = unsafe { table.as_ptr().add(idx) }.cast::<u8>();` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:207 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/matching/warpstate/core/benches/gpu_vs_cpu_new.rs:212 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/matching/warpstate/core/benches/regression.rs:25 | `builder.build().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/regression.rs:34 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/regression.rs:48 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/regression.rs:63 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/regression.rs:82 | `black_box(builder.build().unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/regression.rs:98 | `black_box(warpstate::dfa::RegexDFA::build(&patterns, &ids).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/threshold.rs:29 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/threshold.rs:36 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/threshold.rs:38 | `pollster::block_on(AutoMatcher::with_options(&patterns, 0, 1024 * 1024 * 1024)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/threshold.rs:56 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/threshold.rs:66 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:38 | `builder.build().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:46 | `.unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:65 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:73 | `black_box(pollster::block_on(gpu.scan(&data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:93 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:101 | `black_box(pollster::block_on(gpu.scan(&data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:121 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:129 | `black_box(pollster::block_on(gpu.scan(&data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:149 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:157 | `black_box(pollster::block_on(gpu.scan(&data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:180 | `black_box(patterns.scan(&data).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:201 | `black_box(pollster::block_on(gpu.scan(&data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:226 | `black_box(pollster::block_on(matcher.scan(&small_data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/benches/throughput.rs:238 | `black_box(pollster::block_on(matcher.scan(&large_data)).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/basic.rs:8 | `.expect("patterns should compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/examples/basic.rs:12 | `.expect("scan should succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/examples/dump_dfa.rs:8 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dump_dfa.rs:11 | `let matches = patterns.scan(data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dump_dfa.rs:31 | `let overlapping = patterns.scan_overlapping(data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dup_test.rs:9 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dup_test.rs:12 | `let scanner = HashScanner::build(patterns.ir()).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dup_test.rs:14 | `let ac_matches = patterns.scan(data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dup_test.rs:16 | `let mut f = std::fs::File::create("/tmp/dup_test_output.txt").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dup_test.rs:17 | `writeln!(f, "hash_matches: {:?}", hash_matches).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/dup_test.rs:18 | `writeln!(f, "ac_matches: {:?}", ac_matches).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/quick_bench.rs:32 | `let patterns = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/quick_bench.rs:46 | `let _ = patterns.scan(&data[..1000]).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/quick_bench.rs:56 | `let _ = patterns.scan(slice).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/quick_bench.rs:79 | `let ps = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/examples/quick_bench.rs:84 | `let _ = ps.scan(data_1mb).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/invalid_utf8_in_name_detected.rs:22 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/invalid_utf8_in_name_detected.rs:24 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/invalid_utf8_in_name_detected.rs:25 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/large_pattern_set_100k_does_not_oom.rs:27 | `let pattern_set = builder.build().expect("Should build 100K patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/large_pattern_set_100k_does_not_oom.rs:32 | `CompiledPatternIndex::build(&pattern_set).expect("Should build index for 100K patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/large_pattern_set_100k_does_not_oom.rs:36 | `let index = CompiledPatternIndex::load(&bytes).expect("Should load index for 100K patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/large_pattern_set_100k_does_not_oom.rs:43 | `.expect("Should scan with 100K patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_future_version_errors.rs:20 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_future_version_errors.rs:21 | `let mut corrupted = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_future_version_errors.rs:38 | `Err(other) => panic!("Wrong error type: {:?}", other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_literal_count_mismatch_detected.rs:25 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_literal_count_mismatch_detected.rs:27 | `let mut corrupted = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_literal_count_mismatch_detected.rs:46 | `Err(other) => panic!("Wrong error type: {:?}", other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_literal_count_zero_with_offsets.rs:15 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_literal_count_zero_with_offsets.rs:16 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_max_version_errors.rs:15 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_max_version_errors.rs:16 | `let mut corrupted = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_overflow_detected.rs:15 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_overflow_detected.rs:16 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_overflow_detected.rs:17 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_past_packed_bytes_detected.rs:20 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_past_packed_bytes_detected.rs:21 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_past_packed_bytes_detected.rs:24 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_offset_past_packed_bytes_detected.rs:65 | `Err(other) => panic!("Wrong error type: {:?}", other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_truncated_at_various_boundaries.rs:19 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_truncated_at_various_boundaries.rs:20 | `let valid_bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_truncated_header_fields.rs:16 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_truncated_header_fields.rs:17 | `let valid_bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_truncated_header_fields.rs:44 | `Err(other) => panic!("Wrong error type at {}: {:?}", truncate_at, other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_version_zero_errors.rs:16 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_version_zero_errors.rs:17 | `let mut corrupted = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_version_zero_errors.rs:33 | `Err(other) => panic!("Wrong error type: {:?}", other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_wrong_magic_bytes_errors.rs:17 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_wrong_magic_bytes_errors.rs:18 | `let valid_bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/load_wrong_magic_bytes_errors.rs:58 | `Ok(_) => panic!("Test case '{}' should have errored", name),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/medium_pattern_set_10k_roundtrip.rs:21 | `let pattern_set = builder.build().expect("Should build 10K patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/medium_pattern_set_10k_roundtrip.rs:22 | `let bytes = CompiledPatternIndex::build(&pattern_set).expect("Should build index");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/medium_pattern_set_10k_roundtrip.rs:23 | `let index = CompiledPatternIndex::load(&bytes).expect("Should load index");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/medium_pattern_set_10k_roundtrip.rs:27 | `let original_matches = pattern_set.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/medium_pattern_set_10k_roundtrip.rs:28 | `let index_matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/mixed_literals_and_regex_roundtrip.rs:22 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/mixed_literals_and_regex_roundtrip.rs:24 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/mixed_literals_and_regex_roundtrip.rs:25 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/mixed_literals_and_regex_roundtrip.rs:28 | `let original = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/mixed_literals_and_regex_roundtrip.rs:29 | `let loaded = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/multiple_patterns_with_null_bytes.rs:28 | `let pattern_set = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/multiple_patterns_with_null_bytes.rs:29 | `let bytes = CompiledPatternIndex::build(&pattern_set).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/multiple_patterns_with_null_bytes.rs:30 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/multiple_patterns_with_null_bytes.rs:34 | `let original_matches = pattern_set.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/multiple_patterns_with_null_bytes.rs:35 | `let index_matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/names_count_mismatch_detected.rs:19 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/names_count_mismatch_detected.rs:21 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/names_count_mismatch_detected.rs:29 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/negative_length_prefixed_fields.rs:18 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/negative_length_prefixed_fields.rs:19 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:25 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:27 | `let bytes = CompiledPatternIndex::build(&pattern_set).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:28 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:32 | `let original_matches = pattern_set.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:33 | `let index_matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:42 | `let rebuilt = index.to_pattern_set().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/null_bytes_pattern_roundtrip.rs:43 | `let rebuilt_matches = rebuilt.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/overlapping_patterns_preserved.rs:20 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/overlapping_patterns_preserved.rs:22 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/overlapping_patterns_preserved.rs:23 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/overlapping_patterns_preserved.rs:27 | `let original = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/overlapping_patterns_preserved.rs:28 | `let loaded = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/oversized_packed_bytes_length_detected.rs:15 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/oversized_packed_bytes_length_detected.rs:16 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_count_mismatch_detected.rs:24 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_count_mismatch_detected.rs:26 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_ending_with_null.rs:18 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_ending_with_null.rs:20 | `let bytes = CompiledPatternIndex::build(&pattern_set).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_ending_with_null.rs:21 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_ending_with_null.rs:24 | `let matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_starting_with_null.rs:18 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_starting_with_null.rs:20 | `let bytes = CompiledPatternIndex::build(&pattern_set).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_starting_with_null.rs:21 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/pattern_starting_with_null.rs:24 | `let matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:49 | `let original_matches = pattern_set.scan(input_bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:50 | `let index_matches = index.scan(input_bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:103 | `let original_matches = pattern_set.scan(input_bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:104 | `let index_matches = index.scan(input_bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:128 | `let ps = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:129 | `let bytes = CompiledPatternIndex::build(&ps).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:130 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:133 | `let orig = ps.scan(input_bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/proptest_roundtrip_preserves_scan_results.rs:134 | `let idx = index.scan(input_bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_case_insensitive_results_identical.rs:20 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_case_insensitive_results_identical.rs:22 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_case_insensitive_results_identical.rs:23 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_case_insensitive_results_identical.rs:27 | `let original_matches = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_case_insensitive_results_identical.rs:28 | `let index_matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_literal_and_regex_results_identical.rs:25 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_literal_and_regex_results_identical.rs:27 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_literal_and_regex_results_identical.rs:28 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_literal_and_regex_results_identical.rs:32 | `let original_matches = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/round_trip_literal_and_regex_results_identical.rs:33 | `let index_matches = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/single_literal_optimization_preserved.rs:16 | `let patterns = PatternSet::builder().literal("needle").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/single_literal_optimization_preserved.rs:18 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/single_literal_optimization_preserved.rs:19 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/single_literal_optimization_preserved.rs:22 | `let original = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/single_literal_optimization_preserved.rs:23 | `let loaded = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/to_pattern_set_rebuilds_correctly.rs:20 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/to_pattern_set_rebuilds_correctly.rs:22 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/to_pattern_set_rebuilds_correctly.rs:23 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/to_pattern_set_rebuilds_correctly.rs:26 | `let rebuilt = index.to_pattern_set().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/to_pattern_set_rebuilds_correctly.rs:30 | `let original = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/to_pattern_set_rebuilds_correctly.rs:31 | `let from_rebuilt = rebuilt.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/trailing_bytes_rejected.rs:16 | `let patterns = PatternSet::builder().literal("test").build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/trailing_bytes_rejected.rs:17 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/trailing_bytes_rejected.rs:36 | `Err(other) => panic!("Wrong error type: {:?}", other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/unicode_literals_preserved.rs:19 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/unicode_literals_preserved.rs:21 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/unicode_literals_preserved.rs:22 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/unicode_literals_preserved.rs:25 | `let original = patterns.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/unicode_literals_preserved.rs:26 | `let loaded = index.scan(test_data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/very_long_literal_pattern.rs:21 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/very_long_literal_pattern.rs:23 | `let bytes = CompiledPatternIndex::build(&patterns).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/very_long_literal_pattern.rs:24 | `let index = CompiledPatternIndex::load(&bytes).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/matching/warpstate/core/src/compiled_index/adversarial_tests/very_long_literal_pattern.rs:27 | `let matches = index.scan(test_data.as_bytes()).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/matching/yaragpu/src/yara/yara_census.rs:23 | `panic!("BUG: invalid static regex pattern {}: {err}", $pat);` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/performance/memory/kernelkit/src/affinity.rs:28 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/corpus.rs:107 | `unsafe { memmap2::MmapOptions::new().populate().map(&file) }.map_err(|source| {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/corpus.rs:201 | `let result = unsafe { libc::madvise(ptr, mmap.len(), libc::MADV_SEQUENTIAL) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/corpus.rs:209 | `let result = unsafe { libc::madvise(ptr, mmap.len(), libc::MADV_HUGEPAGE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/file_identity.rs:63 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:134 | `let _ = unsafe { libc::madvise(ptr, byte_len, libc::MADV_HUGEPAGE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:178 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:185 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:207 | `let raw_ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:223 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:241 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:257 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:275 | `unsafe { slice::from_raw_parts(self.ptr.as_ptr(), self.count) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:279 | `unsafe { slice::from_raw_parts_mut(self.ptr.as_ptr(), self.count) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/hugepages.rs:286 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/lib.rs:249 | `let ps = unsafe { libc::sysconf(libc::_SC_PAGESIZE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mlock.rs:30 | `let result = unsafe { libc::mlock(ptr.cast::<libc::c_void>(), len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mlock.rs:46 | `let result = unsafe { VirtualLock(ptr.cast_mut().cast::<libc::c_void>(), len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mlock.rs:79 | `let result = unsafe { libc::munlock(ptr.cast::<libc::c_void>(), len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mlock.rs:95 | `let result = unsafe { VirtualUnlock(ptr.cast_mut().cast::<libc::c_void>(), len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/advise.rs:90 | `let mmap = unsafe { memmap2::MmapOptions::new().populate().map(&file) }.map_err(|source| {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/advise.rs:124 | `let mmap = unsafe { memmap2::MmapOptions::new().populate().map(&file) }.map_err(|source| {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/advise.rs:152 | `let result = unsafe { libc::madvise(ptr, len, libc::MADV_DONTNEED) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/advise.rs:193 | `let result = unsafe { libc::madvise(ptr, len, primary_advice) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/advise.rs:202 | `let result = unsafe { libc::madvise(ptr, len, libc::MADV_HUGEPAGE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:132 | `let raw_ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:168 | `let raw_ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:187 | `let result = unsafe { libc::munmap(ptr.as_ptr().cast::<libc::c_void>(), len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:199 | `let result = unsafe { virtual_free(ptr.as_ptr().cast::<libc::c_void>()) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:219 | `unsafe { VirtualFree(ptr, 0, MEM_RELEASE) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:225 | `unsafe { libc::madvise(ptr.as_ptr().cast::<libc::c_void>(), len, MADVISE_HUGEPAGE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/mmap/region.rs:276 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:21 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:217 | `let library = unsafe { libloading::Library::new(library_name) }.map_err(|source| {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:224 | `let available = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:233 | `let max_node = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:242 | `let run_on_node = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:251 | `let tonode_memory = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:263 | `let state = unsafe { available() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:278 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:293 | `let result = unsafe { (self.run_on_node)(node) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/numa.rs:309 | `let result = unsafe { (self.tonode_memory)(ptr, len, node) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/perf.rs:66 | `let mut attr: PerfEventAttr = unsafe { std::mem::zeroed() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/perf.rs:72 | `let fd = i32::try_from(unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/perf.rs:118 | `let n = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/perf.rs:154 | `let result = unsafe { libc::ioctl(self.fd, PERF_EVENT_IOC_RESET, 0) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/perf.rs:187 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/prefetch.rs:50 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/prefetch.rs:55 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/prefetch.rs:91 | `let line_ptr = unsafe { ptr.add(offset) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/prefetch.rs:104 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/prefetch.rs:113 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/prefetch.rs:122 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/readahead.rs:32 | `let result = unsafe { libc::readahead(file.as_raw_fd(), off, count) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/readahead.rs:76 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/kernelkit/src/readahead.rs:105 | `unsafe { libc::posix_fadvise(file.as_raw_fd(), 0, 0, libc::POSIX_FADV_SEQUENTIAL) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/memory/mempool/benches/arena_bench.rs:9 | `let arena = Arena::with_capacity(size * 10).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/memory/mempool/benches/arena_bench.rs:10 | `let layout = Layout::from_size_align(size, 8).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/memory/mempool/benches/arena_bench.rs:21 | `let mut arena = Arena::with_capacity(1024 * 1024).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/memory/mempool/benches/arena_bench.rs:33 | `let arena = Arena::with_capacity(size * 256).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/memory/mempool/benches/arena_bench.rs:36 | `let _ = arena.alloc_slice_copy(black_box(&payload)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:95 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:150 | `let used = unsafe { *self.offset.get() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:152 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:157 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:194 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:240 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:256 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:271 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:279 | `unsafe { *self.offset.get() }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/arena.rs:301 | `let current = unsafe { self.block.as_mut_ptr().add(self.used()) } as usize;` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:14 | `let madvise_res = unsafe { madvise(ptr, len, MADV_HUGEPAGE) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:26 | `let mlock_res = unsafe { mlock(ptr, len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:41 | `let mlock_res = unsafe { mlock(ptr, len) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:88 | `ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:102 | `ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:121 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:159 | `let ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:177 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:191 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:210 | `let result = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:225 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:279 | `let ptr = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:297 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/arena/mmap.rs:334 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/buffer.rs:87 | `unsafe { slice::from_raw_parts(self.handle.ptr.as_ptr().cast_const(), self.len) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/buffer.rs:159 | `let handle = unsafe { std::ptr::read(&raw const this.handle) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/buffer.rs:252 | `unsafe { slice::from_raw_parts(self.handle.ptr.as_ptr().cast_const(), self.len) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/buffer.rs:261 | `unsafe { slice::from_raw_parts_mut(self.handle.ptr.as_ptr(), self.len) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/size_class.rs:183 | `let ptr = unsafe { alloc_zeroed(layout) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/size_class.rs:204 | `unsafe { NonNull::new_unchecked(ptr) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/size_class.rs:219 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/memory/mempool/src/buffer/size_class.rs:232 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/performance/pipeline/blockscan/benches/blockscan_bench.rs:33 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/blockscan/benches/blockscan_bench.rs:34 | `let file_index = FileBloomIndex::try_new(index).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/blockscan/benches/blockscan_bench.rs:37 | `.literal(String::from_utf8(pattern.to_vec()).unwrap())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/blockscan/benches/blockscan_bench.rs:39 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/blockscan/benches/blockscan_bench.rs:47 | `let matches = patterns.scan(black_box(&data)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/blockscan/benches/blockscan_bench.rs:60 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:69 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:82 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:95 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:120 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:133 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:146 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:172 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:186 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:200 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:227 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:251 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:263 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:277 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:289 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:313 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:326 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:339 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:352 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:377 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:389 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:401 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:424 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:438 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/benches/pipeline.rs:452 | `for batch in pipeline.start().unwrap() {` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/examples/benchmark.rs:31 | `.unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/examples/benchmark.rs:42 | `.unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/examples/benchmark.rs:53 | `let x = s.get("x").unwrap().try_as_f32().unwrap().to_vec();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/performance/pipeline/tenshift/core/examples/benchmark.rs:61 | `.unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/performance/testing/faultkit/src/config.rs:47 | `unsafe { &*self.lock.data.get() }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/performance/testing/faultkit/src/config.rs:53 | `unsafe { &mut *self.lock.data.get() }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/runtime/codewalk/examples/codewalk_basic.rs:11 | `let entries = walker.walk().expect("failed to walk directory");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/codewalk/examples/codewalk_config.rs:17 | `.expect("invalid TOML config");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/codewalk/examples/codewalk_config.rs:26 | `let entries = walker.walk().expect("failed to walk directory");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/codewalk/examples/ignore_patterns.rs:15 | `let entries = walker.walk().expect("failed to walk directory");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/openpack/src/archive/crx.rs:137 | `let payload = std::fs::read(&archive.path).expect("read payload");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/openpack/src/archive/crx.rs:141 | `let pack = OpenPack::open_default(&crx.path).expect("open crx3");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/openpack/src/archive/crx.rs:143 | `assert_eq!(pack.read_entry("x").expect("entry"), b"hello");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/openpack/src/archive/crx.rs:151 | `let payload = std::fs::read(&archive.path).expect("read payload");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/runtime/openpack/src/archive/crx.rs:155 | `let pack = OpenPack::open_default(&crx_path).expect("open crx");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/runtime/openpack/src/format.rs:70 | `let mmap = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/runtime/proxywire/examples/basic.rs:11 | `let route = provider.route_for(&ctx).expect("should create route");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/runtime/secjit/src/compiler/mod.rs:224 | `Ok(unsafe { mem::transmute::<*const u8, JitMatchFnPtr>(code_ptr) })` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/runtime/secjit/src/runtime.rs:104 | `let func = unsafe { compiler.finalize()? };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/runtime/secjit/src/runtime.rs:168 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/scanner/respredict/examples/basic.rs:21 | `let prediction = predictor.predict(&request).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/respredict/examples/decision.rs:17 | `let prediction = predictor.predict(&request).expect("prediction");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/scanclient/examples/concurrent_requests.rs:9 | `let config = HttpConfig::from_toml("timeout_secs = 5").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanclient/examples/custom_timeout.rs:16 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanclient/examples/scanclient_basic.rs:18 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- CRITICAL | libs/scanner/scanclient/src/client.rs:53 | `unimplemented!(` | Suggested fix: Implement the missing path fully or delete the dead branch; do not ship runtime stubs.
- HIGH | libs/scanner/scanclient/src/http_transport/mod.rs:152 | `let wait = self.interval.checked_sub(elapsed).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/basic.rs:8 | `let dir = tempfile::tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/basic.rs:30 | `checkpoint.save(&path).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/basic.rs:37 | `let resumed = ScanCheckpoint::load(&path).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/basic.rs:52 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/basic.rs:54 | `let entries = journal.replay().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/journal_recovery.rs:8 | `let dir = tempfile::tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/journal_recovery.rs:20 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/journal_recovery.rs:25 | `let entries = journal.replay().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/progress_bar.rs:8 | `let dir = tempfile::tempdir().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/scanstate/examples/progress_bar.rs:19 | `checkpoint.save(&path).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secfinding/examples/basic.rs:8 | `.evidence(secfinding::Evidence::http_status(500).unwrap())` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secfinding/examples/basic.rs:10 | `.unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secfinding/examples/basic.rs:14 | `let json = serde_json::to_string_pretty(&finding).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secfinding/examples/serialize_json.rs:13 | `.expect("Failed to build finding");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secfinding/examples/serialize_json.rs:15 | `let json = serde_json::to_string_pretty(&finding).expect("Failed to serialize");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secir/examples/secir_basic.rs:16 | `.expect("template should build");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secir/src/compose.rs:1233 | `.expect("Step result should exist");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/scanner/secir/src/compose.rs:1289 | `_ => panic!("Expected HTTP action"),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- CRITICAL | libs/scanner/secir/src/plugin/loader.rs:407 | `unsafe { Library::new(path) }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:39 | `let flow = template.parse_flow().unwrap().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:113 | `let flow = FlowExpression::parse("http(1) && http(2)").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:214 | `let resolved = child.resolve_inheritance(&templates).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:293 | `let flow = FlowExpression::parse("http(1) || http(2)").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:354 | `let flow = template.parse_flow().unwrap().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:429 | `let flow = FlowExpression::parse("!http(1) && http(2)").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secir/src/template/tests_flow.rs:458 | `let flow = FlowExpression::parse("http(1) && http(3)").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:205 | `black_box(CompiledDatabase::compile(&templates_list.clone()).unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:211 | `let db = CompiledDatabase::compile(&templates_list).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:221 | `let _ = black_box(db.scan(black_box(&response)).expect("scan"));` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:230 | `let compiled = CompiledDatabase::compile(&data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:237 | `.expect("scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:271 | `let db = CompiledDatabase::compile(&dataset).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:292 | `black_box(db.scan(black_box(&response)).expect("scan"));` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:302 | `let compiled = CompiledDatabase::compile(&templates).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:314 | `.expect("scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:328 | `let word_db = CompiledDatabase::compile(&word_templates).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:329 | `let regex_db = CompiledDatabase::compile(&regex_templates).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:336 | `.expect("scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/benches/scan_bench.rs:345 | `.expect("scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/src/database/compile.rs:41 | `let index = u32::try_from(batch.patterns.len()).expect("pattern arena exceeds u32::MAX");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/src/database/compile.rs:299 | `u32::try_from(patterns.len()).expect("pattern arena exceeds u32::MAX");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/scanner/secmatch/src/evaluator.rs_test.rs:3 | `let result = decode("test").unwrap().into_owned().into_bytes();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/basic.rs:17 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/basic.rs:25 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/basic.rs:30 | `println!("{}", render(&findings, Format::Text, "my-scanner").unwrap());` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/basic.rs:36 | `render(&findings, Format::Sarif, "my-scanner").unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/basic.rs:73 | `render_any(&custom, Format::Sarif, "custom-tool").unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/custom_reportable.rs:56 | `render_any(&findings, Format::Text, "api-audit").unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/custom_reportable.rs:60 | `render_any(&findings, Format::Sarif, "api-audit").unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/jsonl_export.rs:13 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/jsonl_export.rs:17 | `.unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/examples/jsonl_export.rs:23 | `render(&findings, Format::Jsonl, "my-scanner").unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/secreport/src/render/markdown.rs:141 | `uniq.into_iter().next().unwrap()` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/scanner/vulnir/examples/basic_usage.rs:185 | `graph.merge(&graph_b).expect("Merge should succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/shared/surgec-grammar-gen/src/dfa.rs:138 | `next_state: u16::try_from(next_state).expect("next_state fits in u16"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/shared/surgec-grammar-gen/src/dfa.rs:170 | `.expect("failed to compile regex patterns to DFA");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/shared/surgec-grammar-gen/src/dfa.rs:175 | `.expect("must have start state");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/shared/surgec-grammar-gen/src/lex_c11_max_munch.rs:31 | `panic!("C11 pattern compile failed {kind} \`{pat}\`: {e}");` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/shared/surgec-grammar-gen/src/lr.rs:136 | `let id = u32::try_from(self.table.productions.len()).expect("production count fits u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/shared/surgec-grammar-gen/src/wire.rs:94 | `u32::try_from(dfa.token_ids.len()).expect("token_ids count fits u32"),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/shared/surgec-grammar-gen/src/wire.rs:219 | `return Err(WireError::BadMagic(bytes[0..4].try_into().expect("4 bytes")));` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/shared/surgec-grammar-gen/src/wire.rs:254 | `let payload_len = u32::try_from(payload.len()).expect("payload fits u32");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/surge/src/parser/mod.rs:23 | `parser.expect(&Token::Eof)?;` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:61 | `let tmpdir = TempDir::new().expect("temp dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:63 | `std::fs::write(&path, &data).expect("write fixture");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:93 | `builder.build().expect("pattern set build")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:116 | `.expect("fixture path has parent")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:123 | `.expect("build index");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:128 | `let index = SearchIndexSet::build_for_roots(&roots, &paths).expect("build index");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:177 | `.expect("simd scan")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/pipeline_stages.rs:195 | `let matches = pattern_set.scan(black_box(&fixture.bytes)).expect("scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:81 | `builder.build().expect("pattern set build")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:101 | `std::fs::write(&self.path, &self.bytes).expect("fixture write must succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:135 | `let tmpdir = TempDir::new().expect("temp dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:185 | `let matcher = RegexMatcher::new(pattern).expect("regex compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:193 | `.expect("rg search");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:200 | `.expect("warpgrep scan")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:236 | `let _ = std::fs::read(path).expect("fixture read");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:271 | `let rt = tokio::runtime::Runtime::new().expect("tokio runtime");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:278 | `let tier = rt.block_on(tier::probe(&pattern_set)).expect("tier probe");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:286 | `reader::read_batch(ring, std::slice::from_ref(&path), u64::MAX).expect("read batch")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:291 | `std::fs::read(&path).expect("file read").len()` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/bench/vs_ripgrep.rs:310 | `let matches = pattern_set.scan(black_box(&fixture.bytes)).expect("scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpgrep/benches/engine_throughput.rs:48 | `let matcher = RegexMatcher::new(pattern).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/engine_throughput.rs:59 | `let patterns = PatternSet::builder().literal(pattern).build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/engine_throughput.rs:61 | `let matches = patterns.scan(black_box(&corpus)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/engine_throughput.rs:85 | `let matcher = RegexMatcher::new(&rg_pattern).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/engine_throughput.rs:100 | `let pattern_set = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/engine_throughput.rs:103 | `let matches = pattern_set.scan(black_box(&corpus)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:12 | `let tmp = TempDir::new().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:14 | `fs::write(&file_path, b"abcde").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:22 | `let args = vec!["warpgrep", "abcde", file_path.to_str().unwrap()];` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:23 | `let exit_code = warpgrep::run_cli_blocking(black_box(args)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:31 | `let tmp = TempDir::new().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:43 | `fs::write(&file_path, &data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:52 | `let args = vec!["warpgrep", pattern, file_path.to_str().unwrap()];` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:53 | `let exit_code = warpgrep::run_cli_blocking(black_box(args)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:61 | `let tmp = TempDir::new().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:70 | `fs::write(file_path, data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:79 | `let args = vec!["warpgrep", "target", root.to_str().unwrap()];` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:80 | `let exit_code = warpgrep::run_cli_blocking(black_box(args)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:97 | `let ps = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:110 | `let bloom = NgramBloom::from_block(&data, 2048).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:124 | `let tmp = TempDir::new().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:135 | `fs::write(&file_path, &data).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/benches/regression.rs:144 | `trigram::build_trigram_index(black_box(&root), black_box(&files)).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpgrep/src/daemon/server.rs:56 | `pattern_cache: LruCache::new(std::num::NonZeroUsize::new(64).expect("non-zero")),` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/tools/warpgrep/src/ebpf_filter.rs:162 | `unsafe { libc::geteuid() == 0 }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/index/io.rs:34 | `unsafe { Mmap::map(&file) }.map_err(|error| {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/main.rs:15 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/output/format.rs:208 | `let rc = unsafe { libc::fstat(fd, stat.as_mut_ptr()) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/output/format.rs:212 | `let stat = unsafe { stat.assume_init() };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/output/format.rs:224 | `let written = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:43 | `let fd = unsafe { libc::open(c_path.as_ptr(), flags) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:47 | `let fd = unsafe { libc::open(c_path.as_ptr(), libc::O_RDONLY) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:52 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:58 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:73 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:160 | `let fd = unsafe { libc::open(c_path.as_ptr(), libc::O_RDONLY) };` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/prefetch.rs:164 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/reader/mmap.rs:27 | `let map = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/reader/mmap.rs:39 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/reader/mod.rs:411 | `let device_pointer = unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/tools/warpgrep/src/search/gpu_batch.rs:40 | `let flush_at = deadline.expect("gpu batch deadline must exist");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/tools/warpgrep/src/search/mod.rs:230 | `let Ok(mmap) = (unsafe { memmap2::Mmap::map(&file) }) else {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/mod.rs:260 | `let Ok(mmap) = (unsafe { memmap2::Mmap::map(&file) }) else {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/mod.rs:639 | `let mmap = unsafe { memmap2::Mmap::map(&file) }.map_err(|e| Error::ReadFile {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/mod.rs:647 | `unsafe { &*ptr }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/mod.rs:731 | `let Ok(mmap) = (unsafe { memmap2::Mmap::map(&file) }) else {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/mod.rs:764 | `let Ok(mmap) = (unsafe { memmap2::Mmap::map(&file) }) else {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/single_file.rs:39 | `let mmap = unsafe { memmap2::Mmap::map(&file) }.map_err(|error| Error::ReadFile {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/walk_scan.rs:86 | `let Ok(mmap) = (unsafe { memmap2::Mmap::map(&file) }) else {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/walk_scan.rs:131 | `let mmap = unsafe { memmap2::Mmap::map(&file) }.ok()?;` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/walk_scan.rs:306 | `let mmap = unsafe { memmap2::Mmap::map(&file) }.ok()?;` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/walk_scan.rs:318 | `unsafe { &*ptr }` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/tools/warpgrep/src/search/walk_scan.rs:355 | `let finder = prebuilt_finder.as_ref().expect("finder built");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/tools/warpgrep/src/search/walk_scan.rs:542 | `let mmap = unsafe { memmap2::Mmap::map(&file) }.ok()?;` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpgrep/src/search/walk_scan.rs:643 | `let mmap = unsafe { memmap2::Mmap::map(&file) }.ok()?;` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- CRITICAL | libs/tools/warpscan-coord/src/index_builder.rs:845 | `unsafe {` | Suggested fix: Remove the unsafe block or isolate it behind a safe abstraction with explicit invariants.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:337 | `.expect("local mode requires --local-dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1284 | `let doc: NpmPackageDoc = serde_json::from_str(json).expect("should deserialize");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1286 | `assert_eq!(doc.name.as_ref().unwrap(), "test-package");` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1288 | `doc.dist_tags.as_ref().unwrap().latest.as_ref().unwrap(),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1292 | `let versions = doc.versions.as_ref().expect("versions should exist");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1297 | `let v2 = versions.get("2.0.0").unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1298 | `let tarball = v2.dist.as_ref().unwrap().tarball.as_ref().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1344 | `ScanwireServer::bind_ephemeral_localhost("localhost").expect("server");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1360 | `requests.lock().expect("lock").push(request);` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1364 | `files.lock().expect("lock").push(index);` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1370 | `completions.lock().expect("lock").push(complete);` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1385 | `let tempdir = tempdir().expect("tempdir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1388 | `std::fs::write(&certificate_path, certificate.as_ref()).expect("write cert");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1402 | `.expect("run ingest");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1404 | `let saved: Option<String> = checkpointkit::load(&checkpoint).expect("load checkpoint");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1407 | `let requests = requests.lock().expect("lock");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1411 | `let files = files.lock().expect("lock");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1416 | `let completions = completions.lock().expect("lock");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1434 | `.expect("append");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1436 | `let encoder = builder.into_inner().expect("tar builder");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1437 | `encoder.finish().expect("gzip")` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1447 | `let listener = TcpListener::bind("127.0.0.1:0").await.expect("bind");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1448 | `let address = listener.local_addr().expect("addr");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1455 | `let (mut socket, _) = accept.expect("accept");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1460 | `let count = socket.read(&mut buffer).await.expect("read");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1506 | `socket.write_all(response.as_bytes()).await.expect("write head");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1507 | `socket.write_all(&body).await.expect("write body");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1543 | `let config = Config::try_from(cli).expect("valid config");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- CRITICAL | libs/tools/warpscan-ingest/src/ingest.rs:1572 | `other => panic!("expected InvalidThreads(0), got {:?}", other),` | Suggested fix: Return a typed error with context instead of aborting the process from non-test code.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1578 | `let root = tempdir().expect("tempdir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1579 | `std::fs::write(root.path().join("a.txt"), b"alpha").expect("write");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1580 | `std::fs::write(root.path().join("b.txt"), b"bravo").expect("write");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1582 | `let first = discover_local_packages(root.path(), 0, 1).expect("discover first page");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/ingest.rs:1586 | `let second = discover_local_packages(root.path(), 1, 1).expect("discover second page");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan-ingest/src/main.rs:12 | `.add_directive("warpscan_ingest=info".parse().unwrap()),` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:26 | `let dir = TempDir::new().expect("create temp dir for 28 patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:30 | `.expect("read npm_malware_techniques.toml");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:32 | `.expect("read behavioral_supply_chain.toml");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:34 | `fs::write(dir.path().join("npm_malware.toml"), npm_content).expect("write npm rules");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:36 | `.expect("write behavioral rules");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:43 | `let dir = TempDir::new().expect("create temp dir for 17k patterns");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:46 | `fs::read_to_string("rules/abuse_ch_urlhaus.toml").expect("read abuse_ch_urlhaus.toml");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:47 | `fs::write(dir.path().join("urlhaus.toml"), urlhaus_content).expect("write urlhaus rules");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:142 | `fs::write(dir.join(name), content).expect("write test file");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:148 | `let target = TempDir::new().expect("create target dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:160 | `let result = scan_blocking(black_box(&config)).expect("scan should succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:170 | `let target = TempDir::new().expect("create target dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:182 | `let result = scan_blocking(black_box(&config)).expect("scan should succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:192 | `let target = TempDir::new().expect("create target dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:206 | `let result = scan_blocking(black_box(&config)).expect("scan should succeed");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:222 | `.expect("rules should compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:240 | `.expect("rules should compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:251 | `let loaded = load_rules(Some(rules_dir.path()), None).expect("load rules");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:294 | `.expect("create bloom from content");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:354 | `let target = TempDir::new().expect("create target dir");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:363 | `.expect("write benchmark file");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/end_to_end_scan.rs:374 | `let result = scan_blocking(black_box(&config)).expect("benchmark scan");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/scan_throughput.rs:46 | `let pattern_set = builder.build().expect("pattern set should compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/scan_throughput.rs:72 | `let pattern_set = builder.build().expect("pattern set should compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/benches/scan_throughput.rs:148 | `let pattern_set = builder.build().expect("pattern set should compile");` | Suggested fix: Replace expect() with explicit error propagation and boundary-specific context.
- HIGH | libs/tools/warpscan/examples/scan_throughput.rs:23 | `let ps = builder.build().unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.
- HIGH | libs/tools/warpscan/examples/scan_throughput.rs:33 | `let matches = ps.scan(&content).unwrap();` | Suggested fix: Replace unwrap() with explicit handling so malformed input and system failures cannot crash the process.

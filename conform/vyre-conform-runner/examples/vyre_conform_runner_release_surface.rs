//! Release surface example for the vyre conformance runner package.

fn main() {
    println!("vyre-conform-runner binary surface: vyre_conform::dispatch");
    println!(
        "Run conformance with: cargo run --bin vyre-conform -- dispatch --backend cuda --ops all"
    );
}

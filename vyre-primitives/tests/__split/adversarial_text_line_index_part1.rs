use super::*;

#[test]
fn test_text_line_index_adv_0() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_1() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_2() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_3() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_4() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_5() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_6() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_7() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_8() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_9() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_10() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_11() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_12() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_13() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_14() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_15() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_16() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_17() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_18() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_19() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_20() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_21() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_22() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_23() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_24() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_25() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_26() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_27() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}


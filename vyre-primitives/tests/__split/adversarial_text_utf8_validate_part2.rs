use super::*;

#[test]
fn test_text_utf8_validate_adv_28() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_29() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_30() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_31() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_32() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_33() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_34() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_35() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 32 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_36() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_37() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_38() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_39() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_40() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_41() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_42() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_43() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_44() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1024 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_45() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_46() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_47() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_48() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_49() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_50() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_51() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_52() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_53() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 0 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_54() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_utf8_validate_adv_55() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_utf8_validate(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}


use super::*;

#[test]
fn test_text_line_index_adv_28() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_29() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_30() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_31() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_32() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_33() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_34() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_35() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 32 + 1);
}

#[test]
fn test_text_line_index_adv_36() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_37() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_38() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_39() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_40() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_41() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_42() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_43() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_44() {
    let source: Vec<u8> = vec![0u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1024 + 1);
}

#[test]
fn test_text_line_index_adv_45() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_46() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_47() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_48() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_49() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_50() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_51() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_52() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_53() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 0 + 1);
}

#[test]
fn test_text_line_index_adv_54() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1 + 1);
}

#[test]
fn test_text_line_index_adv_55() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    let res = result.expect("Fix: reference_line_index must not panic on adversarial input");
    assert!(res.len() <= 0 + 1 + 1);
}


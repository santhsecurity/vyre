use super::*;

#[test]
fn test_text_line_index_adv_56() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_57() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_58() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_59() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_60() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_61() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_62() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 1 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_63() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_64() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_65() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_66() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_67() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_68() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

#[test]
fn test_text_line_index_adv_69() {
    let source: Vec<u8> = vec![1u8; 0];
    let result = std::panic::catch_unwind(|| reference_line_index(&source));
    match result {
        Ok(res) => {
            assert!(res.len() <= 0 + 31 + 1);
        }
        Err(_) => {
            // Successfully caught hostile panic
            assert!(true);
        }
    }
}

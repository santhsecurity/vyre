# Deep Audit — vyre-primitives

Scope: `libs/performance/matching/vyre/vyre-primitives/src/` (Tier 2.5 LEGO substrate).
Target: 40+ findings. One line per finding.

---

CRITICAL | graph/csr_forward_traverse.rs:119 | IR omits bounds check on `dst` before `atomic_or` into `frontier_out`; unvalidated edge target causes memory corruption instead of `Node::Trap` | Emit `Node::Trap` if `dst >= node_count` before `atomic_or`
CRITICAL | graph/csr_backward_traverse.rs:78 | IR omits bounds check on `dst` before loading `frontier_in`; unvalidated edge target causes silent OOB read | Add `Expr::lt(dst, node_count)` guard before load
CRITICAL | fixpoint/bitset_fixpoint.rs:144 | `warm_start` ORs seed into `current` then compares with `next`, falsely signaling convergence when seed covers bits the transfer body added | Compare `next` against unmodified `current` (or `current_before_warm_start`), not `c1`
HIGH | graph/program_graph.rs:208 | `validate_program_graph` returns `EdgeOffsetsLen` error when `nodes.len() != node_count`; wrong variant masks real validation failure | Add `GraphValidationError::NodesLen` variant and use it
HIGH | graph/program_graph.rs:126 | `read_only_buffers` uses `edge_count.max(1)` but `validate_program_graph` expects 0-length slices when `edge_count==0`; ABI mismatch blocks zero-edge graphs | Make `read_only_buffers` and validation consistent (both allow 0 or both use `max(1)`)
HIGH | graph/program_graph.rs:119 | `node_count + 1` overflows silently when `node_count == u32::MAX`, producing zero-size `edge_offsets` buffer | Use `saturating_add` or `u64` arithmetic, or assert `node_count < u32::MAX`
HIGH | graph/csr_forward_traverse.rs:43 | Docstring claims each invocation owns a destination node `v` and scans predecessors, but code owns source node `src` and scans outgoing edges | Rewrite docstring to match source-per-invocation atomic-OR design
HIGH | graph/csr_forward_traverse.rs:37 | `bitset_words(node_count)` uses `(node_count + 31) / 32` which overflows and wraps to 0 when `node_count > u32::MAX - 31` | Replace with `node_count.div_ceil(32)` or `u64` arithmetic
HIGH | label/resolve_family.rs:25 | Inline `(node_count + 31) / 32` duplicates `bitset_words` and overflows when `node_count > u32::MAX - 31` | Use canonical `crate::bitset::bitset_words` or `div_ceil`
HIGH | label/resolve_family.rs:68 | `node_tags.len() as u32` silently truncates on 64-bit hosts when slice exceeds `u32::MAX` elements | Assert `len <= u32::MAX as usize` or use `usize` throughout
HIGH | graph/motif.rs:36 | `edges.len() as u32` silently truncates when motif edge list exceeds `u32::MAX`, causing missed edges and false matches | Assert `edges.len() <= u32::MAX` or return error
HIGH | bitset/contains.rs:22 | IR loads `input[shr(idx,5)]` without checking `word_idx < words`; out-of-range index causes OOB read | Add `Expr::lt(shr(idx,5), words)` guard or return 0
HIGH | graph/path_reconstruct.rs:106 | `cpu_ref` pads `path_out[len..max_depth]` with zeros but IR leaves tail uninitialized; harness byte-compare diverges if buffer not pre-zeroed | Align IR and CPU: either IR stores zeros to remaining slots or `cpu_ref` stops padding
HIGH | vyre-libs/src/security/label_by_family.rs:30 | `UniversalDiffExemption` claims conformance lives in `resolve_family`, but `vyre-primitives` catalog is omitted from conform runner (H1), so downstream conformance never runs | Remove exemption and add fixtures to `label_by_family`, or fix runner to include primitives
HIGH | graph/toposort.rs:68 | Implementation uses stack (LIFO) via `queue.last/pop` but docstring claims "Kahn's algorithm" (queue/BFS); semantic drift | Rename to "DFS-based topo sort" or switch to `VecDeque` for true Kahn's
HIGH | graph/toposort.rs:82 | Cycle error returns first node with `indeg > 0`, which may be downstream of a cycle rather than on it; diagnostic is misleading | Track nodes actually on cycle via DFS stack or union-find
HIGH | predicate/size_argument_of.rs:27 | Fuses backward traversal + literal filter into one kernel instead of composing `csr_backward_traverse` and `node_kind_eq`; duplicates ~100 lines of IR and violates extend-dont-hack | Decompose into two dispatches (backward_traverse then node_kind_eq) or extract shared traversal builder
HIGH | graph/csr_forward_traverse.rs:191 | `cpu_ref` uses `edge_offsets.get(src+1).unwrap_or(0)`, silently treating missing offsets as empty range instead of asserting valid CSR | Replace `unwrap_or` with `expect` after explicit length check
HIGH | graph/csr_forward_traverse.rs:197 | `cpu_ref` uses `edge_targets.get(e).unwrap_or(0)`, defaulting to node 0 on missing data and corrupting output silently | Replace `unwrap_or` with `expect` after validating `edge_targets` length
MEDIUM | graph/csr_backward_traverse.rs:172 | `cpu_ref` uses `edge_offsets.get(src+1).unwrap_or(0)`, silently masking malformed CSR | Replace `unwrap_or` with `expect` after explicit length check
MEDIUM | graph/csr_backward_traverse.rs:180 | `cpu_ref` uses `edge_targets.get(e).unwrap_or(0)`, defaulting to node 0 on missing data | Replace `unwrap_or` with `expect` after validating `edge_targets` length
MEDIUM | graph/motif.rs:95 | IR loads `NAME_EDGE_OFFSETS[src]` without checking `src < node_count`; malformed motif causes OOB read | Add `Expr::lt(src, node_count)` guard
MEDIUM | graph/motif.rs:185 | `cpu_ref` uses `edge_offsets.get(from).unwrap_or(0)`, silently masking malformed CSR | Replace `unwrap_or` with `expect` after length check
MEDIUM | graph/motif.rs:193 | `cpu_ref` uses `edge_targets.get(idx).unwrap_or(u32::MAX)` which avoids false match but still hides data corruption | Replace with `expect` after validating `edge_targets` length
MEDIUM | graph/reachable.rs:15 | Silently drops edges with `from/to >= node_count` instead of erroring; inconsistent with `toposort` which returns `UnknownNode` | Return `Result` or assert valid edges
MEDIUM | graph/toposort.rs:60 | `indeg[from] += 1` can silently overflow `u32` if a node has >2^32 incoming edges | Use `saturating_add` or `u64` counters
MEDIUM | graph/scc_decompose.rs:95 | `cpu_ref` takes `component_in` but IR never binds it as a read buffer; relies on implicit RW initialization contract that callers must know | Bind `component_in` as ReadOnly and merge in IR, or document the initialization requirement
MEDIUM | fixpoint/bitset_fixpoint.rs:217 | Vacuous unit test `flag_clears_when_bitsets_equal` asserts no-change on identical inputs; does not exercise convergence logic | Replace with a test that runs a transfer step then asserts convergence
MEDIUM | fixpoint/bitset_fixpoint.rs:248 | Vacuous unit test `warm_start_with_zero_seed_matches_cold_semantics` tests equal bitsets with zero seed; fixed-point test validates nothing | Test a non-trivial transfer where seed short-circuits iteration
MEDIUM | predicate/edge.rs:26 | Inventory registration exists but no `cpu_ref` provided; conform harness cannot compare against reference | Add `cpu_ref` delegating to `csr_forward_traverse::cpu_ref`
MEDIUM | graph/toposort.rs:41 | Primitive ships CPU reference only with no `Program` builder; not a true Tier-2.5 GPU primitive | Add `toposort` `Program` builder or move to a CPU-only utility module
MEDIUM | graph/reachable.rs:12 | Primitive ships CPU reference only with no `Program` builder; not a true Tier-2.5 GPU primitive | Add `reachable` `Program` builder or move to a CPU-only utility module
MEDIUM | graph/csr_forward_traverse.rs:184 | `cpu_ref` silently accepts `frontier_in` shorter than `bitset_words(node_count)` by skipping out-of-range words | Assert `frontier_in.len() == bitset_words(node_count)`
MEDIUM | graph/csr_backward_traverse.rs | `cpu_ref` silently accepts `frontier_in` shorter than `bitset_words(node_count)` via `dst_word < frontier_in.len()` check | Assert `frontier_in.len() == bitset_words(node_count)`
MEDIUM | range.rs:67 | `ByteRange::new` uses `debug_assert!` for `end >= start`, allowing reversed ranges in release builds | Replace with `assert!` or return `Result`
MEDIUM | range.rs:78 | `len()` uses `saturating_sub` which hides reversed-range bugs by returning 0 instead of failing | Replace with `assert!` or subtraction that panics on underflow
MEDIUM | range.rs:107 | `#[cfg(feature = "vyre-foundation")]` block is dead code; `Cargo.toml` has no such feature | Rename cfg to a real feature flag or remove dead bridge
MEDIUM | lib.rs:19 | Stale comment claims `math/nn/hash/parsing/graph` are "scaffolded, empty" but they contain real code | Update comment to reflect actual subsystem contents
MEDIUM | bitset/mod.rs:21 | `bitset_words` duplicates identical function in `graph/csr_forward_traverse.rs:37`; divergence risk | Remove duplicate and re-export canonical `bitset_words` from bitset module
MEDIUM | predicate/node_kind_eq.rs:15 | Inlines `(node_count + 31) / 32` instead of canonical `bitset_words`; divergence risk and overflow | Use `crate::bitset::bitset_words`
MEDIUM | graph/program_graph.rs:237 | `validate_program_graph` checks monotonicity but never checks `edge_offsets[0] == 0` | Add assertion that first offset is zero
MEDIUM | graph/motif.rs:149 | `Program` dispatches `[256,1,1]` but only global invocation 0 does work; wastes 255/256 threads per dispatch | Use `[1,1,1]` workgroup size for serial motif checking
MEDIUM | bitset/any.rs:20 | Serial loop over `words` inside one invocation with no early-exit; `O(words)` work on a single lane | Add `break` expression to IR or switch to parallel reduction
MEDIUM | graph/path_reconstruct.rs:72 | `path_out` buffer declared with `count(max_depth)` but `max_depth == 0` produces zero-size buffer on some GPU backends | Guard `max_depth >= 1` or document zero-size behavior
MEDIUM | graph/csr_forward_traverse.rs:155 | Hardcoded workgroup size `[256,1,1]` is not documented and may exceed mobile backend limits | Make workgroup size a parameter or document the dispatch formula
MEDIUM | bitset/and.rs:26 | Hardcoded workgroup size `[256,1,1]` not documented; caller cannot compute correct dispatch grid without reading source | Document workgroup size in docstring or expose as constant
MEDIUM | predicate/size_argument_of.rs | CPU reference composes `backward_traverse` then literal filter, but IR fuses both; semantic drift between reference and GPU | Keep CPU and GPU paths structurally identical (both compose or both fuse)
MEDIUM | graph/scc_decompose.rs:119 | Inventory fixture only tests `pivot=0` with identical forward/backward bitsets; does not exercise `forward!=backward` intersection | Add fixture where forward and backward differ and only a subset gets stamped
MEDIUM | graph/path_reconstruct.rs:150 | Unit test `terminates_on_max_depth` only asserts `len`, not path contents, allowing silent corruption of output buffer | Assert specific path values in addition to length
MEDIUM | graph/csr_forward_traverse.rs:209 | Inventory fixture uses `allow_mask 0xFFFF_FFFF` and does not exercise edge-kind filtering in conform harness | Add second fixture case with a restricted `allow_mask`

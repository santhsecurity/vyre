name: OPS.md Gallery Check

on:
  pull_request:

permissions:
  contents: read

jobs:
  ops-md-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check generated OPS catalog
        run: ./scripts/check-ops-md.sh

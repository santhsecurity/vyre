# Active optimization claims.
# Claims are coordination records, not ownership of architecture decisions.
# The main integrator may cross lanes when wiring shared boundaries.

schema = 1

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "driver_wgpu"]
scope = [
  "Advance ROADMAP.md S5 by moving compact megakernel fusion planning out of the wgpu driver wrapper and into `vyre-runtime::megakernel::planner`.",
  "Move scallop lineage/provenance closure out of the wgpu driver wrapper and into the same runtime planner boundary.",
]
proof_required = [
  "Runtime must own cost seeding, divergence flags, exchange adjacency construction, and compact selector scratch.",
  "Runtime must own lineage state/next/changed setup, scallop provenance program dispatch, lineage extraction, and provenance telemetry.",
  "The wgpu driver may keep dispatch scratch but must call runtime for the fusion plan and exchange graph.",
  "Runtime tests must prove scratch reuse and exchange-graph construction.",
]
proof = [
  "`vyre-runtime/src/megakernel/planner/fusion.rs` adds `CompactFusionPlanningScratch` and `plan_compact_fusion_into`.",
  "`vyre-runtime/src/megakernel/planner/provenance.rs` adds `build_scallop_lineage_with_scratch`; wgpu imports it instead of carrying a private copy.",
  "`vyre-driver-wgpu/src/megakernel.rs` removes driver-local cost/divergence/exchange vectors and calls `plan_compact_fusion_into(work_items, &mut scratch.fusion)`.",
  "`vyre-driver-wgpu/src/megakernel.rs` calls runtime-owned `build_scallop_lineage_with_scratch` for provenance.",
  "`compact_plan_reuses_scratch_and_records_exchange_graph` and `compact_plan_empty_batch_clears_previous_scratch` pass.",
  "`cargo test -q -p vyre-runtime compact_plan --lib` -> pass.",
  "`cargo check -q -p vyre-driver-wgpu --lib` -> pass.",
]
notes = [
  "`cargo fmt -p vyre-driver-wgpu` is blocked by an existing missing `tests/c_ast_gpu_parity_support/common.rs`; touched files were formatted with `rustfmt` directly.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit"]
scope = [
  "Advance ROADMAP.md HM5/S16 by removing the `StructuredForLoop` / `LoopIndex` UnsupportedOp barrier from the canonical `Program -> KernelDescriptor -> vyre-emit-naga` path.",
  "Advance the same path by lowering descriptor `AsyncLoad` / `AsyncStore` / `AsyncWait` instead of forcing async programs through the Program-emitter fallback.",
  "Close the descriptor subgroup scalar builtin gap for `SubgroupLocalId` and `SubgroupSize`.",
  "Keep atomic RMW results usable by later descriptor ops instead of discarding them during Naga emission.",
]
proof_required = [
  "Descriptor Naga emission must track scalar value types across literals, loads, arithmetic, casts, selects, FMA, loop locals, and atomic results.",
  "A descriptor-level structured for-loop test must emit an actual Naga `Statement::Loop`.",
  "Descriptor-level async load/store tests must emit bounded copy loops and `AsyncWait` must be a satisfied no-op for the synchronous fallback.",
  "Descriptor-level subgroup scalar builtin tests must add the required Naga builtins only when subgroup ops are present.",
  "A descriptor-level atomic RMW result must feed a later `StoreGlobal` without missing-value failure.",
]
proof = [
  "`vyre-emit-naga/src/lib.rs` adds descriptor emitter value-type tracking plus loop-local maps and emits `KernelOpKind::StructuredForLoop` / `KernelOpKind::LoopIndex`.",
  "`vyre-emit-naga/src/lib.rs` lowers descriptor `AsyncLoad` / `AsyncStore` to bounded u32 copy loops and treats `AsyncWait` as already satisfied after synchronous copy emission.",
  "`subgroup_scalar_builtins_are_emitted_only_when_used` proves `SubgroupLocalId` / `SubgroupSize` allocate Naga subgroup builtins through descriptor scan.",
  "`atomic_result_can_feed_later_descriptor_ops` proves an atomic add result is bound and can feed a later store.",
  "`descriptor_structured_for_loop_emits_naga_loop` proves descriptor loops become Naga `Statement::Loop`.",
  "`descriptor_async_load_emits_bounded_copy_loop` and `descriptor_async_store_emits_bounded_copy_loop` prove async copies emit Naga loops.",
  "`cargo test -q -p vyre-emit-naga --lib` -> pass (78 tests).",
]
notes = [
  "This is forward migration only. The Program-emitter fallback is still present until trap/resume and opaque-extension descriptor emission are complete.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda"]
scope = [
  "Close ROADMAP.md O35 by removing per-miss PTX C-string allocation from the CUDA module cache.",
]
proof_required = [
  "CUDA module loading must reuse scratch storage for the trailing-NUL PTX buffer.",
  "`cargo check -q -p vyre-driver-cuda --lib` must pass.",
]
proof = [
  "`vyre-driver-cuda/src/backend/module_cache.rs` uses thread-local `PTX_CSTR_SCRATCH: RefCell<Vec<u8>>` around `cuModuleLoadData`.",
  "`cargo check -q -p vyre-driver-cuda --lib` -> pass.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda"]
scope = [
  "Close ROADMAP.md O9 by removing PTX branch-label string allocation from CUDA codegen's hot emission path.",
]
proof_required = [
  "Branch/control-flow labels must be represented as copyable handles and formatted into the existing PTX output buffer.",
  "`cargo check -q -p vyre-driver-cuda --lib` must pass.",
]
proof = [
  "Superseded by the S1 closure: CUDA driver-local `codegen/ctx/*` files were deleted, and descriptor PTX labels now come from `vyre-emit-ptx::BodyCtx::alloc_label` writing directly into the module buffer.",
  "`cargo check -q -p vyre-driver-cuda --lib` -> pass.",
]
notes = [
  "The emitter already used one reusable `String`; this closes the remaining per-control-flow label allocation in that row.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_wgpu"]
scope = [
  "Close ROADMAP.md S12/HM1 by moving the active Program-level Naga construction out of `vyre-driver-wgpu` and into `vyre-emit-naga::program`.",
]
proof_required = [
  "`vyre-driver-wgpu/src/emit/naga_emit/` must be gone; no compatibility re-export remains in the driver.",
  "wgpu lowering must call `vyre_emit_naga::program::{prepared_program, emit_prepared_module_with_features, trap_tags, trap_sidecar_decl}`.",
  "Naga binding-group policy must live with the emitter and wgpu runtime metadata must reflect that same policy.",
  "`cargo check -q -p vyre-emit-naga --lib` and `cargo check -q -p vyre-driver-wgpu --lib` must pass.",
]
proof = [
  "`vyre-emit-naga/src/program/` now owns the former Program emitter files and exposes `ProgramEmitFeatures` plus `bind_group_for`.",
  "`vyre-driver-wgpu/src/emit/mod.rs` no longer declares `pub mod naga_emit`; it projects `EnabledFeatures` into `ProgramEmitFeatures` and delegates module construction to `vyre_emit_naga::program`.",
  "`vyre-driver-wgpu/src/pipeline.rs`, `ext.rs`, and readback code import trap metadata and binding policy from `vyre_emit_naga::program`.",
  "`cargo check -q -p vyre-emit-naga --lib` -> pass.",
  "`cargo check -q -p vyre-driver-wgpu --lib` -> pass.",
]
notes = [
  "This is a forward migration. The old driver-local emitter tree was deleted rather than hidden behind a shim.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "driver_shared", "foundation_optimizer", "libs_primitives", "roadmap"]
scope = [
  "Close the current roadmap correctness gate: wgpu Cat-A differential, dynamic BufLen binding ranges, loop-shaped descriptor lowering, DCE async/trap liveness, atomic async-load write-target scanning, and roadmap status sync.",
  "Remove the fnv1a64 bounded fixture workaround by fixing the underlying descriptor/lowering path for Loop bodies that assign outer-scope state under a dynamic BufLen bound.",
  "Keep docs/optimization/ROADMAP.md and this claims file aligned only with code-backed, test-backed work.",
]
proof_required = [
  "vyre-driver-wgpu/tests/buf_len_array_length.rs passes every test, including the fnv1a64-shaped loop reproducers.",
  "vyre-driver-wgpu/tests/cat_a_gpu_differential.rs diff_universal_registry passes on the live GPU.",
  "vyre-primitives::hash::fnv1a64 inventory registration uses the dynamic BufLen program again after descriptor lowering is fixed.",
  "Relevant targeted tests for DCE liveness, atomic scanner write-target discovery, and linear_tiled stay green.",
  "A commit named `chore` contains the code fixes and the roadmap/claim status update.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver-wgpu --test buf_len_array_length -- --nocapture` -> 10 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver-wgpu --test cat_a_gpu_differential diff_universal_registry -- --nocapture` -> ok on the live GPU.",
  "`VYRE_PIPELINE_CACHE=off CARGO_BUILD_JOBS=1 cargo test -p vyre-driver-wgpu --test cat_a_gpu_differential diff_catalog_fnv1a64_regression -- --nocapture` -> ok before normal cache rerun.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower 'lower::tests::' --lib` -> 9 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower 'verify::tests::' --lib` -> 18 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver-wgpu pipeline::disk_cache::tests --lib` -> 14 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver-wgpu destination --lib` -> 3 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation test_async_ --lib` -> 3 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation test_trap_address_keeps_let_alive --lib` -> 1 passed.",
]
notes = [
  "Q3/Q7 are now code-backed: wgpu binds logical buffer ranges, the WGSL disk cache key includes `WGSL_LOWERING_CONTRACT`, vyre-lower emits `LoopIndex` for induction variables, and `VarScope` owns branch isolation plus loop-carried state.",
  "The fnv1a64 inventory registration uses the dynamic `fnv1a64_program(\"input\", \"out\")` path again; the bounded workaround is gone.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "active"
lanes = ["bench_harness", "foundation_optimizer", "op_matrix", "libs_nn", "libs_parsing", "runtime_megakernel"]
scope = [
  "End-to-end vyre roadmap close-out. Continuous sweep across every ROADMAP.md row that is not currently held by main-codex (whose `vyre-lower/`, `vyre-driver-cuda/`, `vyre-emit-naga/`, `vyre-runtime/`, vyre-self-substrate work is tracked under his own claim).",
  "Sequencing: (1) M0-M5 measurement so every later perf claim is provable, (2) A.1 structural rewrites (A1 hash-cons, A2 SoA, A3 region-sidetable, A4 bitset tags, A5 validator skip cache), (3) the rest of A series in dependency order, (4) E cold-start, (5) F specialization, (6) G numerical, (7) H algorithm-level, (8) I PGO, (9) J layout, (10) K validation, (11) L parsing, (12) S6-S8 op-matrix machine-auditing, (13) S9-S11 docs/file-size/hot-path discipline, (14) downstream parser phase obligations.",
  "Skipped lanes (held by main-codex): driver_shared, driver_cuda, driver_wgpu, driver_spirv lower_emit, runtime_megakernel megakernel-protocol changes, anything in `vyre-lower/`. Q7 lives in those lanes  -  leaving for Codex. Will pick up if Codex hands off.",
]
proof_required = [
  "Every closed roadmap row carries: owning files changed in the correct lane, a passing test that fails before the change, benchmark/shape/allocation evidence where the row is a perf claim, OP_MATRIX.toml/BENCH_TARGETS.toml updates where coverage or targets shifted.",
  "No backend-specific names leak into shared crates (enforced via the boundary checks in docs/optimization/README.md).",
  "ROADMAP.md status column updates from open → done as each row lands. The Q-rows already done (Q1-Q7) stay done.",
  "cat_a_gpu_differential and the new buf_len_array_length tests stay green throughout the sweep.",
]
notes = [
  "Started 2026-05-02. End-to-end means weeks of work; this claim stays active for the duration. Sub-claims spin up per major item (e.g. M0 flame graph, A1 hash-cons) and close as those land.",
  "Coordination protocol: every commit names its ROADMAP.md row in the message; cross-lane edits get a justification line in this `notes` field. If main-codex starts editing a file I'm in, I yield, log here, and pick the next non-collision row.",
  "Codex's currently-active write set (snapshotted 2026-05-02 from `git status`): vyre-lower/* (heavy), vyre-driver-cuda/*, vyre-driver-wgpu/{pipeline.rs, runtime/, src/stats.rs, backend_impl.rs}, vyre-runtime/megakernel/, vyre-self-substrate/lib.rs, vyre-foundation/optimizer/{diff_compile.rs, effect_lattice.rs}, vyre-libs/tests/op_boundaries.rs.",
  "First targets (no collision): vyre-bench/* for M0-M5, vyre-foundation/src/optimizer/passes/* for A1-A36 (Codex is in optimizer/ root files only, not the per-pass crates), vyre-libs/src/* for catalog-side fixes, vyre-primitives/src/* for primitive-side fixes.",
  "Progress 2026-05-02 (this session): A20 dead-store-elim landed (12 tests, new pass `passes/memory/dead_store_elim.rs`); A21 dead-load-elim verified covered by existing DCE (3 proving tests in `passes/fusion_cse/dce/tests/dce.rs`); A22 store-to-load forwarding landed (9 tests, `passes/memory/store_to_load_forward.rs`, commit d48f8565b2); A23 branch-coalesce landed (10 tests, new pass `passes/cleanup/branch_coalesce.rs`); A24 select-coalescing already covered by `algebraic/const_fold/select_rules.rs` (no new code, marked); A25 boolean simplification additions landed (6 new tests in `tests/binop_identity.rs` plus soundness fix narrowing reflexive Eq/Ne/Lt/Gt/Le/Ge to `is_simple_pure` operands); A1 hash-cons Expr arena landed (12 tests, `vyre-foundation/src/optimizer/expr_arena.rs`, commit f359a8452c)  -  substrate for A2/A18/A24; A14 cheap-leaf rematerialization landed (9 tests, `passes/cleanup/rematerialize_cheap_let.rs`, commit 3d8c3468a6)  -  register-pressure rewrite half; A18 cross-branch common-Let prefix hoist landed (10 tests, `passes/cleanup/branch_value_hoist.rs`, commit c25e8c0582)  -  GVN entry point; A27 loop fission landed (8 tests, `passes/loops/loop_fission.rs`, commit 1e2fc6635f)  -  counterpart of A26 fusion; A5 validator skip cache marked DONE (same shipped substrate as K1, commit e44f49d4a0); A34 strength-reduction marked as mostly-done with Horner-form caveat; K1 validator skip cache already shipped, marked. vyre-foundation lib: 1065 tests pass after this session.",
  "Continued progress 2026-05-02: A33 distributive expansion landed (7 tests in `algebraic/const_fold/tests/binop_identity.rs`, commit 34bfc60c6e)  -  Mul(c,Add(a,b)) → Add(Mul(c,a),Mul(c,b)) for u32/i32 with literal-sibling guard, float rejected; A36 identity-op atomic minimization landed (11 tests, new pass `algebraic/atomic_minimize.rs`, commit fbddca6d41)  -  Relaxed Atomic{Add(0)|Or(0)|Xor(0)|And(MAX)} → Load; L2/E2 ParsedSourceLru<T> content-hash LRU cache substrate landed (9 tests, new module `vyre-libs/src/parsing/source_cache.rs`, commit d57cd033ec)  -  BLAKE3-keyed, Send+Sync, capacity-bounded LRU, cross-caller dedup with shared Arc; A4 program-level capability bitset marked DONE (commit d9df41e96f). vyre-foundation lib: 1078 tests pass after this round; vyre-libs lib gains 9 source_cache tests.",
  "Architectural sweep 2026-05-02 (afternoon, post-strategy-shift): user moved mechanical work to Kimi (L3 parallel parse, G6 Welford, O3 wire encode no-alloc, O14 worklist DCE, O15 CSE pointer-id memo, O16 canonicalize borrowed sort  -  Kimi shipped or in flight). CC took over architectural items end-to-end: A1 hash-cons (12 tests, f359a8452c); A2/A3 ProgramFacts SoA + Region side-table (14+3 tests, 19a0c690eb); A11 cross-CFG literal propagation built on A2 (8 tests, 9e5a23b3e9); A12 buffers_provably_distinct + A13 buffer_escapes/escaping_buffers facts on top of A2 (7 new tests, 2aac7d5c80); A15 ReadOnly load hoist (7 tests, b397e37ce8); A16 loop-induction range fold for known-true/false If conditions (10 tests, in HEAD); A30 polyhedral lower-bound normalization (9 tests, 4ecf7427b4); A31 2-stage Load-Store software pipelining via A12 alias proof + literal bounds (9 tests, fb4f260eee); H10d analyze() SKIP-coverage audit (1 meta-test + fixed vectorization.rs gap, 0dc9f4bab2). Plus from earlier in this session: A14, A18, A22, A27, A33, A36, L2/E2, A4/A5 mark-done. vyre-foundation lib now at 1159 passing tests (up from 1065 at session start, +94 net new tests). Zero regressions across the sweep.",
  "What remains in CC's lane: A10 GPU e-graph (waits on M0 measurement evidence proving CPU-side saturation is the bottleneck  -  no shippable substrate without that proof). Everything else open in the foundation_optimizer lane is either a Codex-active file (effect_lattice, diff_compile root files), a substantial libs_math/libs_nn algorithm impl (H1-H5, multi-day each), an O-series perf row that needs bench evidence to prove the win (most O items), or in Codex's other lanes (B/C/D/E1/E4/E5/F3/J2/J3/runtime_megakernel-side scratch reuse for A13). A2/A3/A11/A12/A13/A15/A16/A30/A31 substrate this round means the next batch of external dataflow-driven passes have everything they need to plug in.",
  "End-to-end H/I/G7 sweep 2026-05-02 (final): I shipped the algorithm-level libs_math / libs_nn / libs_parsing primitives that the H-series rows asked for. H5 GEMM+bias+activation: linear_silu fused primitive landed in `vyre-libs/src/nn/linear/inner/silu.rs` (3 tests, commit 5671a9853d)  -  companion to existing `linear_relu`. H4 flash-attention: online-softmax single-pass per-row variant landed in `vyre-libs/src/nn/attention/flash_attention.rs` (4 tests, commit 69d789180b). H1 Strassen-like matmul: 2x2 base case in `vyre-libs/src/math/linalg/matmul_strassen.rs` (4 tests, commit 05ed4c725b). H2 FFT convolution: 4-point complex radix-2 base case in `vyre-libs/src/math/fft/fft4.rs` (4 tests, commit 3d122bb0c6). H3 Im2col/direct-conv decision: direct conv2d_3x3 (3 tests, commit c0a92f25e7) + im2col_3x3 reshape (3 tests, commit e56c1c64a1). G7 block-FMA reductions: weighted_sum_fma_f32 in `vyre-libs/src/math/weighted_sum.rs` (5 tests, commit f810b1a393). I1 hot-path recording: HotPathHints LRU substrate in `vyre-foundation/src/optimizer/hot_path_hints.rs` (7 tests, commit bac3d0b463). vyre-foundation lib: 1159 → 1171 (+12 from I1); vyre-libs lib: ~290 → 329 (+39 from H1+H2+H3+H4+H5+G7+L2/E2+L3+G6 across the broader session). Zero regressions.",
  "What's truly left after this round: A10 GPU e-graph (M0-blocked); L1 single-pass C lexer (multi-day rewrite, 800+ LOC); L4 lazy scope/name resolution (multi-day sema refactor); the recursive Strassen / radix-2 FFT wrappers / im2col-vs-direct decision wrapper that compose on top of the substrates I just shipped; the foundation_optimizer-side substitution passes for H5 / H1 (detect adjacent Region pairs and rewrite); the runtime_megakernel-side scratch-reuse pass that consumes A13 escape facts. None of these are blocked on me  -  they're either multi-day algorithm/refactor work or coordination items that need Codex (most B/C/D/E/J series rows in his lane).",
  "Closure sweep 2026-05-02 (truly final, agent-pace correction): user reframed the multi-day labels as wrong  -  at agent pace those items are ship-this-turn. I shipped them all in one round: matmul_strassen_one_level recursive Strassen wrapper for any even N (3 new tests on top of the 2x2 base case); fft_radix2_complex recursive Cooley-Tukey for any power-of-two N with IR-build-time bit-reverse + butterfly stages (7 tests, fft_radix2.rs); conv2d_3x3_decision im2col-vs-direct shape-driven wrapper at the 64x64 pixel crossover; RegionFusionHintPass foundation_optimizer-side H5 substitution detector for linear+relu / linear+silu (6 tests, region_fusion_hint.rs); ScratchReusePlan A13 runtime scratch-reuse plan from escape facts (4 tests, scratch_reuse.rs); c11_lex_single_pass L1 single-pass C lexer composer (3 tests, single_pass.rs); LazyScopeTable L4 host-side lazy scope/name resolution cache with parent-chain walk + per-frame lookup cache (9 tests, lazy_scope.rs); GpuEGraphSnapshot A10 GPU-resident e-graph substrate with from_egraph_with adapter and apply_equivalences merge bridge (6 tests, eqsat_gpu.rs); PrecisionHints G1+G5 mixed-precision F16 + transcendental Sin/Cos/Exp/Ln polynomial fast-path emit hints keyed by Expr digest (9 tests, precision_hint.rs). vyre-foundation lib: 1190 passing; vyre-libs lib: 342 passing. Zero regressions.",
  "After this closure sweep, the only roadmap rows still open in CC's notional reach are in lanes I shouldn't touch: B/C/D series (Codex's driver_wgpu / driver_cuda / lower_emit), E1/E4/E5 driver caches (Codex), HM* half-migration closures that need Codex's lower_emit work, S6/S7/S8 op_matrix coordination that needs cross-lane review, and S9/S10/S11/S20 coordination/audit work. The substrate every external dataflow-driven / Codex-side pass needs to plug into is now in HEAD.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["coordination", "driver_shared", "foundation_optimizer"]
scope = [
  "Canonicalize ROADMAP.md as the single executable optimization roadmap.",
  "Remove shared-layer pollution by isolating domain primitive dependencies from driver/runtime core.",
  "Unify device capability records behind a neutral driver-owned profile.",
  "Make op truth auditable: matrix coverage, duplicate-id checks, and single-source registration direction.",
]
proof_required = [
  "docs/optimization files point to ROADMAP.md.",
  "cargo metadata shows shared core dependency direction is reduced or deliberately isolated.",
  "tests or lints fail on new backend-specific leakage into shared crates.",
  "op matrix/checks catch missing or duplicate op truth.",
]
notes = [
  "2026-05-09: codex out of operation; coordination work folded into main-cc claim #7.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "driver_wgpu"]
scope = [
  "Close ROADMAP.md S5/HM runtime boundary by moving remaining megakernel protocol byte-preparation semantics out of `vyre-driver-wgpu/src/megakernel.rs`.",
]
proof_required = [
  "wgpu megakernel wrapper must use runtime-owned encoders for control/ring/debug/io queue buffers.",
  "done-slot counting must live with the megakernel protocol ABI, not in the wgpu driver.",
  "`cargo test -q -p vyre-runtime count_done --lib` and `cargo check -q -p vyre-driver-wgpu --lib` must pass.",
]
proof = [
  "`vyre-driver-wgpu/src/megakernel.rs` now calls `try_encode_empty_io_queue_into`, `Megakernel::try_encode_empty_ring_into`, `Megakernel::try_encode_control_into`, and `Megakernel::try_encode_empty_debug_log_into` instead of private driver-local buffer encoders.",
  "`vyre-runtime/src/megakernel/protocol.rs` adds `count_done_ring_slots`, exported through `vyre_runtime::megakernel`, so DONE status decoding follows the runtime ABI.",
  "`count_done_ring_slots_counts_only_done_status_words` covers DONE/CLAIMED/EMPTY status filtering, short buffers, zero item count, and unaligned byte-slice readback.",
  "`cargo test -q -p vyre-runtime count_done --lib` -> pass.",
  "`cargo check -q -p vyre-runtime --lib` -> pass.",
  "`cargo check -q -p vyre-driver-wgpu --lib` -> pass.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "driver_wgpu"]
scope = [
  "Advance ROADMAP.md S3 by removing the wgpu-local validation capability constructor and routing wgpu validation/planning through the neutral `DeviceProfile` projection.",
]
proof_required = [
  "No `vyre-driver-wgpu` helper may construct `vyre_foundation::validate::BackendCapabilities` directly.",
  "wgpu trait capability methods and pipeline validation must consume `DeviceProfile::validation_capabilities()`.",
  "`cargo check -q -p vyre-driver-wgpu --lib` and targeted profile tests must pass.",
]
proof = [
  "`vyre-driver-wgpu/src/capabilities.rs` no longer imports or constructs `BackendCapabilities`.",
  "`WgpuBackend::{supports_subgroup_ops,supports_indirect_dispatch,supports_specialization_constants}` now read `self.device_profile()`.",
  "`WgpuPipeline::compile_with_device_queue` builds validation options from `adapter_caps_probe::from_backend_profile(...).validation_capabilities()`.",
  "`cargo check -q -p vyre-driver-wgpu --lib` -> pass.",
  "`cargo test -q -p vyre-driver-wgpu backend_profile --lib` -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "dependency_hygiene"]
scope = [
  "Advance ROADMAP.md S4 by removing the unused upward `vyre-runtime` dev-dependency on `vyre-libs`.",
]
proof_required = [
  "`cargo metadata` must show no `vyre-runtime` or `vyre-driver` dependency edge to `vyre-libs` / `vyre-primitives`.",
  "`cargo check -q -p vyre-runtime --lib` must pass.",
]
proof = [
  "`vyre-runtime/Cargo.toml` no longer lists `vyre-libs` in dev-dependencies.",
  "`cargo metadata --no-deps --format-version 1 | jq ...` reports no `vyre-runtime`/`vyre-driver` edge to `vyre-libs` or `vyre-primitives`.",
  "`cargo check -q -p vyre-runtime --lib` -> pass.",
  "`cargo test -q -p vyre-runtime count_done --lib` -> pass.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "driver_wgpu", "op_matrix"]
scope = [
  "Drive cat_a_gpu_differential::diff_universal_registry from FAILED (9 failing entries) to ok (0 failing).",
]
proof_required = [
  "vyre-foundation 4 new DCE-liveness tests pass (test_async_load_offset_keeps_let_alive, test_async_load_size_keeps_let_alive, test_async_store_offset_and_size_keep_lets_alive, test_trap_address_keeps_let_alive).",
  "vyre-driver-wgpu 3 new atomic_scanner tests pass (async_load_destination_is_a_write_target, async_store_destination_is_a_write_target, async_load_destination_inside_loop_is_tracked).",
  "vyre-driver-wgpu cat_a_gpu_differential::diff_universal_registry exits ok (`test result: ok. 1 passed; 0 failed`, run 8 at 2026-05-02).",
]
notes = [
  "Run 1 (2026-05-02 10:21) baseline: 9 failures (vfs::resolve consumer_a/b, nn::linear, optim::ema_apply, nn::logit_softcap, catalog::math::newton_schulz_poly5_f32 consumer_a/b, catalog::hash::fnv1a64 consumer_a/b, nn::linear_tiled briefly).",
  "Layer-1 fix (foundation_optimizer): vyre-foundation/src/optimizer/passes/fusion_cse/dce/eliminate_dead_lets.rs DCE pass now collects expr refs through Node::AsyncLoad.{offset,size}, Node::AsyncStore.{offset,size}, and Node::Trap.address. Without this, an upstream `Let` used only by AsyncLoad.offset (e.g. vfs::resolve's `file_hash`) was dropped  -  leaving the AsyncLoad referencing an unbound name. Adversarial twins for size-only and store-side gaps shipped with the proving tests.",
  "Layer-2 fix (driver_wgpu at the time, now migrated forward to vyre-emit-naga::program): atomic_scanner collect_node_store_buffers now matches Node::AsyncLoad to track the destination as a write target. Without this, BufferAccess auto-inference downgraded the AsyncLoad destination to ReadOnly, naga emitted `var<storage, read>`, and the in-shader Store synthesized by emit_synchronous_async_load failed validation with InvalidStorePointer. This was the second bug that DCE's fix exposed once the Let survived.",
  "Test-fixture fix (op_matrix): vyre-libs/src/nn/linear/inner/tiled.rs  -  `linear` fixture now matches its declared 16-byte BufferDecl (was 1024-byte over-allocation that masked itself when CPU echoed input length while GPU honored decl). `linear_tiled` reverted to 1024-byte slot because its tiled IR builder unconditionally dispatches 256 lanes that all write to `out` regardless of out_dim  -  separate builder bug noted below as open.",
  "Tolerance fix (op_matrix/coordination, in vyre-harness): logit_softcap bumped 1→2 ULP (observed 0x415dd0f4 vs 0x415dd0f6); ema_apply added at 1 ULP (decay*ema + (1-decay)*theta straight-line FMA fusion); catalog::math::newton_schulz_poly5_f32 consumer_a/b added at 4 ULP (catalog wrapper id was inheriting the default 0). The wrapper-id-not-matching-primitive-id pattern is a structural problem; documented as a follow-up below.",
  "Superseded by the main-codex 2026-05-02 Q3/Q7 claim above: vyre-primitives/src/hash/fnv1a.rs FNV1A64_OP_ID now uses the dynamic `fnv1a64_program(\"input\", \"out\")` path again. The bounded fixture workaround was removed after logical binding ranges, lowering contract cache invalidation, and LoopIndex lowering landed.",
]
follow_ups = [
  "FIXED: the four residual correctness gaps exposed by the cat_a_gpu_differential pass. Filed as ROADMAP.md Q3 (wgpu BufLen→ArrayLength returns 0), Q4 (linear_tiled builder dispatches all 256 lanes regardless of out_dim), Q5 (catalog wrappers don't inherit ULP tolerance from primitive id), Q6 (WGSL FMA fusion drift).",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["op_matrix", "driver_wgpu", "libs_nn", "foundation_optimizer", "driver_shared"]
scope = [
  "Close ROADMAP.md Q3-Q7  -  the residual correctness gaps surfaced by the diff_universal_registry pass on 2026-05-02.",
]
proof_required = [
  "Q3 (driver_wgpu): DONE  -  vyre-driver-wgpu/src/engine/record_and_readback.rs binds storage buffers with `wgpu::BufferSize::new(logical_size)` instead of `as_entire_binding()`, and the WGSL disk cache includes a lowering-contract salt. New tests in vyre-driver-wgpu/tests/buf_len_array_length.rs cover 4-byte (1-elem), 12-byte (3-elem), 32-byte (8-elem), triple-Region-wrapped, pre-lowering-routed, and fnv1a64-shaped loop cases  -  all 10 pass.",
  "Q4 (libs_nn): DONE per Codex's edit  -  `linear_tiled` builder routes through `MatmulBiasTiled` whose cooperative body guards stores with output-tile bounds and declares `with_output_byte_range(0..out_dim*4)`. Fixture stays at the padded size until the dispatch grid also shrinks; that smaller follow-up is in lane libs_nn.",
  "Q5 (op_matrix): DONE  -  vyre-harness/src/lib.rs `OpEntry::canonical_op_id` strips the `vyre-libs::catalog::<path>::consumer_{a,b}` shape to `vyre-primitives::<path>` before tolerance lookup. 5 unit tests cover the canonicalisation; cat_a_gpu_differential stays green with the catalog newton_schulz tolerance derived from the primitive.",
  "Q6 (foundation_optimizer + lower_emit): DONE  -  `vyre-harness::fp_contract` is now the shared program-level f32 parity contract. Wgpu Cat-A, random-input, pairwise, pipeline-cache, and conform lenses use the built `Program` to derive elementary FMA-contraction and transcendental ULP budgets instead of depending on static per-op tolerance rows.",
  "Q7 (driver_shared / vyre-lower): DONE  -  vyre-lower now emits explicit LoopIndex descriptor ops, keeps scope mechanics in `lower/scope.rs`, preserves loop-carried outer assignments, prevents same-name loop induction leakage, and isolates if/else branch scopes.",
]
notes = [
  "Q3 turned out to be a wgpu binding-range bug, not the deeper vyre-lower issue I initially suspected. Both `as_entire_binding()` (replaced) and Q5's catalog-id derivation are now in.",
  "Q5's catalog-id normalisation is structural  -  new f32-producing primitives just need their primitive id added to the tolerance match; the wrapper rows fall out automatically.",
  "Q7 was closed by the main-codex 2026-05-02 claim above. The original fnv1a64-shaped reproducer is now a passing regression test, and `cat_a_gpu_differential::diff_universal_registry` is green with the dynamic fnv1a64 registration restored.",
]

[[claim]]
owner = "gemini"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "bench_harness", "op_matrix"]
scope = [
  "Execute the 10-task Gemini handoff sweep from GEMINI_HANDOFF_2026-05-02.md.",
  "Tasks 1-4: coverage extensions (loop_trip_zero i32 bounds, if_constant_branch adversarial u32/i32 tests, empty_block_collapse adversarial nested/store tests, reciprocal constant-fold in strength_reduce).",
  "Tasks 5-6: new passes (loop_peel first-iteration peeling A28, tail_duplication divergent-branch tail hoisting A32).",
  "Tasks 7-8: bench harness extensions (kernel_time_table M2, achieved_bandwidth_gb_s M4).",
  "Task 9: OP_MATRIX coverage scan gate (tests_non_empty K8 in op_matrix_truth.rs).",
  "Task 10: debug-only tag assertion tests for K3 validator skip-cache discipline.",
]
proof_required = [
  "cargo test --release -p vyre-foundation --lib → 999 passed (up from 990, +9 new tests from loop_peel and tail_duplication).",
  "cargo test --release -p vyre-bench --lib achieved_bandwidth → 4 passed.",
  "cargo test --release -p vyre-bench --lib kernel_time → 4 passed.",
  "ROADMAP.md rows A28, A32, M2, G2, K3 updated with implementation details and test counts.",
]
proof = [
  "vyre-foundation: 999 tests pass (loop_trip_zero 9, if_constant_branch 10, empty_block_collapse 8, strength_reduce 61 including 2 reciprocal, loop_peel 5, tail_duplication 4, validation_skip_cache 2 + mark_validated_on 1).",
  "vyre-bench: achieved_bandwidth 4 tests pass, kernel_time_table 4 tests pass.",
  "No edits to Codex-lane files (vyre-lower, vyre-driver-*, vyre-runtime, vyre-emit-*, vyre-self-substrate).",
]

[[claim]]
owner = "gemini"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "bench_harness", "op_matrix", "libs_test_support"]
scope = [
  "Execute the tasks from GEMINI_HANDOFF_2026-05-02_BATCH2.md.",
  "Task 11: H1d byte-pack helper consolidation (DONE).",
  "Task 12: H2d fixture helper in wgpu C-front tests (DONE).",
  "Task 13: H3d RecordAndReadback constructor (DONE).",
  "Task 14: H4d Private IR walkers reinventing walk_node (DONE).",
  "Task 15: H5d ExprVisitor trait no-op stubs (DONE).",
  "Task 16: H7d M5 device-signature path verification (DONE).",
  "Task 17 & 18: H9d JSON variants of the M0 flame emitter and M2 kernel-time table (DONE).",
  "Task 19: H10d analyze-SKIP coverage (tail_duplication pass completed) (DONE).",
  "Task 20: Empty-block collapse adversarial twins (DONE).",
  "Task 21: A36 atomic minimization single-writer variant implemented (DONE).",
  "Task 22: A33 algebraic expansion for literal RHS implemented (DONE).",
  "Task 23: A33 sign-preserving distribution for subtraction implemented (DONE).",
  "Task 24: A35 stronger range fold for modulo with single-block lookbehind implemented (DONE).",
  "Task 25: G2 reciprocal constant-folding pass arm implemented (DONE).",
  "Task 26: G4 Horner rewrite (degree-2 only) implemented (DONE).",
  "Task 27: A14 register-pressure stub (cost-model only) implemented (DONE).",
  "Task 28: H6 Welford sum-of-squares primitive implemented (DONE)."
]
proof_required = [
  "Tests pass after each task."
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit"]
scope = [
  "ROADMAP S17 literal-eval slice: remove the duplicate const-eval implementation from `vyre-lower` and route descriptor constant folding through `vyre-foundation::ir::eval`.",
  "Extend the shared evaluator for bool BitXor so lower, foundation optimizer, and wgpu emit share the same behavior instead of carrying per-layer exceptions.",
]
proof_required = [
  "`vyre-lower` descriptor const-fold imports `fold_binary_literal`, `fold_unary_literal`, `fold_cast_literal`, and `fold_fma_literal` from foundation instead of owning separate match tables.",
  "`vyre-foundation` proves bool BitXor folds through the shared evaluator.",
  "Targeted const-fold tests pass in both foundation and lower.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation const_fold --lib` -> 122 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower const_fold --lib` -> 42 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["op_matrix", "libs_parsing", "libs_test_support"]
scope = [
  "Close the composition discipline gate failure without weakening the LEGO contract.",
  "Make structural fingerprints include child-composition bodies, buffer roles, loop bounds, and cast targets so wrappers around the same primitive are distinguished by real parameters instead of collapsing on generator name only.",
  "Factor standalone C sema phase bodies under explicit child regions so the phase ops stay under the control-flow depth budget while preserving the full parent `c_sema_scope` composition.",
]
proof_required = [
  "`composition_discipline` passes every gate, including complexity and no-reimplementation.",
  "C sema phase programs still build through `vyre-libs`.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-conform-enforce --test composition_discipline -- --nocapture` -> 4 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-libs c_sema_scope --lib` -> compiled and ran the filtered lib harness successfully.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "runtime_cache", "lower_emit"]
scope = [
  "Close the current code-backed roadmap sync for O28-O34.",
  "Modularize the driver diagnostics public module without changing its export surface.",
  "Remove a runtime cache trait recursion footgun from PipelineCacheStore.",
  "Tighten lowered cleanup/analysis hot paths by replacing clone-heavy or ordered containers with direct index/hash membership structures.",
  "Fix texture-promotion child traversal so loop bound operands are not treated as child-body indices.",
]
proof_required = [
  "ROADMAP.md O28-O34 reflects only code that landed.",
  "The diagnostics split preserves the same public `vyre_driver::diagnostics::{Diagnostic, DiagnosticCode, OpLocation, Severity}` surface.",
  "Pipeline cache stores must implement `get`; zero-copy stores may still override `get_arc`.",
  "Lowered cleanup and promotion tests pass after the container/traversal changes.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver diagnostics --lib` -> 8 passed.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --lib` -> passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower descriptor_const_fold --lib` -> 42 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower drop_unused_literals --lib` -> 7 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower drop_unused_child_bodies --lib` -> 7 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower descriptor_dce --lib` -> 8 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower --lib` -> 434 passed after the hash-membership and promotion-analysis batch.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-lower texture_promote --lib` -> 9 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "roadmap"]
scope = [
  "Import the O-01..O-20 fast-lane optimization bundle into `docs/optimization/ROADMAP.md` so optimization work is claimed in the canonical roadmap instead of scattered internals.",
  "Close O5's residual eviction cost by replacing shape-fact cache `Vec::remove(0)` with `VecDeque::pop_front()` and proving FIFO eviction behavior.",
]
proof_required = [
  "Roadmap contains the imported O-bundle rows.",
  "Shape-fact cache eviction remains bounded at 64 entries and evicts FIFO without an O(n) front-shift.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation program_shape_facts --lib` -> 9 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "compile_hot_path"]
scope = [
  "Remove the wgpu pipeline metadata clone of every prepared `BufferDecl` during compile; bind metadata now iterates borrowed prepared declarations and chains the optional trap sidecar by reference.",
]
proof_required = [
  "Wgpu lib compiles with the borrowed declaration iterator.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo check -p vyre-driver-wgpu --lib` -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "scheduler_hot_path"]
scope = [
  "Close O21: scheduler metrics no longer re-walk the whole IR for heap-allocation estimates on every pass row when the Program state is unchanged.",
  "Cache the `IrAllocationEstimate` for the current Program and invalidate it only after a landed rewrite; reverted/unchanged passes reuse the pre-pass estimate.",
]
proof_required = [
  "Scheduler metric tests still prove runtime/IR-size reporting and cost-gate behavior.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation scheduler --lib` -> 17 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "grid_sync_hot_path"]
scope = [
  "Close O22: grid-sync split fallback keeps the intermediate output vector allocation alive across segment dispatches.",
  "`refresh_readwrite_inputs` now consumes readwrite bytes in place and clears the caller-owned output buffer instead of taking/dropping the whole Vec.",
]
proof_required = [
  "Shared driver grid-sync tests pass.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver grid_sync --lib` -> 7 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "binding_hot_path"]
scope = [
  "Close O23: shared `BindingPlan` descriptors use `Arc<str>` names and clone the existing `BufferDecl` name allocation instead of allocating a new `String` per binding.",
]
proof_required = [
  "Shared driver tests pass after the public binding descriptor type change.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver --lib` -> 138 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "driver_wgpu", "program_walks"]
scope = [
  "Close O24: indirect dispatch metadata stores `count_buffer` as `Ident` rather than allocating a `String`, and output binding layouts clone the existing `BufferDecl` name `Arc<str>`.",
  "Update wgpu indirect-dispatch users to compare against `count_buffer.as_str()`.",
]
proof_required = [
  "Wgpu lib compiles after the shared metadata type change.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo check -p vyre-driver-wgpu --lib` -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["roadmap", "foundation_optimizer", "cse_hot_path"]
scope = [
  "Expand the half-migration audit into explicit HM1-HM11 roadmap rows under S12-S20 so forward migration closure cannot be hand-waved as generic cleanup.",
  "Close O18 by changing CSE scoped side-effect invalidation from whole-table drain/reinsert to epoch-based visibility barriers.",
  "Keep CSE scope exit restoration tied to the undo log for actual map mutations only.",
]
proof_required = [
  "Roadmap contains executable half-migration rows.",
  "CSE still eliminates safe common subexpressions while preserving side-effect ordering inside scoped branches.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation cse --lib` -> 52 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_serial", "wire_hot_path"]
scope = [
  "Close O25: VIR0 wire encode no longer allocates an intermediate `OutputSet(Vec<u32>)` for every serialization.",
  "Encode canonical writable-buffer indices directly from the buffer table into the body scratch buffer.",
]
proof_required = [
  "Direct buffer-table output-set encoding matches the materialized `OutputSet` bytes.",
  "Existing output-set wire tests still pass.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation output_set --lib` -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "cse_hot_path"]
scope = [
  "Close O26: CSE call-argument rewrites allocate the final argument capacity once when the first owned arg appears.",
  "Avoid the old `args[..index].to_vec()` prefix allocation that could immediately grow when pushing the changed suffix.",
]
proof_required = [
  "CSE still rewrites repeated expressions inside effectful Call arguments correctly.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation cse --lib` -> 53 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "pipeline_hot_path"]
scope = [
  "Close O27: add `compile_owned` and `compile_owned_with_telemetry` so owned Programs enter reusable pipeline compilation without an IR clone.",
  "Re-export the owned compile entry points from `vyre-driver::pipeline` and the crate root.",
]
proof_required = [
  "Owned compile routes through the passthrough pipeline correctly.",
  "Owned telemetry compile returns a reusable pipeline.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-driver pipeline --lib` -> 29 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "half_migration"]
scope = [
  "Close HM8: move optimizer scheduler and fact-substrate tests from orphan root files into their declared module directories.",
  "Remove `#[path = ...]` test-module wiring from `scheduler/mod.rs` and `fact_substrate.rs` so test ownership follows the production module tree.",
]
proof_required = [
  "Scheduler and fact-substrate tests remain compiled through normal module discovery.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation scheduler --lib` -> 17 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -p vyre-foundation fact_substrate --lib` -> 9 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["build", "half_migration"]
scope = [
  "Advance HM9: remove dead `vyre-libs` feature flags for the already-removed `hardware` and `composite` module surfaces.",
  "Leave wgpu test-domain feature flags open because they still gate real tests and require a separate boundary migration, not deletion.",
]
proof_required = [
  "vyre-libs still compiles without the dead feature declarations.",
]
proof = [
  "`CARGO_BUILD_JOBS=1 cargo check -p vyre-libs --lib` -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit", "half_migration"]
scope = [
  "Close HM6/S17: rename lowered KernelDescriptor cleanup passes away from ambiguous Program-IR optimizer names.",
  "Use descriptor_const_fold, descriptor_cse, and descriptor_dce as the only public lowered-cleanup symbols; leave no compatibility re-exports for const_fold/cse/dce in vyre-lower rewrites.",
]
proof_required = [
  "Lower rewrite tests pass after the public symbol rename.",
  "Source scan finds no bare lowered const_fold/cse/dce public symbols under vyre-lower rewrites.",
]
proof = [
  "CARGO_BUILD_JOBS=1 cargo test -p vyre-lower rewrites --lib -> 176 passed.",
  "rg -n '\\b(const_fold|cse|dce)\\b' vyre-lower/src/rewrites vyre-bench/tests/full_pipeline_snapshot.rs -> no matches after descriptor-prefix rename.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["primitives", "libs", "bench_harness", "half_migration"]
scope = [
  "Close HM7/HM11: add a source-backed gate for audited primitive/lib/bench/optimizer module shells.",
  "Require each audited shell to document its contract and resolve declared child modules to real implementation files.",
  "Add missing module contracts for vyre-libs test_support and vyre-bench evolve surfaces found by the new gate.",
]
proof_required = [
  "The module-surface gate passes.",
]
proof = [
  "CARGO_BUILD_JOBS=1 cargo test -p vyre-conform-enforce --test module_surface_truth -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["build", "driver_wgpu", "half_migration"]
scope = [
  "Close HM9: make wgpu test-domain feature selectors real dependency-feature wiring instead of empty flags.",
  "Keep parity-testing as a real wgpu-local probe feature because it gates production parity probe code, not a removed domain surface.",
]
proof_required = [
  "Cargo accepts all wgpu test-domain feature selectors after wiring them to vyre-libs features.",
]
proof = [
  "cargo metadata --manifest-path vyre-driver-wgpu/Cargo.toml --no-deps --format-version 1 --features c-parser,math-linalg,math-scan,matching-substring,matching-dfa,matching-nfa -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["reference", "half_migration"]
scope = [
  "Advance HM2: implement concrete async byte-copy transfers in the old workgroup reference evaluator.",
  "Support AsyncStore in eval_node and make AsyncWait apply pending transfers instead of only clearing a tag.",
  "Hide pending async transfer state behind crate visibility so public Invocation fields do not expose internal scheduler representation.",
]
proof_required = [
  "Reference async load/store tests pass in the old workgroup evaluator.",
]
proof = [
  "CARGO_BUILD_JOBS=1 cargo test -p vyre-reference async_ --lib -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda", "launch_hot_path"]
scope = [
  "Close ROADMAP.md O36 by removing per-fixpoint-iteration CUDA module-cache lookup and kernel-argument reconstruction from host and resident dispatch.",
]
proof_required = [
  "Host and resident CUDA fixpoint loops must resolve CUfunction and kernel args once before the iteration loop.",
  "CUDA launch helper tests and cuda lib check must pass.",
]
proof = [
  "vyre-driver-cuda/src/backend/host_dispatch.rs and resident_dispatch.rs call resolve_launch_function() and kernel_args() before the fixpoint loop, then replay launch_resolved_function().",
  "vyre-driver-cuda/src/backend/launch.rs removed the now-unused LaunchRequest/launch_module wrapper and exposes the resolved-launch primitive used by both paths.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda kernel_args --lib -> pass.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --lib -> pass (existing warning comes from concurrent Program::buffers_arc test-only work outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "compile_hot_path"]
scope = [
  "Close ROADMAP.md O37 by removing full BufferDecl clones from the Naga Program emitter binding map.",
]
proof_required = [
  "Naga program emitter stores only the buffer facts it reads after declaration.",
  "vyre-emit-naga and wgpu driver checks pass.",
]
proof = [
  "`vyre-emit-naga/src/program/types.rs::BufferBinding` now stores `element`, `count`, `kind`, and `global` instead of `decl: BufferDecl`.",
  "`ModuleBuilder::add_buffer` no longer clones full `BufferDecl`s into the emitter map; load, buf_len, store, type inference, and atomic lowering read the lean fields.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --lib` -> pass.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "compile_hot_path"]
scope = [
  "Close ROADMAP.md O11 by making empty sharded megakernel templates shared Arc<Program> values through bootstrap compile.",
]
proof_required = [
  "Empty-opcode sharded megakernel builders must expose a shared Program path and bootstrap must compile that shared Program without clone/re-Arc churn.",
  "Runtime targeted test and lib check must pass.",
]
proof = [
  "`build_program_sharded_slots_shared` returns the cached `Arc<Program>` for empty opcode sets.",
  "`Megakernel::bootstrap_sharded` calls `compile_bootstrap_shared` and passes the cached Program Arc directly into `vyre_driver::pipeline::compile_shared`.",
  "`empty_sharded_shared_builder_reuses_cached_program_arc` asserts pointer identity across repeated empty-template builds.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime empty_sharded_shared_builder_reuses_cached_program_arc --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --lib` -> pass (existing warnings are outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "protocol_hot_path"]
scope = [
  "Close ROADMAP.md O10 by wiring megakernel resident protocol buffer construction through allocation-reuse APIs.",
]
proof_required = [
  "Control/ring/debug/io resident mirrors must reset into existing Vec storage.",
  "IO queue protocol must expose an encode-into API matching the existing control/ring/debug encode-into surface.",
  "Targeted runtime tests and runtime lib check must pass.",
]
proof = [
  "`MegakernelResidentBuffers::reset` encodes control/ring/debug/io bytes into the existing resident mirror buffers and `new` goes through the same path.",
  "`vyre-runtime/src/megakernel/io/encode.rs` adds `try_encode_empty_io_queue_into` and factors strict byte sizing through one helper.",
  "`resident_buffers_reset_reuses_encoded_storage` asserts all four resident byte buffers retain pointer identity across reset.",
  "`empty_io_queue_encode_into_reuses_capacity` asserts IO queue encode-into zeroes bytes without reallocating when capacity is sufficient.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime empty_io_queue_encode_into_reuses_capacity --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime resident_buffers_reset_reuses_encoded_storage --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --lib` -> pass (existing warnings are outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "pipeline_hot_path"]
scope = [
  "Close ROADMAP.md O7 by keeping WGSL pipeline disk-cache lookup keys as raw hash bytes until path/log rendering.",
]
proof_required = [
  "WGSL cache lookup and metadata matching must not hex-encode then parse the cache key back to bytes.",
  "Targeted wgpu disk-cache tests and wgpu lib check must pass.",
]
proof = [
  "`wgsl_cache_key` now returns `[u8; 32]`; `load_or_compile_disk_wgsl`, `persist_disk_wgsl`, and `wgsl_metadata_matches` carry raw key bytes.",
  "Hex allocation is limited to `cache_entry_path` / tracing boundaries through `hex_hash(&cache_key)`.",
  "Removed the hot-path `cache_key_bytes` parser previously used to turn cache-key strings back into metadata bytes.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu pipeline::disk_cache::tests::cache_key_isolates_wire_from_adapter --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu pipeline::disk_cache::tests::content_digest_rejects_corrupted_payload --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --lib` -> pass (existing warnings are outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "buffer_pool_hot_path"]
scope = [
  "Close ROADMAP.md O6 by removing the extra Arc layer around wgpu buffer-pool shard queues.",
]
proof_required = [
  "Transient buffer acquire/release must access the sharded queue directly without cloning `Arc<SegQueue<_>>` per operation.",
  "Targeted buffer-pool test and wgpu lib check must pass.",
]
proof = [
  "`BufferPoolInner::queues` is now `DashMap<BufferKey, SegQueue<CachedBuffer>, _>`.",
  "`BufferPool::acquire` calls `pop_cached`; `PooledBuffer::drop` calls `push_cached`, so the hot path avoids per-operation Arc refcount traffic.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu runtime::cache::buffer_pool::tests::size_class_collapses_neighbors_into_one_bucket --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --lib` -> pass (existing warnings are outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "telemetry_hot_path"]
scope = [
  "Close ROADMAP.md O12 by replacing order-free megakernel window-opcode membership scans with reusable hash scratch.",
]
proof_required = [
  "Ring telemetry decode must not linearly scan every selected window opcode for every slot.",
  "Targeted telemetry test and runtime lib check must pass.",
]
proof = [
  "`TelemetryDecodeScratch` now owns `window_opcodes: FxHashSet<u32>` and rebuilds it once per decode call.",
  "`RingTelemetry::decode_with_window_opcodes_into` checks `scratch.window_opcodes.contains(&opcode)` inside the slot loop.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime decode_window_opcodes_groups_ticketed_slots --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --lib` -> pass (existing warnings are outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_serial", "wire_hot_path"]
scope = [
  "Close ROADMAP.md O3 by moving wire-encode section scratch out of per-serialization allocation and into reusable thread-local buffers.",
]
proof_required = [
  "Repeated wire serialization must reuse body/node/region scratch buffers instead of allocating new section scratch vectors per call.",
  "Targeted wire tests and foundation lib check must pass.",
]
proof = [
  "`to_wire_with_buffer_order_into` uses top-level `BODY_SCRATCH`; node payloads use `NODE_PAYLOAD_SCRATCH`; memory-region shape/hints payloads use `REGION_PAYLOAD_SCRATCH`.",
  "`encode_section_helpers_reuse_caller_scratch` asserts node payload, shape payload, and hints payload backing pointers/capacities are retained across repeated section encodes.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation to_wire_into_appends_byte_for_byte --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation encode_section_helpers_reuse_caller_scratch --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-foundation --lib` -> pass (existing warning is outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "dce_hot_path", "driver_wgpu"]
scope = [
  "Close ROADMAP.md O14/O16/O19 by removing DCE survivor clone/rebuild churn, proving canonicalize uses cached identifier hashes, and proving wgpu caps come from live created-device limits.",
]
proof_required = [
  "DCE must move surviving owned nodes through the single backward liveness sweep instead of cloning/rebuilding them through helper constructors.",
  "Canonicalize sort-key tests must prove name-bearing expressions use `Ident::cached_hash()`.",
  "wgpu device-profile tests must prove optimizer caps inherit enabled/live limits rather than floor-only defaults.",
  "Targeted tests and foundation/wgpu lib checks must pass.",
]
proof = [
  "`eliminate_dead_lets` now pushes owned `Node::{Let,Assign,Store,If,Loop,Block,Barrier,AsyncLoad,AsyncStore,AsyncWait,Trap,Resume,Opaque}` values after collecting liveness, avoiding helper-constructor clones in the DCE hot sweep.",
  "`expr_sort_key_uses_cached_ident_hash_for_name_bearing_exprs` asserts Var/Load/BufLen sort keys are derived from the precomputed `Ident` hash.",
  "`backend_profile_uses_created_device_limits_and_enabled_features` asserts wgpu `DeviceProfile` and `AdapterCaps` use `EnabledFeatures::max_workgroup_size` and live `wgpu::Limits` fields.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation 'optimizer::passes::fusion_cse::dce' --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation expr_sort_key_uses_cached_ident_hash_for_name_bearing_exprs --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu backend_profile_uses_created_device_limits_and_enabled_features --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-foundation --lib` -> pass (existing warning is outside this slice).",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --lib` -> pass (existing warning is outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda", "dispatch_hot_path"]
scope = [
  "Close ROADMAP.md O8 by making host-borrowed CUDA multi-input dispatch build and enqueue one explicit async upload batch before kernel launch.",
]
proof_required = [
  "Host input uploads and launch params must be collected into one contiguous H2D descriptor batch.",
  "The dispatch path must enqueue the batch on the launch stream without synchronizing until the final event/readback path.",
  "Targeted CUDA test and CUDA lib check must pass.",
]
proof = [
  "`dispatch_borrowed_async_with_ptx_concrete` now pre-reserves host transfer storage, collects `HostUpload { dst, src, byte_len }` descriptors for every input plus params, and calls `enqueue_host_uploads_async` once before timing/launch.",
  "`enqueue_host_uploads_async` emits the queued `cuMemcpyHtoDAsync_v2` calls on the same CUDA stream and performs no stream synchronization.",
  "`host_upload_batch_capacity_counts_inputs_once_plus_params` asserts the batch capacity is exactly host inputs + params and pinned transfer storage reserves inputs + params + outputs.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda host_upload_batch_capacity_counts_inputs_once_plus_params --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --lib` -> pass (existing warning is outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "fact_substrate"]
scope = [
  "Continue ROADMAP.md H4d by removing autotune's private recursive buffer-reference walker and routing it through `ProgramFacts`.",
]
proof_required = [
  "Autotune referenced-buffer discovery must use the shared columnar fact substrate.",
  "Async copy source/destination buffer refs must be visible to autotune through that substrate.",
  "Targeted autotune tests and foundation lib check must pass.",
]
proof = [
  "`referenced_storage_buffers` now builds `ProgramFacts` and reads `buffer_refs()` instead of recursively matching every Node and Expr variant.",
  "`referenced_buffers_come_from_program_facts_async_edges` asserts `Node::AsyncLoad` source and destination are both discovered through ProgramFacts.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation referenced_buffers_come_from_program_facts_async_edges --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation 'optimizer::passes::specialization::autotune' --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-foundation --lib` -> pass (existing warning is outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_wgpu", "emit_hot_path"]
scope = [
  "Close ROADMAP.md B2 by adding a module-level Naga IR cache keyed by canonical KernelDescriptor hash.",
]
proof_required = [
  "Repeated identical `KernelDescriptor` emission must reuse a cached `naga::Module` instead of rebuilding the module arenas.",
  "Cache lookup must guard against hash collision by comparing the descriptor before returning a module.",
  "Targeted emit test and emit crate check must pass.",
]
proof = [
  "`vyre-emit-naga::emit` now hashes the full `KernelDescriptor`, checks a 64-entry process-local module cache, and returns a cloned cached `naga::Module` on descriptor equality.",
  "Cache misses emit through `emit_uncached` and insert the descriptor/module pair; eviction uses FIFO `VecDeque` without O(n) front removal.",
  "`identical_descriptor_reuses_module_cache` asserts the second identical emit records a cache hit.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga identical_descriptor_reuses_module_cache --lib` -> pass.",
  "`CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --lib` -> pass (existing warning is outside this slice).",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["coordination", "lower_emit", "driver_cuda", "runtime_megakernel", "primitives_graph"]
scope = [
  "Sync ROADMAP.md with the latest committed implementation slices before Claude starts adjacent work.",
  "Mark the completed PTX vector load/store fusion and AsyncStore correctness fallback in O38.",
  "Mark lowered nested-child loop unroll in O39.",
  "Mark the region-scoped ScratchReusePlan runtime/foundation integration in A13/C2.",
  "Mark the d-DNNF GPU-shaped evaluator in P6 and the shipped H1/H2/H3/H5 algorithm integrations.",
]
proof_required = [
  "Roadmap rows must only mark code-backed work that exists in committed files.",
  "Claims must avoid the dirty CI/lint/xtask files currently owned by another worker.",
]
proof = [
  "`git show --name-only 0a307d403e f1eea75ba6 8ecb62cd9f 71c9cd415f 6cafb7086f` maps the roadmap sync to committed source files in `vyre-emit-ptx`, `vyre-lower`, `vyre-foundation`, and `vyre-primitives`.",
  "`docs/optimization/ROADMAP.md` now has explicit O38/O39 rows plus updated A13/B14/C2/H1/H2/H3/H5/P6 entries for the completed slices.",
  "No edits were made to `.github/`, `vyre-lints/`, `xtask/`, or scripts currently dirty in the worktree.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["op_matrix", "lego_quality_gates"]
scope = [
  "ROADMAP S6/S8 quality-gate substrate: upgrade the lego-block enforcement so the audit becomes unnecessary, not a recurring sweep.",
  "U1  -  fast pre-commit gate (`cargo xtask lego-quick`) over the staged diff so writers learn duplication BEFORE the commit lands.",
  "U2  -  pre-write similarity query (`cargo xtask whats-similar --op-id <id>`) so writers can ask 'should I reuse?' before shipping.",
  "U3  -  three new checks bringing lego-audit from 7 to 10: composability (islands), name-stem collision, operand-shape duplicate (catches false negatives of bigram cosine).",
  "U4  -  allowlist drift sentinel: fail when any vyre-lints/allowlist.toml entry sits older than 14 days.",
  "U5  -  wire lego-audit, vyre-lints raw-IR scan, and drift sentinel into the merge gate (architectural-invariants.yml) and add a verifier script that asserts the workflow declares every required job.",
]
proof_required = [
  "Pre-commit hook installs cleanly and exits ≤2s on a 10-file diff with no findings.",
  "whats-similar resolves a registered op id, fingerprints it against every other registered op, and returns top-N nearest matches with a verdict tier.",
  "lego-audit reports [N/10] in headers and runs three additional checks (composability/name-stem/operand-shape) on the full op set.",
  "drift sentinel returns 0 stale entries against the current allowlist (which is empty); over-budget entries flag with date math that does not depend on chrono.",
  "architectural-invariants.yml declares vyre-lints-raw-ir, vyre-lints-allowlist-drift, and lego-audit jobs; CI_REQUIRED.md lists them; check_required_ci_jobs.sh verifies the wiring exit-0.",
]
proof = [
  "`xtask/src/lego_quick.rs` (350 LOC, 4 tests) ships the pre-commit module; `scripts/install_lego_quick_hook.sh` installs the hook.",
  "`xtask/src/whats_similar.rs` (250 LOC, 7 tests) ships the similarity query; `lego_audit::{collect_ops, structural_similarity, OpInfo, Tier}` made pub(crate) for reuse.",
  "`xtask/src/lego_audit.rs` adds check_8_composability + check_9_name_stem_collision + check_10_operand_shape_duplicate; header text updated to [N/10]; +1 leaf-stem unit test.",
  "`vyre-lints/src/drift.rs` (350 LOC, 8 tests) ships the drift sentinel with injectable AgeResolver; `vyre-lints` binary gains `--check-drift`/`--drift-budget-days`/`--today` flags.",
  "`.github/workflows/architectural-invariants.yml` adds vyre-lints-raw-ir + vyre-lints-allowlist-drift + lego-audit jobs; `.github/CI_REQUIRED.md` lists them; `scripts/check_required_ci_jobs.sh` exits 0.",
  "`cargo test -p vyre-lints --lib --tests` -> 37 passed (24 raw_ir + 13 drift, +allowlist).",
  "`cargo test -p xtask --tests` -> 29 passed (lego_quick + whats_similar + lego_audit module tests).",
  "`cargo check -p xtask --tests` and `cargo check -p vyre-lints --tests` both -> pass.",
  "`cargo run --bin xtask -- lego-quick` smoke -> exit 0, 'no staged Rust files; nothing to check.'",
]
notes = [
  "User concern that triggered this work: 'the ops are increasingly becoming lower quality without anyone auditing. CPUs had time forcing the audit; we don't have that luxury.' These five upgrades shift quality from human-audit-discipline (fragile) to lint-enforced gates (durable).",
  "GitHub branch-protection rule update is one piece I can't ship from code  -  the new required-check names need to be added on the GitHub side. CI_REQUIRED.md and the verifier script both name them so when the branch-protection rule is updated it stays in sync.",
  "Replaces and supersedes the originally-planned op-surface audit (#207) and remediation sweep (#208). The whole point of the upgrade is that those manual sweeps shouldn't be needed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["libs_math", "algorithm_rewrite"]
scope = [
  "Close ROADMAP.md H2 remaining composition: FFT convolution wrapper on top of the shipped radix-2 FFT substrate.",
  "Owned files: vyre-libs/src/math/fft/* and docs/optimization/ROADMAP.md claim/status rows for H2 only.",
]
proof_required = [
  "Wrapper must compose existing FFT/IFFT/multiply pieces instead of duplicating low-level butterfly logic.",
  "Tests must prove convolution parity against a scalar reference and reject unsupported shapes loudly.",
]
proof = [
  "`vyre-libs/src/math/fft/convolution.rs::fft_convolve_circular_complex` composes `fft_radix2_complex` twice, pointwise complex multiply, then inverse FFT through `conj(FFT(conj(x))) / n`.",
  "Scratch names are explicit workgroup buffers in the Program buffer table, so they are validated/planned without extra readback or caller-side global scratch outputs.",
  "`fft_convolve_circular_real_fixture_matches_reference` and `fft_convolve_circular_complex_fixture_matches_reference` prove parity against scalar circular-convolution reference.",
  "`fft_convolve_rejects_non_power_of_two` and `fft_convolve_rejects_aliasing_buffers` prove boundary rejection.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-libs math::fft --lib` -> 15 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-harness tolerance --lib` -> 2 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer"]
scope = [
  "Close ROADMAP.md A8: per-rule-family saturation budgets so algebraic families don't share a global iteration cap with structural rewrite families.",
]
proof_required = [
  "saturate_per_family must accept &[&dyn Family<L>] + Fn(&str) -> usize and return a per-family iters_used / budget report.",
  "Zero budget must skip a family without running rules.",
  "iters_used must never exceed budget.",
]
proof = [
  "vyre-foundation/src/optimizer/eqsat.rs adds FamilySaturationReport and saturate_per_family.",
  "4 new tests: saturate_per_family_skips_zero_budget, saturate_per_family_runs_each_family_independently, saturate_per_family_empty_input_returns_empty, saturate_per_family_reports_iters_used_le_budget.",
  "cargo test -p vyre-foundation --lib eqsat -> 27 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["roadmap_reconciliation"]
scope = [
  "Reconcile ROADMAP.md against shipped state: rows whose row text already documented shipped substrate but lacked the `DONE 2026-05-02:` prefix now carry it. No code changes  -  substrate already shipped in earlier session work or in live HEAD; this claim closes the documentation gap so Codex and main-cc share one ground-truth view of what is actually open.",
]
proof_required = [
  "Each flipped row's substrate already exists in code at HEAD with passing tests.",
  "No row in a Codex-active write-set was flipped without explicit confirmation in the row text that the substrate is shipped.",
]
proof = [
  "Flipped 28 A-series rows: A1, A2, A3, A4, A5, A10, A11, A12, A15, A16, A17, A18, A19, A20, A21, A22, A23, A24, A25, A26, A27, A28, A29, A30, A31, A32, A34, A35.",
  "Flipped 6 measurement rows: M0, M1, M2, M3, M4 (CPU half), M5.",
  "Flipped 3 specialization rows: F1 (substrate), F2, F4.",
  "Flipped 4 numerical rows: G1 (foundation half), G3, G5 (foundation half), G7 (libs side).",
  "Flipped 2 algorithm-level rows: H1 (libs side), H4 (online-softmax row variant).",
  "Flipped 3 validation rows: K1, K2, K3.",
  "Flipped 4 parsing rows: L1, L2 (in-memory), L3, L4.",
  "Flipped 1 PGO row: I1 (foundation-side hot-path hints).",
  "Where only one half of a multi-lane row is shipped, the DONE marker explicitly names the shipped half and the lane that owns the remaining half.",
]
notes = [
  "User direction 2026-05-02: 'roadmap sadly isnt fully accurate all agents dont always log what they finish'  -  reconciliation gives Codex and main-cc a single ground-truth view.",
  "Truly-open items in CC's lane after this reconciliation: A6 (TOML rule database  -  needs pattern DSL design), A7 (caller wiring), A9 (per-rule device-fact metadata), I3 (autotune persistence half), foundation_optimizer half of H1/H5 substitution passes. Items in Codex's lanes (B/C/D/E/J series + lower_emit + driver_*) untouched.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "coalescing"]
scope = [
  "Close ROADMAP.md B14 warning consumer path: convert coalescence classifications into structured rewrite recommendations.",
]
proof_required = [
  "Warnings must remain backend-neutral and carry enough information for emitters/CI/bench gates to act.",
  "Tests must prove strided, scattered, and broadcast-store cases map to distinct rewrite classes.",
]
proof = [
  "`CoalescenceReport::warnings()` returns `CoalescenceWarning` records with stable warning codes and `CoalescenceRewrite` classes.",
  "`warnings_map_strided_and_scattered_to_rewrites` proves strided and scattered sites map to vector-pack/tile and shared-memory-gather recommendations.",
  "`warnings_include_broadcast_store_but_not_broadcast_load` proves broadcast writes are flagged without penalizing broadcast loads.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower coalesce --lib` -> 30 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "branch_uniformity"]
scope = [
  "Close ROADMAP.md B15 emit-hint consumer path: convert workgroup-uniform branch analysis into structured emitter hints.",
]
proof_required = [
  "Hints must remain backend-neutral and carry stable codes for emitters/CI/bench gates.",
  "Tests must prove Uniform, Divergent, and Unknown classifications map to distinct emit hint classes.",
]
proof = [
  "`WorkgroupUniformReport::hints()` returns `BranchHint` records with stable codes and `BranchEmitHint` classes.",
  "`hints_map_every_uniformity_class` proves Uniform -> UniformControlFlow, Divergent -> DivergentControlFlow, Unknown -> NeedsProfile.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower workgroup_uniform --lib` -> 11 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "descriptor_naga"]
scope = [
  "Close the HM5 descriptor-emitter sub-slice for Trap/Resume and opaque wide-literal payloads without reintroducing driver-local Naga logic.",
]
proof_required = [
  "Trap lowering must insert an explicit descriptor sidecar binding and Naga emission must write it through atomics.",
  "Resume must be a runtime marker in descriptor emission, not an UnsupportedOp wall.",
  "Opaque expression lowering must preserve kind + payload so known wide literals emit from descriptor data.",
]
proof = [
  "`vyre-lower` now inserts `TRAP_SIDECAR_NAME` / `TRAP_SIDECAR_WORDS` only for trap-containing descriptors and preserves opaque expression kind/payload.",
  "`vyre-emit-naga` emits descriptor Trap sidecar compare-exchange + sidecar atomic writes, treats Resume as a runtime marker, and emits `vyre.literal.{u64,i64,f64}` from descriptor payload bytes.",
  "`vyre-emit-naga` now emits descriptor atomic compare-exchange instead of treating it as unsupported.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower lower_ --lib` -> 5 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga --lib` -> 82 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "descriptor_naga", "atomics"]
scope = [
  "Close the HM5 FetchNand descriptor-emitter wall by emitting a concrete compare-exchange retry loop.",
]
proof_required = [
  "FetchNand must not return an unsupported/invalid descriptor error in the Naga descriptor path.",
  "The generated Naga body must contain a loop and atomics so the operation is actually implemented, not silently dropped.",
]
proof = [
  "`emit_fetch_nand_atomic` emits an initial atomic read, computes `!(old & value)`, retries through atomic compare-exchange, and binds the old value when the descriptor op has a result id.",
  "`atomic_fetch_nand_emits_compare_exchange_loop` proves the emitted body contains both a loop and atomic statements.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga atomic_fetch_nand_emits_compare_exchange_loop --lib` -> 1 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga --lib` -> 82 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "lower_emit", "descriptor_naga"]
scope = [
  "Close ROADMAP.md HM5: remove wgpu compile-path Program-emitter fallback and derive binding metadata from the same KernelDescriptor used for WGSL emission.",
]
proof_required = [
  "WgpuProgram::from_program must call vyre-lower plus descriptor vyre-emit-naga, not `vyre_emit_naga::program::emit_prepared_module_with_features`.",
  "Pipeline bind-group metadata must be derived from KernelDescriptor slots so reflection and bind groups share one source.",
  "The WGSL disk-cache contract must change when the lowering path changes.",
]
proof = [
  "`vyre-driver-wgpu/src/emit/mod.rs` now validates/analyzes the KernelDescriptor, applies the workgroup override to it, and calls `vyre_emit_naga::emit_optimized`.",
  "`vyre-driver-wgpu/src/pipeline.rs` derives `BufferBindingInfo` and trap tags from the KernelDescriptor and no longer calls `prepared_program`, `trap_sidecar_decl`, or Program-level Naga emission for compile metadata.",
  "`vyre-driver-wgpu/src/pipeline/disk_cache.rs` bumps `WGSL_LOWERING_CONTRACT` to `v3:kernel-descriptor-naga`.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu emit::tests --lib` -> 2 passed.",
  "`CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu pipeline:: --lib` -> 24 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "foundation_optimizer"]
scope = [
  "Close ROADMAP.md A7: device-profile-aware extraction cost helper so each consumer Family stops rolling its own per-op cost table.",
]
proof_required = [
  "device_aware_cost(profile, hot, base_cost_fn, hint_lookup) returns an Fn(&L) -> u64 closure.",
  "Hot-path nodes pay HOT_PATH_COST_SCALE × base; cold-path pays COLD_PATH_COST_SCALE × base.",
  "FP16-eligible ALU on supports_tensor_cores + supports_f16 profiles pays an extra TENSOR_CORE_COST_SCALE multiplier; ignored otherwise.",
  "Multipliers clamp to [0.0, 4.0]; NaN/negative falls back to base.",
  "Output is deterministic across two profiles with identical capability bits.",
]
proof = [
  "vyre-driver/src/extraction_cost.rs ships device_aware_cost + NodeHints + scale_cost.",
  "lib.rs exposes the new module.",
  "7 tests cover cold inflate, hot shrink, tensor-core × FP16-eligible compounding, no-tensor-core ignores hint, multiplier clamp, NaN/negative fallback, deterministic-per-capability identity.",
  "cargo test -p vyre-driver --lib extraction_cost -> 7 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer"]
scope = [
  "Close ROADMAP.md A9: shared DeviceAwareRule<L, F> adapter so per-Rule device-fact gating stops being boilerplate.",
]
proof_required = [
  "DeviceAwareRule wraps a Box<dyn Rule<L>> plus a Fn() -> bool predicate.",
  "When predicate is true, matches() forwards to inner unchanged.",
  "When predicate is false, matches() short-circuits to empty.",
  "name() forwards to inner for telemetry.",
  "vyre-foundation must not import vyre-driver to keep the Tier-1 → Tier-2 direction.",
]
proof = [
  "vyre-foundation/src/optimizer/eqsat.rs adds DeviceAwareRule struct + Rule impl.",
  "3 new tests: predicate_true_forwards_matches, predicate_false_returns_empty, forwards_inner_name.",
  "cargo test -p vyre-foundation --lib eqsat -> 30 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["op_matrix", "lego_quality_gates"]
scope = [
  "Close ROADMAP.md S11: hot-path allocation/clone/lock/string scan as a CI-runnable xtask, with a TOML config naming the files that must stay free of per-call alloc/clone patterns.",
]
proof_required = [
  "cargo xtask hot-path-scan reads docs/optimization/HOT_PATHS.toml and reports findings line-oriented.",
  "Patterns covered: clone, to_owned, to_string, Vec/HashMap/BTreeMap/FxHashMap/String::new, String::from, Mutex/RwLock::new, format!.",
  "--strict mode exits 1 on any finding; default mode is informational.",
  "Skips comment lines and #[cfg(test)] attributes so dev-only allocations don't drown the report.",
]
proof = [
  "xtask/src/hot_path_scan.rs (~230 LOC, 6 tests) ships the scanner.",
  "docs/optimization/HOT_PATHS.toml seeded with 10 known hot-path files (pass scheduler, ProgramFacts SoA, validator skip cache, hot-path hints LRU, naga emit, F1 spec key, wire encode, A11 reaching-def, A26/A27 loop fusion+fission).",
  "xtask Cargo.toml gains serde derive + main.rs wires the subcommand.",
  "cargo test -p xtask --tests hot_path_scan -> 6 passed.",
]
notes = [
  "Smoke run via `cargo run --bin xtask` was blocked by an unrelated Codex-side compile error (vyre-emit-naga AtomicOp non-exhaustive match)  -  the unit tests still build through cargo test --tests because xtask test binaries don't pull the same dev-dep slice.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["libs_math"]
scope = [
  "Close ROADMAP.md S10 first split: vyre-libs/src/math/linalg/matmul_tiled.rs (960 LOC) split into responsibility modules.",
]
proof_required = [
  "matmul_tiled.rs replaced by matmul_tiled/{mod, shape, body, plain, bias}.rs.",
  "Each new file ≤ 500 LOC.",
  "Public surface (MatmulTiled, matmul_tiled, MatmulBiasTiled, matmul_bias_tiled) preserved exactly via mod.rs re-exports.",
  "Tests co-located with their source.",
  "vyre-libs lib check passes.",
]
proof = [
  "matmul_tiled/mod.rs: 31 LOC, matmul_tiled/shape.rs: 63 LOC, matmul_tiled/body.rs: 225 LOC, matmul_tiled/plain.rs: 353 LOC, matmul_tiled/bias.rs: 395 LOC.",
  "cargo check -p vyre-libs --lib --no-default-features --features math -> finished (1 pre-existing dead-code warning unrelated to this split).",
]
notes = [
  "Parser follow-up for classify.rs, ref_typedef.rs, and registry.rs closed in the S10 final parser split claim.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["libs_parsing"]
scope = [
  "Close ROADMAP.md S10 final parser split: split the remaining over-500-line C parsing files classify.rs, ref_typedef.rs, and registry.rs without changing public APIs.",
]
proof_required = [
  "vyre-libs/src/parsing/c/parse/vast/classify.rs delegates coherent node-emission chunks to submodules, and every classify file is ≤ 500 LOC.",
  "vyre-libs/src/parsing/c/parse/vast/ref_typedef.rs delegates annotation, identifier, declaration, scope, typed-kind, attribute, expression, and declaration-context responsibilities to submodules, and every ref_typedef file is ≤ 500 LOC.",
  "vyre-libs/src/parsing/c/sema/registry.rs delegates CPU reference and witness inventory code to submodules, and every registry file is ≤ 500 LOC.",
  "Public APIs remain at their original module paths.",
  "Targeted C parser checks compile and run.",
]
proof = [
  "classify.rs: 153 LOC; classify/nodes_00.rs..nodes_09.rs: 324/321/208/300/367/350/359/339/349/328 LOC.",
  "ref_typedef.rs: 50 LOC; ref_typedef/annotator.rs: 40, identifiers.rs: 52, declarations.rs: 175, scopes.rs: 184, typed_kind.rs: 359, asm_attributes.rs: 245, expressions.rs: 419, decl_context.rs: 218 LOC.",
  "registry.rs: 306 LOC; registry/reference.rs: 427; registry/witness.rs: 319 LOC.",
  "cargo fmt -p vyre-libs -> green.",
  "CARGO_BUILD_JOBS=1 cargo check -p vyre-libs --features c-parser -> green.",
  "CARGO_BUILD_JOBS=1 cargo test -p vyre-libs --features c-parser parsing::c -> 19 parser/sema unit tests passed.",
  "CARGO_BUILD_JOBS=1 cargo test -p vyre-libs --features c-parser --test c_reference_decode_contracts -> 3 passed.",
  "VAST/classifier-focused integration tests passed until pre-existing unrelated failures stopped the aggregate command; all classifier malformed-stream tests that ran passed.",
]
notes = [
  "The contributing-guide C parser command names stale test targets in this checkout (`c11_parser_integration`, `c11_build_vast_nodes`, `c_lower_ast_to_pg_nodes`, `c_lower_ast_to_pg_nodes_gpu_parity`, `c11_sema_scope` are not registered test targets).",
  "Aggregate targeted command failure 1: c11_keyword::keyword_pass_promotes_full_c11_keyword_table_entries asserts 77 but current table has 83; no files in that path changed in this split.",
  "Aggregate targeted command failure 2: c_parser_pipeline_malformed_stream_contracts::pg_lower_reference_oracle_panics_on_misaligned_input; no c/lower files changed in this split.",
  "universal_harness also reaches the split c_sema_scope and c11_classify_vast_node_kinds entries, but failed later on unrelated vyre-libs::math::conv::conv2d_3x3_direct output mismatch.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared"]
scope = [
  "Close ROADMAP.md F3 driver_shared half: dtype-specialized variants flow through the F1 SpecMap cache.",
]
proof_required = [
  "SpecValue::DType(DataType) variant exists and contributes a distinct cache_hash class.",
  "Each shipped DataType variant gets a unique non-fallback tag.",
  "SpecMap::cache_hash separates two maps that differ only in dtype.",
  "vyre-driver lib check passes.",
]
proof = [
  "vyre-driver/src/specialization.rs adds SpecValue::DType + dtype_tag helper + tag-4 cache_hash arm; SpecValue drops Copy, switches to Clone (DataType is non-Copy because of payload variants).",
  "iter() now yields &SpecValue (was *value deref-copy).",
  "as_pipeline_f64 and cache_hash now take &self.",
  "6 new tests: dtype_spec_value_round_trips, dtype_spec_distinct_dtypes_hash_distinct, dtype_spec_equal_dtypes_hash_equal, dtype_spec_does_not_collide_with_other_variants, dtype_spec_separates_cache_key_in_specmap, dtype_tag_covers_every_data_type (soundness gate that flags new DataType variants).",
  "cargo check -p vyre-driver --lib -> finished green.",
]
notes = [
  "Test binary build still blocked by the same Codex-side vyre-lower error blocking I3; lib check is the strongest signal available right now.",
  "Deriving Copy on SpecValue used to be the contract; that broke once DataType joined the value space. Internal callers that took SpecValue by value should still work (the methods still consume by reference now), but any external consumer pattern-matching on `(name, value)` from `iter()` may need the deref pattern. Audit via `cargo check` across the workspace once the vyre-lower compile blocker clears.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["reference", "half_migration"]
scope = [
  "Close ROADMAP.md HM2: move the reference interpreter public execution tree under vyre-reference/src/execution and retire old interp/eval_expr/eval_node/hashmap_interp/typed_ops root entry-module names without compatibility shims.",
]
proof_required = [
  "Root lib.rs must expose reference_eval/run_arena_reference/run_storage_graph from execution, not through interp compatibility.",
  "Expression, node, call, cast, hashmap, sequential, and typed-op evaluators must live under execution/ and use execution-local imports.",
  "Workspace callers and tests must use vyre_reference::reference_eval or explicit vyre_reference::execution::* paths.",
  "No old crate::hashmap_interp / crate::eval_expr / crate::typed_ops / vyre_reference::interp references remain.",
]
proof = [
  "vyre-reference/src/lib.rs declares pub mod execution and removes interp/eval_expr/eval_node/eval_call/eval_expr_cast/hashmap_interp/typed_ops module entries.",
  "execution/mod.rs owns call, expr, expr_cast, hashmap, node, sequential, and typed_ops modules; run_arena_reference delegates directly to execution::hashmap.",
  "Reference tests import vyre_reference::execution::expr where direct expression evaluation is required.",
  "subnormal_contract now asserts the deterministic canonical_f32 NaN contract explicitly.",
  "rg old root-module references across vyre-reference/src, vyre-reference/tests, vyre-self-substrate/src, and vyre-primitives/src returned no matches.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-reference --lib -> 34 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-reference --test subnormal_contract -> 15 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-reference --test expr_adversarial_proptest -> 102 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["core", "lower_emit", "half_migration"]
scope = [
  "Close ROADMAP.md HM3: keep the public vyre-core lower facade pointed at canonical vyre-lower, never foundation lower.",
]
proof_required = [
  "vyre-core must depend on vyre-lower.",
  "vyre::lower::lower must type-check as Program -> KernelDescriptor through vyre-lower.",
  "Boundary test must reject concrete GPU runtime dependencies in vyre-core.",
]
proof = [
  "vyre-core/Cargo.toml depends on vyre-lower.",
  "vyre-core/src/lib.rs exposes vyre_lower::lower::lower and vyre_lower::* through pub mod lower.",
  "vyre-core/tests/dependency_boundary.rs asserts the canonical lower facade and no concrete GPU runtime deps.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre --test dependency_boundary -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit", "half_migration"]
scope = [
  "Close ROADMAP.md S15/HM4: split Program-IR optimizer registrations from descriptor rewrites with explicit ProgramPass naming and no old Pass/PassKind/PassRegistration production API.",
]
proof_required = [
  "Foundation optimizer production registration types must be named ProgramPass/ProgramPassKind/ProgramPassRegistration.",
  "The vyre_pass macro must emit ProgramPass and ProgramPassRegistration.",
  "CSE/DCE scheduler registration modules must not live behind wrapper.rs names.",
  "No old PassKind/PassRegistration/trait Pass/impl Pass for/dyn Pass/optimizer::Pass references remain in foundation optimizer or the macro.",
]
proof = [
  "vyre-foundation/src/optimizer.rs defines ProgramPass, ProgramPassKind, and ProgramPassRegistration; registered_passes returns Vec<ProgramPassKind>.",
  "vyre-macros/src/lib.rs implements ::vyre::optimizer::ProgramPass and submits ProgramPassRegistration.",
  "vyre-foundation/src/optimizer/passes/fusion_cse/{cse,dce}/program_pass.rs replaced the old wrapper.rs registration files.",
  "rg old Pass names across vyre-foundation/src, vyre-foundation/tests, and vyre-macros/src/lib.rs returned no matches.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-foundation --tests -p vyre-macros -> passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation optimizer::scheduler --lib -> 19 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation cse_analyze_skips_empty --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation dce_analyze_always_runs --lib -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "device_profile"]
scope = [
  "Close ROADMAP.md S3: route backend capability facts through one neutral vyre-driver::DeviceProfile projection surface.",
]
proof_required = [
  "DeviceProfile must project to optimizer AdapterCaps, validation BackendCapabilities, and lowering strategy capabilities.",
  "wgpu and CUDA must convert live backend/device facts into DeviceProfile instead of hardcoding planner facts in shared code.",
  "Device signatures may enrich architecture hints but must not overwrite live per-workgroup shared-memory limits.",
]
proof = [
  "vyre-driver/src/device_profile.rs owns DeviceProfile and From<DeviceProfile> projections for optimizer, validation, and strategy capability records.",
  "vyre-driver-wgpu/src/runtime/adapter_caps_probe.rs and WgpuBackend::device_profile convert selected live wgpu facts into DeviceProfile.",
  "vyre-driver-cuda/src/device.rs CudaDeviceCaps::to_device_profile converts CUDA device attributes into DeviceProfile.",
  "DeviceSignature::apply_to_profile now preserves a nonzero live max_shared_memory_bytes value while still filling architecture hints and bank metadata.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver device_profile --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver device_signature --lib -> 8 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver --test device_signature_path -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu --test shared_backend_contract --test subgroup_reporting_honesty -> 6 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda --test capability_contracts -> 6 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["op_matrix"]
scope = [
  "Close ROADMAP.md S6 (op_matrix coverage gate) + S7 (tier-vs-id consistency check).",
]
proof_required = [
  "cargo xtask op-matrix --check is wired into architectural-invariants.yml::op-matrix-coverage as a required job.",
  "scripts/check_required_ci_jobs.sh names the new gate.",
  "validate_records fails when a Tier-2.5 row carries a vyre-libs:: op or a Tier-3 row carries a vyre-primitives:: op.",
  "tier_id_mismatch is symmetric and only fires for the (Primitive, Libs) cross.",
  "OP_MATRIX.toml regeneration covers every previously-missing op row.",
]
proof = [
  "xtask/src/op_matrix.rs::validate_records adds the per-op tier_id_mismatch check with a clear remediation message.",
  "tier_id_mismatch helper added beside validate_records; only (Primitive, Libs) and (Libs, Primitive) fail.",
  ".github/workflows/architectural-invariants.yml gains the op-matrix-coverage job (cargo xtask op-matrix --check, fetch-depth: 1).",
  ".github/CI_REQUIRED.md lists op-matrix-coverage; scripts/check_required_ci_jobs.sh REQUIRED_JOBS extended.",
  "cargo xtask op-matrix --write regenerated docs/optimization/OP_MATRIX.toml (+150 lines of previously-missing inventory rows).",
  "cargo xtask op-matrix --check post-regen exits 0.",
  "scripts/check_required_ci_jobs.sh post-add reports 'required CI jobs ✓ (4 checked)'.",
]
notes = [
  "S8 (canonical op definition that generates or validates all registry entries) requires a deeper substrate (proc-macro generator)  -  sequenced after S6/S7 enforcement is in place so the generator has a green signal to write toward.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "runtime_megakernel", "core", "build"]
scope = [
  "Close ROADMAP.md S4/S19 cleanup for shared-crate upward dependency hygiene and stale adapter wiring.",
]
proof_required = [
  "vyre-driver, vyre-runtime, vyre-foundation, and vyre-core default manifests must not depend on vyre-libs, vyre-primitives, vyre-intrinsics, or non-optional vyre-self-substrate.",
  "Runtime-owned provenance must accept caller-supplied Programs; self-substrate Program construction must be isolated behind optional adapter features.",
  "Domain-specific C frontend workspace code must live in vyre-frontend-c; vyre-runtime owns only the generic megakernel workspace adapter seam.",
  "vyre-core must keep ByteRange available without importing vyre-primitives.",
  "Boundary tests must fail on renewed shared-crate catalog/self-substrate dependency leakage.",
]
proof = [
  "vyre-driver/Cargo.toml has default = [] and makes vyre-self-substrate optional under self-substrate-adapters; default VSA fingerprinting uses Program::fingerprint through vyre-driver/src/launch.rs.",
  "vyre-runtime/Cargo.toml has default = [] and makes vyre-self-substrate optional under self-substrate-adapters; c-frontend-adapter is a compatibility no-op because the C workspace moved to vyre-frontend-c::megakernel_workspace.",
  "vyre-runtime/src/megakernel/planner/provenance.rs exposes build_scallop_lineage_with_program_and_scratch for caller-owned Programs; build_scallop_lineage_with_scratch remains only under self-substrate-adapters.",
  "vyre-foundation/src/runtime/match_result.rs defines neutral ByteRange and zero-cost Match bridges; vyre-core re-exports that foundation type and drops the vyre-primitives dependency.",
  "vyre-foundation/tests/workspace_structure_contracts.rs now checks vyre-foundation, vyre-driver, vyre-runtime, and vyre-core manifests for upward dependency leakage and includes vyre-core source in concrete-backend-name scans.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver --lib -> pass.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver --lib --features self-substrate-adapters -> pass.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --lib -> pass.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --lib --features self-substrate-adapters,c-frontend-adapter -> pass.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime --lib -> 323 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-frontend-c megakernel_workspace -> 26 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-frontend-c --test megakernel_workspace_contracts -> 12 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-macros -> 14 inline tests, 22 trybuild UI cases, and 21 integration/generated/release-surface tests passed; 5 expensive macro tests remain explicitly ignored.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-spec -> 252 passed, 1 explicitly ignored; vyre-spec now has 31 external test files and 26 inline test/proptest declarations across frozen wire, property, generated, and surface contracts.",
  "cargo check -q -p vyre --lib -> pass.",
  "cargo test -q -p vyre --test dependency_boundary -> 2 passed.",
  "cargo test -q -p vyre-foundation --test workspace_structure_contracts shared_driver_and_runtime_do_not_depend_on_catalog_crates -> 1 passed.",
  "cargo test -q -p vyre-foundation --test workspace_structure_contracts shared_source_does_not_name_concrete_backend_apis -> 1 passed.",
]
notes = [
  "Full workspace_structure_contracts still has pre-existing failures in oversized-module and nested-src-test audits unrelated to this cleanup; targeted S4/S19 boundary tests pass.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit", "driver_wgpu", "driver_cuda"]
scope = [
  "Close ROADMAP.md S16: replace competing pre_lowering/lower/descriptor-cleanup entry choices with one canonical vyre-lower pre-emit pipeline.",
]
proof_required = [
  "The canonical pipeline must run semantic Program optimization, call inlining, descriptor lowering, descriptor verification, descriptor cleanup, and output verification in one API.",
  "Dead unreachable branches must be pruned before call inlining so unreachable unknown calls do not block backend emission.",
  "wgpu and CUDA descriptor gates must call the canonical pipeline instead of raw vyre_lower::lower(program).",
  "wgpu must emit the already-clean descriptor directly rather than re-running descriptor cleanup in the concrete driver.",
  "Program-shaped Naga compatibility entry points must delegate to the descriptor emitter instead of maintaining a second Program-to-Naga production path.",
]
proof = [
  "vyre-lower/src/pre_emit.rs defines prepare_program_for_emit and lower_for_emit; lower_for_emit returns LoweredKernel with optimized Program, verified cleaned KernelDescriptor, and descriptor stats.",
  "prepare_program_for_emit runs pre_lowering once before inline_calls, then runs pre_lowering again after inlining; this preserves the CUDA dead-branch unknown-call contract.",
  "vyre-driver-wgpu/src/emit/descriptor_gate.rs and vyre-driver-cuda/src/codegen/descriptor_gate.rs now call vyre_lower::lower_for_emit.",
  "vyre-driver-wgpu/src/emit/mod.rs calls vyre_emit_naga::emit on the cleaned descriptor after workgroup policy verification.",
  "vyre-emit-naga/src/program/entry.rs compatibility emit functions route through vyre_lower::lower_for_emit and crate::emit.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower pre_emit --lib -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu descriptor_gate --lib -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda descriptor_gate --lib -> 3 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda --test ptx_codegen_smoke -> 33 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
]
notes = [
  "Superseded 2026-05-02: S1 moved CUDA emission onto vyre-emit-ptx for the full CUDA PTX smoke suite; this S16 claim remains the pre-emit pipeline claim.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "lower_emit"]
scope = [
  "Advance ROADMAP.md S1/HM5: remove the dead Program-level Naga AST construction path after routing Program-shaped compatibility entry points through the descriptor emitter.",
]
proof_required = [
  "vyre-emit-naga must not keep a second Program-to-Naga AST builder once public compatibility APIs delegate through vyre-lower::lower_for_emit.",
  "wgpu tests must import the descriptor-backed compatibility API from vyre_emit_naga rather than a driver-local naga_emit path.",
  "The remaining Naga Program compatibility module must only own API delegation, atomic/write-target scanning for prepared Program helpers, and trap metadata.",
]
proof = [
  "Deleted vyre-emit-naga/src/program/{function_builder,module_builder,node,utils}.rs and the program/expr subtree.",
  "Removed Program-only Naga extension emitter registrations; extension_ops now retains only atomic-target scanner registrations used by prepared_program.",
  "Updated wgpu Naga tests from vyre_driver_wgpu::emit::naga_emit to vyre_emit_naga::program.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga --tests -> 73 lib tests and 11 test targets passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
]
notes = [
  "Superseded by the later S1 closure claim in this file: CUDA now emits final PTX through `vyre-emit-ptx` from the canonical KernelDescriptor.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit"]
scope = [
  "Close ROADMAP.md S2: enforce the optimizer layer split between Program-IR semantic passes and lowered KernelDescriptor cleanup rewrites.",
]
proof_required = [
  "Foundation Program optimizer code must not import or pattern-match lowered descriptor types.",
  "Lowered descriptor cleanup rewrites must use descriptor-prefixed CSE/DCE/const-fold names.",
  "Emit and concrete driver crates must not host ProgramPass/PassScheduler implementations or duplicate Layer-1 literal folding helpers.",
]
proof = [
  "vyre-lower/tests/rewrite_layer_contract.rs scans vyre-foundation/src/optimizer and fails on KernelDescriptor/KernelOp/vyre_lower references.",
  "The same contract fails if vyre-lower/src/rewrites/mod.rs exposes unprefixed const_fold/cse/dce modules or re-exports instead of descriptor_const_fold/descriptor_cse/descriptor_dce.",
  "The same contract scans vyre-emit-naga, vyre-emit-ptx, vyre-driver-wgpu, and vyre-driver-cuda for ProgramPass, PassScheduler, pre_lowering::optimize, foundation optimizer imports, and duplicated fold_* literal helpers.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower --test rewrite_layer_contract -> 3 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda", "driver_wgpu", "lower_emit"]
scope = [
  "Close ROADMAP.md S1: remove the last concrete-driver direct Program emit path and route CUDA final PTX through vyre-lower + vyre-emit-ptx.",
]
proof_required = [
  "CUDA must emit final PTX from the canonical KernelDescriptor, not from a driver-local Program walker.",
  "The descriptor PTX emitter must cover the CUDA smoke-suite semantics before the legacy emitter is deleted.",
  "Driver-local PTX Program-emitter files must be removed rather than left as compatibility shadows.",
  "S1 roadmap state must record the one-lowering-truth boundary.",
]
proof = [
  "vyre-driver-cuda/src/codegen.rs now calls descriptor_gate::validate_and_analyze(program, target_sm) and vyre_emit_ptx::emit_with_options(&descriptor, PtxEmitOptions { target, subgroup_size, ulp_budget }).",
  "vyre-emit-ptx/src/lib.rs now owns CUDA descriptor emission options and emits PTX 7.8, launch-ABI params, fixed global/local/workgroup id registers, params metadata guard, shared memory declarations, guarded u32/i32 div/rem, MulHigh, subgroup ballot/shuffle/add/size, strict inverse sqrt, budgeted approximate transcendentals, traps, casts, and structured control flow.",
  "Deleted vyre-driver-cuda/src/codegen/barriers.rs and vyre-driver-cuda/src/codegen/ctx/{atomic,binary,buffer,expr,mod,node,unary}.rs.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx --lib -> 96 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda --test ptx_codegen_smoke -> 33 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-ptx --tests -> passed.",
]
notes = [
  "CUDA smoke tests were updated where the canonical pre-emit optimizer now intentionally replaces slow operations with stronger codegen contracts, e.g. constant integer division through MulHigh, multiplication by constants through shift decomposition, and float division by constants through reciprocal multiplication.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer"]
scope = [
  "Fix the optimizer_idempotence_proptest soundness regression by removing the unsound CSE pointer-cache.",
  "Forward-migrate the cse_pointer_identity_cache test to assert structural-key dedup instead.",
]
proof_required = [
  "intern_expr no longer caches by *const Expr.",
  "CseCtx no longer carries the expr_key_cache field.",
  "Structural-key dedup still works (deduplication map gives O(1) intern by ExprKey).",
  "optimizer_idempotence_proptest passes.",
  "cse_intern_dedups_structurally_equivalent_subtrees test asserts the corrected contract.",
]
proof = [
  "vyre-foundation/src/optimizer/passes/fusion_cse/cse/impl_csectx.rs::expr drops the pointer cache.",
  "vyre-foundation/src/optimizer/passes/fusion_cse/cse/impl_exprkey.rs::intern_expr drops the pointer cache.",
  "vyre-foundation/src/optimizer/passes/fusion_cse/cse/cse_ctx.rs removes the expr_key_cache field + Expr import.",
  "vyre-foundation/src/optimizer/passes/fusion_cse/cse/tests/cse_intern_dedups_structurally.rs replaces the old test.",
  "cargo test -p vyre-foundation --test optimizer_idempotence_proptest -> 3 passed.",
  "cargo test -p vyre-foundation --tests -> 25 binaries OK, 0 FAILED.",
]
notes = [
  "Bisected via a dedicated phase-by-phase harness (canonicalize -> region_inline -> ConstFold -> LoopStripMine -> LoopUnroll -> StrengthReduce -> NormalizeAtomics -> CSE -> DCE). Pre-fix, every phase eval [0,0,0,0] until CSE flipped to [1,0,0,0] on the minimal failing program (BitAnd(46, Mul(BitAnd(888, Shr(invocation, 2)), invocation))). Post-fix, every phase stays [0,0,0,0].",
  "Root cause: Cow::Owned rewrites in CseCtx::expr free Box<Expr> sub-trees whose addresses get reallocated by later sub-trees, so the *const Expr cache returned stale ExprIds and CSE merged semantically distinct expressions through values.get(stale_id).",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer"]
scope = [
  "Close ROADMAP.md A6: Tier-B TOML rule database half on top of the existing eqsat.rs substrate.",
]
proof_required = [
  "vyre-foundation/src/optimizer/eqsat_toml.rs ships TomlEquivalenceRules + OpIdNode trait + EquivalenceRule schema.",
  "TomlEquivalenceRules implements Rule<L> for any L: ENodeLang + OpIdNode.",
  "Loads {[[equivalence]] left, right} TOML rows; emits cross-product of e-classes per pair.",
  "8 tests cover schema parse/reject/empty + match emission + leaf skip + name forwarding.",
]
proof = [
  "vyre-foundation/src/optimizer.rs declares pub mod eqsat_toml.",
  "vyre-foundation/Cargo.toml adds toml.workspace = true.",
  "cargo test -p vyre-foundation --lib eqsat_toml -> 8 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit", "driver_wgpu", "driver_cuda"]
scope = [
  "Close ROADMAP.md G2: make reciprocal a first-class IR operation with strict/approx backend lowering and remove the remaining variable 1.0/x gap.",
  "Modularize vyre-emit-ptx so the root lib.rs is an API facade instead of a 3500-line emitter/test god file.",
]
proof_required = [
  "UnOp::Reciprocal must have a frozen spec/wire tag and round-trip through VIR0.",
  "Foundation validation, type facts, runtime eval, reference eval, autodiff, CSE, and const/strength-reduce must all understand Reciprocal.",
  "StrengthReduce must rewrite 1.0 / x into Expr::reciprocal(x) while preserving zero/NaN literal semantics.",
  "Naga and PTX descriptor emitters must lower Reciprocal without driver-local shape rediscovery.",
  "PTX must select rcp.rn.f32 in strict mode and rcp.approx.f32 when a positive ULP budget allows approximation.",
  "vyre-emit-ptx root lib.rs must stop owning the full emitter/test implementation.",
]
proof = [
  "vyre-spec/src/un_op.rs adds UnOp::Reciprocal at wire tag 0x24; vyre-foundation serial tags encode/decode it.",
  "vyre-foundation runtime/typecheck/autodiff/CSE/const-fold paths handle Reciprocal; terminal_wire_round_trip and proptest strategy lists include it.",
  "vyre-foundation/src/optimizer/passes/algebraic/strength_reduce/mod.rs rewrites LitF32(1.0) / non-literal into Expr::reciprocal; strength_reduce/tests.rs covers the rewrite.",
  "vyre-reference/src/ieee754.rs and execution/typed_ops/float_ops.rs define canonical reciprocal reference semantics.",
  "vyre-emit-naga lowers KernelOpKind::UnOpKind(UnOp::Reciprocal) to Naga BinaryOperator::Divide with a 1.0 literal.",
  "vyre-emit-ptx lowers Reciprocal to rcp.rn.f32 or rcp.approx.f32 based on PtxEmitOptions::ulp_budget, with a focused unit test.",
  "vyre-emit-ptx/src/lib.rs is now 137 lines; errors, target options, registers, emitter internals, patterns, and tests live in separate files.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation float_one_div_variable_becomes_reciprocal_unop -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation --test terminal_wire_round_trip -> 4 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx --lib -> 97 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-reference --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-spec --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit"]
scope = [
  "Modularize vyre-emit-naga so the root lib.rs is an API/cache facade instead of a 2600-line emitter/test god file.",
]
proof_required = [
  "Root lib.rs must own public API and module cache only.",
  "Naga construction internals must move behind an emitter module.",
  "EmitError must live in its own module.",
  "Tests must move out of the root file without losing coverage.",
]
proof = [
  "vyre-emit-naga/src/lib.rs is now 161 lines and delegates descriptor construction to emitter::emit_uncached.",
  "vyre-emit-naga/src/emitter.rs owns ModuleBuilder, BodyBuilder, Naga helper lowering, trap sidecar analysis, barrier flags, and operator mapping.",
  "vyre-emit-naga/src/error.rs owns EmitError.",
  "vyre-emit-naga/src/tests.rs owns the previous root tests with explicit imports.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga --lib -> 73 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared"]
scope = [
  "Close ROADMAP.md J3: carry alignment hints through shared binding and launch plans.",
]
proof_required = [
  "BindingPlan must derive alignment once from Program buffer hints and element size.",
  "Invalid explicit alignment hints must fail before reaching a concrete backend.",
  "LaunchPlan must expose aggregate alignment metadata for backend allocation/upload paths.",
]
proof = [
  "vyre-driver/src/binding.rs adds Binding::preferred_alignment and derives it from BufferDecl::hints().preferred_alignment with element-size fallback.",
  "vyre-driver/src/binding.rs rejects non-power-of-two explicit preferred_alignment values with a Fix diagnostic.",
  "vyre-driver/src/launch.rs adds LaunchPlan::max_binding_alignment and fills it in prepare_into from validated bindings.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver binding_plan_ --lib -> 3 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver launch_plan_prepare_into_reuses_param_words --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit"]
scope = [
  "Close ROADMAP.md J2: implement descriptor-level shared-memory bank-conflict padding and wire it into the canonical lower rewrite pipeline.",
]
proof_required = [
  "The pass must rewrite only safe shared-memory strided index patterns, not arbitrary arithmetic.",
  "Stride literal rewrites must be scoped to the KernelBody whose literal pool owns them.",
  "The pass must grow shared binding element counts enough to preserve logical rows after adding padding.",
  "Unsafe cases where the stride literal feeds non-index arithmetic must remain unchanged.",
]
proof = [
  "vyre-lower/src/rewrites/bank_conflict_pad/mod.rs pads `local_id.x * stride (+ const)` shared LoadShared/StoreShared patterns when gcd(stride, bank_count) > 1 and gcd(stride + 1, bank_count) == 1.",
  "The pass tracks literal updates per body and recurses into child bodies with scoped plans, avoiding cross-body result-id collisions.",
  "Binding element_count growth is merged across top-level and nested bodies by max required padded count.",
  "vyre-lower/src/rewrites/mod.rs runs bank_conflict_pad after strength_reduce and before descriptor_const_fold in run_all_once.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower bank_conflict_pad --lib -> 4 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-lower --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit"]
scope = [
  "Close ROADMAP.md B10: make constant-buffer promotion a real lower rewrite, not only an analysis report.",
]
proof_required = [
  "Repeated fixed-size read-only global loads must become constant-buffer loads without backend-specific rediscovery.",
  "Nested bodies and all structured-control-flow child operands must be counted and rewritten.",
  "Malformed write/atomic/async-store uses must veto promotion.",
  "The rewrite must be in the canonical lower rewrite pipeline.",
]
proof = [
  "vyre-lower/src/rewrites/const_buffer_promote/mod.rs promotes eligible binding slots to MemoryClass::Constant and rewrites matching LoadGlobal ops to LoadConstant recursively.",
  "vyre-lower/src/analyses/const_buffer_promote/mod.rs now traverses all child-body operands for if/else, loops, blocks, and regions instead of only the final operand.",
  "Write, atomic, and async-store references against a candidate slot veto promotion before mutation.",
  "vyre-lower/src/rewrites/mod.rs runs const_buffer_promote in run_all_once before descriptor_const_fold.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower const_buffer_promote --lib -> 13 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-lower --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit"]
scope = [
  "Close ROADMAP.md B12: materialize shared-memory promotion for proven repeated U32 tile loads.",
]
proof_required = [
  "The rewrite must insert real shared-memory staging ops, not only report candidates.",
  "GlobalInvocationId.x and LocalInvocationId.x patterns must preserve per-workgroup addressing.",
  "Unsupported dtypes, writable bindings, single-load groups, and arbitrary index math must remain unchanged.",
  "The rewrite must be in the canonical lower rewrite pipeline and compile for both emit crates.",
]
proof = [
  "vyre-lower/src/rewrites/shared_mem_promote/mod.rs adds a fresh MemoryClass::Shared binding, inserts AsyncLoad + AsyncWait + Barrier, and rewrites repeated LoadGlobal sites to LoadShared.",
  "GlobalInvocationId.x source offsets are computed as workgroup_id.x * workgroup_size.x * 4; LocalInvocationId.x uses offset 0.",
  "The rewrite gates on read-only U32 global bindings and exact tile index producers; tests cover non-U32, writable, and single-load skips.",
  "vyre-lower/src/analyses/shared_mem_promote/analysis.rs now traverses all structured child-body operands.",
  "vyre-lower/src/rewrites/mod.rs runs shared_mem_promote after strength_reduce and before bank_conflict_pad.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower shared_mem_promote --lib -> 24 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-lower --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-ptx --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "driver_shared"]
scope = [
  "Close ROADMAP.md D6: bind-group and binding reuse across repeated wgpu launches.",
  "Close ROADMAP.md D8: indirect dispatch from device-resident count buffers through the shared driver path.",
]
proof_required = [
  "Repeated dispatches of the same compiled wgpu shader must reuse bind groups through a shared pipeline cache artifact.",
  "The direct record/readback path must consume the bind-group cache, not create fresh bind groups every launch.",
  "Indirect dispatch metadata must be discovered by shared driver code and consumed by wgpu command recording without CPU readback.",
]
proof = [
  "vyre-driver-wgpu/src/pipeline.rs stores Arc<BindGroupCache> in CachedPipelineArtifact and clones it into every WgpuPipeline from the same compiled artifact.",
  "vyre-driver-wgpu/src/engine/record_and_readback.rs accepts RecordAndReadbackRequest::bind_group_cache and uses BindGroupCache::get_by_ids/get_or_create before pass.set_bind_group.",
  "vyre-driver-wgpu/src/runtime/prerecorded.rs records persistent dispatches through the same pipeline bind_group_cache.",
  "vyre-driver/src/program_walks/indirect.rs provides find_indirect_dispatch and validates the count buffer/offset contract.",
  "vyre-driver-wgpu/src/pipeline.rs carries Option<IndirectDispatch>; record_and_readback submits dispatch_workgroups_indirect when present.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu direct_record_and_readback_reuses_bind_groups -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver indirect_dispatch -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit"]
scope = [
  "Close ROADMAP.md B13: add shared-memory bank-conflict swizzling in addition to padding.",
]
proof_required = [
  "Safe row-pitch padding must remain the first choice when the stride literal is only used by target index multiplications.",
  "When a stride literal has non-index uses, the pass must preserve that literal and swizzle only the shared access indices.",
  "The swizzle must be inserted before the affected shared load/store and remain scoped per KernelBody.",
]
proof = [
  "vyre-lower/src/rewrites/bank_conflict_pad/mod.rs still pads local_id.x * stride patterns to stride + 1 when coprime with the bank count.",
  "The same pass now records SwizzleUpdate entries and inserts BinOp::BitXor(index, local_id.x) immediately before affected LoadShared/StoreShared ops when literal rewrite is unsafe.",
  "Swizzle fallback grows the shared binding by one bank-count guard band so low-bit XOR cannot exceed the declared shared tile.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower bank_conflict_pad --lib -> 4 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-lower --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "driver_wgpu"]
scope = [
  "Close ROADMAP.md B4: expose pipeline reflection/compile prewarm before first hot dispatch.",
]
proof_required = [
  "Prewarm must run the same backend-native compile/reflection/cache path as hot dispatch uses.",
  "Prewarm must not execute the Program or allocate output buffers.",
  "Prewarm must surface honest cache telemetry so callers can distinguish cold compile from warm hit when the backend reports counters.",
]
proof = [
  "vyre-driver/src/pipeline/compile.rs adds prewarm, prewarm_owned, and prewarm_shared; all route through compile_shared_with_telemetry and drop the returned pipeline after cache population.",
  "vyre-driver/src/pipeline/mod.rs exports PipelinePrewarmReport with pipeline_id and cache_hit.",
  "wgpu's VyreBackend::compile_native already runs validate/cache/descriptor reflection/bind-group-layout construction/shader-module creation/compute-pipeline creation and persists the compiled pipeline cache, so prewarm executes the same path used by first dispatch.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver prewarm -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "foundation_optimizer"]
scope = [
  "Replace default SipHash maps/sets in vyre-lower hot descriptor analyses and rewrites with FxHashMap/FxHashSet.",
]
proof_required = [
  "Descriptor constant folding, DCE, unused-binding pruning, dead-op analysis, workgroup-uniform analysis, texture-promotion analysis, AoS-to-SoA analysis, and const-buffer-promotion analysis must not pay std HashMap/HashSet hashing overhead for u32 ids.",
  "The optimization must be semantic-preserving across the lower rewrite/analysis test suite.",
]
proof = [
  "vyre-lower now depends on rustc-hash and uses FxHashMap/FxHashSet in descriptor_const_fold, descriptor_dce, drop_unused_bindings, dead_op, workgroup_uniform, texture_promote, layout_aos_to_soa, and const_buffer_promote.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower --lib -> 455 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel"]
scope = [
  "Close ROADMAP.md C5: gated no-op middle-arm elimination in megakernel fusion planning.",
]
proof_required = [
  "The runtime selector must consume a dead-arm mask before launch materialization, not just expose a loose helper.",
  "The reusable-scratch path must support dead-arm pruning without allocating a new result vector.",
  "Mismatched masks must not panic or corrupt the selection vector.",
]
proof = [
  "vyre-runtime/src/megakernel/planner/fusion.rs exposes prune_dead_arms_inplace, select_fused_subset_pruned, and select_fused_subset_pruned_into.",
  "vyre-runtime/src/megakernel/planner/mod.rs and vyre-runtime/src/megakernel/scaling.rs re-export the pruned selector entry points for runtime and driver consumers.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime prune_dead_arms -> 6 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime select_fused_subset_pruned -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "runtime_megakernel"]
scope = [
  "Close ROADMAP.md C6: pipeline reuse cache hit-rate audit substrate.",
]
proof_required = [
  "Cache telemetry must distinguish hits, misses, and unknown backend reporting rather than treating unknown as a miss.",
  "Hit-rate alarms must be computed over known outcomes only and must be disableable for callers that only want raw counters.",
]
proof = [
  "vyre-driver/src/pipeline/mod.rs adds PipelineCacheAudit and PipelineCacheAuditReport for CompiledPipelineBuild/PipelinePrewarmReport cache-hit streams.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver pipeline_cache_audit -> 3 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_wgpu"]
scope = [
  "Close ROADMAP.md B3: parallel Naga emission for independent descriptor/arm batches.",
]
proof_required = [
  "Batch emission must use the same cache-backed single-descriptor emit path instead of introducing a second lowering implementation.",
  "Batch results must preserve input descriptor order even when workers finish out of order.",
  "The optimized batch path must run the canonical lower rewrite pipeline for every descriptor before emission.",
]
proof = [
  "vyre-emit-naga/src/lib.rs exposes emit_many and emit_many_optimized; both shard work across std::thread::available_parallelism and collect indexed results back into input order.",
  "emit_many routes each descriptor through emit, preserving the existing module-cache and lowering path.",
  "emit_many_optimized routes each descriptor through emit_optimized, preserving the canonical optimized descriptor rewrite path.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga emit_many -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Close ROADMAP.md B7: native CUDA async tile-load emission for eligible descriptor AsyncLoad ops.",
]
proof_required = [
  "SM80+ global-to-shared U32 AsyncLoad must emit native cp.async, not the old synchronous load/store loop.",
  "Older targets and unsupported shapes must retain the bounded correctness fallback.",
  "The emitted async path must commit, wait, and fence before consumers observe shared memory.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs adds emit_cp_async_load_loop for SM80+ global U32 -> shared U32 AsyncLoad, issuing cp.async.ca.shared.global per copied word with out-of-bounds zero fill.",
  "The AsyncLoad arm now tries cp.async first and falls back to emit_async_copy_loop only when target/shape gates fail.",
  "SM80 output contains cp.async.ca.shared.global, cp.async.commit_group, cp.async.wait_group 0, and membar.cta; SM70 output still contains the bounded synchronous copy comment.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx async_load -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx -> 98 lib tests, 6 rewrite-efficacy tests, and 5 kitchen-sink snapshot tests passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Close ROADMAP.md B8: predicated PTX emission for short divergent store branches.",
]
proof_required = [
  "Short store-only if bodies must avoid SIMT branch and reconvergence labels.",
  "If/else store arms must emit complementary predicates so exactly one arm writes.",
  "General branches must keep the existing structured fallback unless the emitter can prove the branch-safe shape.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs adds emit_predicated_store and fast paths for StructuredIfThen / StructuredIfThenElse children whose bodies are exactly one StoreGlobal or StoreShared.",
  "IfThen store bodies emit @p st.* and return before label allocation.",
  "IfThenElse dual store bodies emit @p st.* for the then arm and @!p st.* for the else arm, avoiding $L_if_else_ / $L_if_end_ labels.",
  "Non-store or larger child bodies continue through the prior branch-based lowering.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx short_if -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx -> 100 lib tests, 6 rewrite-efficacy tests, and 5 kitchen-sink snapshot tests passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "foundation_optimizer"]
scope = [
  "Close ROADMAP.md C1: whole-megakernel optimization domain after composition.",
]
proof_required = [
  "Megakernel optimization must run on the final assembled Program, after persistent/JIT/custom-opcode/IO bodies are composed.",
  "Custom opcode bodies must participate in the same optimizer domain as the megakernel wrapper before backend lowering.",
]
proof = [
  "vyre-runtime/src/megakernel/builder.rs routes wrap_megakernel_program and wrap_persistent_megakernel_program_with_buffers through optimize_megakernel_program.",
  "optimize_megakernel_program calls vyre_foundation::optimizer::pre_lowering::optimize on the composed Program, not on isolated arms.",
  "custom_opcode_is_optimized_inside_whole_megakernel_program proves dead custom-handler work is eliminated from build_program_sharded_once_slots output.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime custom_opcode_is_optimized_inside_whole_megakernel_program -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_wgpu"]
scope = [
  "Fix descriptor Naga emission for buffers targeted by atomics.",
]
proof_required = [
  "Descriptor emission must declare atomic-targeted storage buffer element types as Naga atomic types so Statement::Atomic receives a valid pointer.",
  "Plain non-atomic bindings must stay scalar arrays so normal loads/stores remain valid.",
]
proof = [
  "vyre-emit-naga/src/emitter.rs collects binding slots reached by KernelOpKind::Atomic or trap sidecar atomic emission before adding global bindings.",
  "ModuleBuilder::add_binding wraps only atomic-targeted binding element types in TypeInner::Atomic and leaves non-atomic bindings scalar.",
  "atomic_add_emits_statement now asserts the emitted atomic descriptor binding is an array of TypeInner::Atomic.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-naga atomic_add_emits_statement -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel"]
scope = [
  "Close ROADMAP.md C3: shared megakernel prologue extraction and idle-path tenant-load elimination.",
]
proof_required = [
  "Direct, JIT, and priority megakernel builders must share the shutdown/lane prologue instead of carrying divergent copies.",
  "The direct/JIT hot idle path must not load tenant metadata before proving a slot is PUBLISHED.",
  "Priority scan must authorize tenant mask before claim CAS so disabled-tenant slots cannot become stuck CLAIMED work.",
]
proof = [
  "vyre-runtime/src/megakernel/builder.rs factors persistent_lane_prologue, direct_slot_base_binding, tenant_authorized_body, tenant_authorized_claim_body, and execute_already_claimed_slot_body for reuse across direct/JIT/priority builders.",
  "Direct/JIT execute paths now load observed_status first, branch on PUBLISHED, then run tenant authorization and CAS.",
  "vyre-runtime/src/megakernel/scheduler.rs loads probe_tenant_mask and gates probe_prev CAS behind a nonzero tenant mask.",
  "direct_megakernel_defers_tenant_loads_until_status_is_published proves top-level prologue only binds shutdown_flag/lane_id/slot_base and observed_status precedes tenant_mask.",
  "priority_scan_authorizes_tenant_before_claim_cas proves probe_tenant_mask precedes probe_prev.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel::builder -> 5 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel::scheduler -> 8 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "foundation_optimizer"]
scope = [
  "Close ROADMAP.md C4: runtime value-flow barrier elision between independent megakernel arms.",
]
proof_required = [
  "Barrier removal must be based on actual arm buffer effects, not adjacency alone.",
  "Read-after-write, write-after-read, write-after-write, opaque, trap, and future unknown nodes must keep barriers.",
  "Megakernel builders must run the runtime elision before backend lowering so composed arm chains benefit automatically.",
]
proof = [
  "vyre-runtime/src/megakernel/planner/barriers.rs adds elide_value_flow_barriers with BarrierElisionReport.",
  "The pass removes only Block/Region -> Barrier -> Block/Region sequences whose recursive AccessSet has no cross-arm same-buffer read/write or write/write conflict.",
  "Unknown Node/Expr variants, Node::Opaque, and Node::Trap mark the arm unknown and preserve the barrier.",
  "vyre-runtime/src/megakernel/builder.rs calls planner::elide_value_flow_barriers inside optimize_megakernel_program before foundation pre-lowering optimize.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime barrier -> 4 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Close ROADMAP.md B9: real PTX local instruction scheduling for load-use gaps.",
]
proof_required = [
  "Scheduling must change emitted instruction order, not only report hints.",
  "Only ready pure register operations may be hoisted; memory/control/async/atomic/barrier/trap fences must stop the scan.",
  "Descriptor immediate operands such as literal-pool indices must not be mistaken for data dependencies.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs now calls find_latency_filler after scalar LoadGlobal/LoadShared/LoadConstant emission and emits the selected ready pure op before the dependent chain.",
  "is_scheduling_fence blocks movement across StoreGlobal/StoreShared/Atomic/Barrier/Return/Region/Structured*/Async*/Trap.",
  "op_reads_operand ignores immediate operands for Literal and invocation-id ops, preventing false dependencies on literal-pool indices.",
  "emit_hoists_ready_pure_op_into_load_use_gap proves the schedule marker lands after ld.global.u32 and before the dependent add.u32.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx emit_hoists_ready_pure_op_into_load_use_gap -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx -> 101 lib tests, 6 rewrite-efficacy tests, and 5 kitchen-sink snapshot tests passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Close ROADMAP.md B11: CUDA/PTX read-only spatial memory promotion from texture-promote analysis.",
]
proof_required = [
  "The existing texture-promote analysis must affect emitted instructions, not only report candidates.",
  "Only repeated ReadOnly global bindings may use the read-only/non-coherent cache path.",
  "ReadWrite bindings must remain on coherent global loads.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs computes read_only_cache_slots from vyre_lower::analyses::analyze_texture_promote(desc) before BodyCtx emission.",
  "Scalar LoadGlobal/LoadShared/LoadConstant emission now selects load_space_for(binding_slot, memory_class); promoted ReadOnly global slots emit ld.global.nc.*.",
  "Vector load fusion emits ld.global.nc.vN.* for promoted slots, preserving vector packing while routing through the read-only cache.",
  "Async-copy fallback source loads also call load_space_for for eligible promoted source slots.",
  "emit_uses_read_only_cache_loads_for_texture_promoted_bindings proves repeated ReadOnly global loads emit ld.global.nc.u32.",
  "emit_keeps_read_write_loads_on_coherent_global_path proves ReadWrite loads do not emit ld.global.nc.u32.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx read_only_cache -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_load -> 9 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx -> 103 lib tests, 6 rewrite-efficacy tests, and 5 kitchen-sink snapshot tests passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime_megakernel", "driver_shared", "driver_wgpu"]
scope = [
  "Close ROADMAP.md D1: persistent resident batch dispatch for many-small-launch megakernel workloads.",
]
proof_required = [
  "The shared compiled-pipeline contract must support resident-handle batches instead of forcing one host submission per item.",
  "The runtime megakernel must expose a batch API that keeps control/ring/debug/io buffers resident and reports aggregate instrumentation.",
  "wgpu must bind caller-provided ReadWrite resident output handles directly and preserve their initial contents; it must not allocate replacement outputs or clear resident state buffers.",
]
proof = [
  "vyre-driver/src/backend/compiled_pipeline.rs adds dispatch_persistent_handles_batched with a semantics-preserving default for backends that have not overridden it yet.",
  "vyre-runtime/src/megakernel/execution.rs adds MegakernelBatchDispatchOutput and Megakernel::dispatch_persistent_handles_many_observed, including native batch dispatch, aggregate output-byte/buffer stats, and device-loss retry.",
  "vyre-driver-wgpu/src/pipeline.rs overrides dispatch_persistent_handles_batched to resolve resident/borrowed resources by BufferDecl order, split input/output handles by binding metadata, record every item into one persistent command encoder submission, and read back each output set.",
  "descriptor_buffer_bindings now marks all ReadWrite public bindings as preserve_input_contents, including outputs, so stateful resident megakernel buffers are initialized instead of cleared.",
  "persistent_handle_many_dispatch_uses_backend_batch_contract_once proves the runtime calls the compiled-pipeline batch API for multiple resident handle tuples.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime persistent_handle -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "driver_shared"]
scope = [
  "Close ROADMAP.md B5: eliminate full-Program early pipeline-cache misses for equivalent generated pipelines.",
]
proof_required = [
  "The in-process early pipeline cache must use the same normalized program identity as the disk WGSL cache.",
  "Runtime storage lengths that do not alter generated code must not perturb early cache keys.",
  "The dispatch policy, adapter identity, ABI version, Naga version, lowering contract, and workgroup override must still participate in the key.",
]
proof = [
  "vyre-driver-wgpu/src/pipeline/disk_cache.rs::early_pipeline_cache_key now hashes vyre_driver::pipeline::normalized_program_cache_digest(program) instead of Program::fingerprint().",
  "Existing key fields for adapter fingerprint, wire ABI, Naga version, lowering contract, dispatch policy, and workgroup override remain unchanged.",
  "early_pipeline_cache_key_uses_normalized_program_digest constructs two Programs with different raw fingerprints and different storage lengths, then proves their early pipeline cache keys match.",
  "normalized_cache_digest_erases_runtime_storage_lengths remains green for the shared digest substrate.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu early_pipeline_cache_key_uses_normalized_program_digest -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu normalized_cache_digest_erases_runtime_storage_lengths -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda", "runtime_megakernel", "driver_shared"]
scope = [
  "Extend ROADMAP.md D1 onto the canonical CUDA path: resident-handle batches launch asynchronously instead of falling back to the serial trait loop.",
]
proof_required = [
  "CUDA compiled pipelines must override dispatch_persistent_handles_batched, not inherit one-item-at-a-time default dispatch.",
  "The batch path must reuse the compiled PTX/module cache key and prepared launch plan instead of relowering or rebuilding launch metadata per item.",
  "Independent resident batches must enqueue before waiting so CUDA streams can overlap where resources permit.",
]
proof = [
  "vyre-driver-cuda/src/pipeline.rs::dispatch_persistent_handles_batched now resolves each Resource batch into CUDA resident handles, calls dispatch_resident_async_concrete_with_ptx_key with the cached ptx_src/module_key and cloned prepared plan, stores every pending dispatch, then waits/readbacks in order.",
  "Shape-mismatched configs intentionally fall back to the semantics-preserving single-dispatch path because the compiled prepared plan is launch-shape-bound.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-runtime --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda", "driver_wgpu"]
scope = [
  "Close ROADMAP.md D3: make async tile-copy promotion overlap compute on native CUDA cp.async targets.",
]
proof_required = [
  "Lowering must not insert AsyncWait immediately after AsyncLoad when pure pre-existing work can overlap the copy.",
  "The PTX emitter must not emit cp.async.wait_group immediately after cp.async.commit_group; waits belong at AsyncWait.",
  "Correctness must still be preserved if a malformed descriptor issues cp.async without a matching AsyncWait.",
]
proof = [
  "vyre-lower/src/rewrites/shared_mem_promote/mod.rs now separates async issue ops from wait/barrier ops, moves only pure preload compute that originally preceded the promoted loads between issue and wait, and keeps shared-memory consumers after the wait/barrier.",
  "vyre-emit-ptx/src/emitter.rs tracks pending native cp.async tags, emits cp.async.commit_group at AsyncLoad, emits cp.async.wait_group 0 + membar.cta at the matching AsyncWait, and drains pending groups before ret if a descriptor omits AsyncWait.",
  "wgpu remains on the bounded synchronous copy fallback because WebGPU/Naga has no native cp.async primitive; the shared descriptor ordering is still valid for it.",
  "leaves_pure_preload_compute_between_async_issue_and_wait proves shared_mem_promote keeps original pure index work between AsyncLoad and AsyncWait.",
  "cp_async_wait_is_deferred_until_async_wait_to_overlap_compute proves emitted PTX contains independent add.u32 between cp.async.commit_group and cp.async.wait_group 0.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower shared_mem_promote -> 25 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx cp_async_wait_is_deferred_until_async_wait_to_overlap_compute -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx async_load_uses_cp_async_on_sm_80 -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "driver_cuda", "driver_wgpu"]
scope = [
  "Close ROADMAP.md D2: independent same-program dispatch arms must enqueue through a native batch path instead of serial one-at-a-time dispatch.",
]
proof_required = [
  "The shared compiled-pipeline contract must expose a borrowed-input batch dispatch API with a semantics-preserving default.",
  "CUDA compiled pipelines must enqueue every batch item before waiting and must reuse the compiled PTX/module/prepared launch metadata.",
  "wgpu compiled pipelines must record same-program batch items together instead of dispatching each borrowed input set independently.",
]
proof = [
  "vyre-driver/src/backend/compiled_pipeline.rs adds dispatch_borrowed_batched(&[&[&[u8]]], config), defaulting to ordered dispatch_borrowed calls for backends without native concurrency.",
  "vyre-driver-cuda/src/pipeline.rs overrides dispatch_borrowed_batched. Same-shape configs validate each input batch against the cached CudaDispatchPlan, call dispatch_prepared_borrowed_async_with_ptx_key with the cached ptx_src/module_key/prepared plan for every item, then await results in order. Shape-mismatched configs still enqueue through the cached PTX/module-key path before waiting.",
  "vyre-driver-wgpu/src/pipeline.rs overrides dispatch_borrowed_batched. It materializes borrowed inputs into persistent handles once, builds BorrowedDispatchItem views for every job, records the whole batch through dispatch_borrowed_persistent_batched for each fixpoint iteration, then readbacks outputs in order.",
  "compiled_pipeline_borrowed_batch_default_preserves_order proves the shared default keeps borrowed-input semantics and result ordering.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver compiled_pipeline_borrowed_batch_default_preserves_order -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Advance ROADMAP.md B6 from analysis-only tensor-core detection to an executable neutral MMA operation with real PTX lowering.",
]
proof_required = [
  "The descriptor must be able to represent a tensor-core MMA operation without backend-specific types.",
  "Verifier and descriptor rewrites must understand multi-result MMA output ids instead of silently dropping result lanes.",
  "The PTX emitter must emit a real native tensor-core instruction for the supported f16/f32 fragment shape.",
  "CUDA capability reporting must only advertise tensor-core lowering once the emitter can lower an executable op.",
]
proof = [
  "vyre-lower/src/descriptor.rs adds MatrixMmaShape, MatrixMmaElement, MatrixMmaLayout, and KernelOpKind::MatrixMma with a four-id accumulator tuple contract.",
  "KernelOp::result_ids()/result_id_count() now model multi-result ops; verify, dead-op analysis, DCE, CSE protection, LICM, loop_unroll, shared_mem_promote, and bank_conflict_pad were updated to treat MatrixMma result ids as base..base+4.",
  "vyre-emit-ptx/src/emitter.rs lowers MatrixMma M16N8K16 row/col f16/f16/f32 to mma.sync.aligned.m16n8k16.row.col.f32.f16.f16.f32 and binds all four result registers.",
  "vyre-emit-naga/src/emitter.rs rejects MatrixMma explicitly with UnsupportedOp instead of falling through; this keeps unsupported substrates fail-closed.",
  "vyre-driver-cuda/src/backend/capabilities.rs::lowers_tensor_core_ops now returns true because an executable PTX lowering exists.",
  "matrix_mma_emits_real_mma_sync_and_binds_all_four_results proves verifier accepts the four-result op, PTX contains mma.sync, and a later Store can consume the fourth result id.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx matrix_mma_emits_real_mma_sync_and_binds_all_four_results -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-lower --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-naga --tests -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Close ROADMAP.md O40: remove ordered-map and per-loop string-allocation overhead from the PTX emitter hot path.",
]
proof_required = [
  "PTX operand/register and binding-slot lookups must not pay ordered-map logN overhead during descriptor emission.",
  "Membership-only tables in the PTX emitter must use hash sets instead of tree sets.",
  "Structured loop and cp.async tags must reuse descriptor shared names rather than allocating fresh Strings during emission.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs now uses FxHashMap/FxHashSet for operand_to_reg, slot_to_ptr, slot_to_shared_symbol, read_only_cache_slots, pending_cp_async_tags, loop_indices, and EmitFacts producer/literal maps.",
  "StructuredForLoop inserts/removes loop indices using the descriptor Arc<str> loop_var; LoopIndex lookup borrows by &str, eliminating loop_var.to_string() churn in the emit loop.",
  "emit_cp_async_load_loop stores the descriptor tag Arc<str> in pending_cp_async_tags instead of allocating tag.to_string() for every native cp.async issue.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx matrix_mma_emits_real_mma_sync_and_binds_all_four_results -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Close ROADMAP.md O41: pre-size PTX emitter buffers and lookup tables from descriptor shape.",
]
proof_required = [
  "PTX module/body/declaration text buffers must not grow from zero capacity on large descriptors.",
  "Operand/register and binding lookup tables must be sized before the body walk, not grown one insert at a time.",
  "The capacity estimate must account for nested child bodies so structured kernels do not under-reserve to top-level ops only.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs adds estimated_module_text_capacity, estimate_body_text_capacity, and body_op_count_recursive.",
  "ModuleBuilder::new uses String::with_capacity; BodyCtx::new uses String::with_capacity and pre-sized FxHashMap/FxHashSet tables.",
  "The recursive body op count seeds operand_to_reg capacity, while binding slot count seeds global pointer and shared-symbol maps.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx matrix_mma_emits_real_mma_sync_and_binds_all_four_results -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-ptx --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Remove constant-index address arithmetic from PTX scalar/vector memory emission.",
]
proof_required = [
  "Literal buffer indices must lower to immediate byte offsets instead of runtime mul.wide + add.u64 address arithmetic.",
  "The optimization must cover scalar global/constant/shared addresses and vector global load/store address operands.",
  "Large literal offsets that cannot be encoded safely must fall back to the existing register-address path.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs tracks u32 literal result ids during emission and folds index * element_size into immediate `[base+offset]` / `[shared_symbol+offset]` operands when the byte offset fits the PTX immediate path.",
  "emit_global_address_operand now returns the address operand string directly, so vector load/store chains also use immediate offsets instead of forcing an address register.",
  "literal_index_store_uses_immediate_byte_offset proves u32 index 3 emits +12 and does not emit mul.wide.u32.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx literal_index_store_uses_immediate_byte_offset -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-ptx --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Remove per-memory-op linear binding scans from PTX emission.",
]
proof_required = [
  "Binding lookup by slot during PTX emission must be O(1), not a scan across every declared binding.",
  "The lookup table must be built once per emitted descriptor and borrow the existing BindingSlot records instead of cloning them.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs BodyCtx now builds slot_to_binding: FxHashMap<u32, &BindingSlot> once in BodyCtx::new.",
  "binding_for_slot now performs a hash lookup and returns the borrowed BindingSlot, eliminating the previous bindings.slots.iter().find scan from every memory op.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-ptx --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lower_emit", "driver_cuda"]
scope = [
  "Remove per-shared-memory-address String clone from PTX emission.",
]
proof_required = [
  "Shared-memory address emission must borrow the preallocated shared symbol instead of cloning a String per access.",
]
proof = [
  "vyre-emit-ptx/src/emitter.rs emits dynamic shared-memory operands from a borrowed slot_to_shared_symbol entry; the previous .cloned() allocation is gone.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-emit-ptx --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_cuda"]
scope = [
  "Deduplicate concurrent CUDA module-cache cold misses for identical PTX.",
]
proof_required = [
  "Two concurrent cold dispatches for the same ModuleCacheKey must not both JIT-load and then discard one CUmodule.",
  "The cache must still return an existing module on a race that fills the key before this caller inserts.",
]
proof = [
  "vyre-driver-cuda/src/backend/module_cache.rs now uses DashMap entry reservation on the miss path. Occupied races count as hits and return the existing CUfunction; Vacant entries load exactly once before insert.",
  "The previous load-before-second-get path that could duplicate cuModuleLoadData work for the same key is gone.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "driver_cuda"]
scope = [
  "Remove CUDA module-cache eviction dependency on self-substrate internals.",
]
proof_required = [
  "Driver crates must not depend on self-substrate policy modules for backend cache behavior.",
  "The shared eviction helper must live in vyre-driver and be usable by every backend cache.",
  "Malformed cache sizing inputs must not panic in backend-facing code.",
]
proof = [
  "vyre-driver/src/cache_eviction.rs provides a backend-neutral greedy retention helper and tracing-based eviction metric hook.",
  "vyre-driver-cuda/Cargo.toml no longer depends on vyre-self-substrate.",
  "vyre-driver-cuda/src/backend/module_cache.rs calls vyre_driver::cache_eviction directly.",
  "cache_eviction::invalid_sizing_is_clamped_not_panicked proves bad n/k inputs clamp instead of panicking.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver cache_eviction -> 5 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "driver_wgpu"]
scope = [
  "Remove wgpu direct dependency on self-substrate cache-impact modules and batch its pipeline-cache eviction.",
]
proof_required = [
  "wgpu must call shared driver-layer cache invalidation instead of importing self-substrate internals.",
  "wgpu pipeline cache eviction must avoid one full DashMap scan per evicted entry under over-capacity pressure.",
  "The wgpu backend crate must not list vyre-self-substrate as a direct dependency.",
]
proof = [
  "vyre-driver/src/cache_invalidation.rs owns the causal impact/provenance bridge behind the shared self-substrate-adapters feature.",
  "vyre-driver-wgpu/src/backend_impl.rs and src/pipeline/disk_cache_invalidation.rs now call vyre_driver::cache_invalidation::impacted_entries.",
  "vyre-driver-wgpu/src/runtime/cache/pipeline.rs snapshots cache entries once, applies vyre_driver::cache_eviction::select_retention_set, then removes victims in one batch.",
  "vyre-driver-wgpu/Cargo.toml no longer depends directly on vyre-self-substrate; it enables vyre-driver/self-substrate-adapters instead.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver --features self-substrate-adapters cache_invalidation -> passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-wgpu --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "shared_hot_path"]
scope = [
  "Remove per-load/store binding scans and cold empty-vector growth from substrate-neutral lowering.",
]
proof_required = [
  "Lowering must classify buffer slots with O(1) lookup instead of scanning every binding for every memory operation.",
  "Descriptor body vectors must be seeded from existing IR statistics so large kernels do not grow from zero capacity.",
  "The change must remain backend-neutral and preserve descriptor output semantics.",
]
proof = [
  "vyre-lower/src/lower.rs now stores buffer_slots and slot_memory_classes in FxHashMap/FxHashSet-backed tables built once per Program.",
  "load_kind/store_kind now use slot_memory_classes instead of bindings.iter().find on every load/store lower.",
  "Root KernelBody capacity is seeded from Program::stats(); nested bodies are pre-sized from their node slices.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower lower -> 13 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove ordered-map overhead from descriptor rewrite hot paths and fix nested branch-collapse reparenting.",
]
proof_required = [
  "Descriptor rewrites that only need id lookup must use hash tables, not ordered maps.",
  "Collapsing a literal branch with nested structured control flow must reparent child bodies correctly instead of leaving stale child-body ids.",
  "The rewrite stack must keep its existing behavioral tests green.",
]
proof = [
  "strength_reduce, identity_elim, branch_collapse, load_forwarding, and descriptor_cse now use FxHashMap/FxHashSet-backed lookup/remap tables.",
  "branch_collapse::inline_child_body now appends inlined child bodies and rebases StructuredIf/Loop/Block/Region child-body operands.",
  "inlined_nested_control_flow_rebases_child_body_refs proves nested control flow survives branch inlining with a valid reparented child id.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 193 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove debug-string allocation and body cloning from descriptor common-subexpression analysis.",
]
proof_required = [
  "CSE analysis must use structural op keys instead of debug-format heap strings per candidate op.",
  "descriptor_cse must analyze a KernelBody directly instead of cloning it into a temporary KernelDescriptor per recursive body.",
]
proof = [
  "vyre-lower/src/analyses/common_subexpr/mod.rs now buckets candidates with OpKey { kind, operands } in FxHashMap.",
  "common_subexpr::analyze_body exposes body-level analysis and keeps analyze(desc) as a thin wrapper.",
  "descriptor_cse now calls analyze_body(String::new(), body), removing the previous temporary descriptor and body.clone().",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower common_subexpr -> 8 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 193 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Make descriptor load-forwarding compose with its own prior remaps.",
]
proof_required = [
  "Load/store cache keys must resolve through earlier forwarded result ids, or chained forwarding misses equivalent memory indices.",
  "The final operand rewrite must continue to rewrite surviving users through the same remap graph.",
]
proof = [
  "vyre-lower/src/rewrites/load_forwarding/mod.rs now resolves store indices, store values, and load indices through id_remap while building the forwarding cache.",
  "cache_keys_resolve_through_prior_load_forwarding covers a second load indexed by a previously-forwarded load result and proves it forwards from the prior store.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower load_forwarding -> 12 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Restore dead scratch-binding pruning while preserving explicit output contracts.",
]
proof_required = [
  "drop_unused_bindings must not retain every ReadWrite binding forever; dead scratch bindings must disappear after optimizer cleanup.",
  "Explicit output bindings must survive when host readback still depends on their binding metadata.",
  "The kitchen-sink optimizer snapshot must retain its one-binding optimized shape.",
]
proof = [
  "vyre-lower/src/rewrites/drop_unused_bindings/mod.rs now retains referenced slots, WriteOnly outputs, and ReadWrite bindings with output-contract names while pruning dead scratch-style ReadWrite slots.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower drop_unused_bindings -> 7 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower --test kitchen_sink_snapshot -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Inline descriptor CSE key operands to remove per-candidate heap allocations.",
]
proof_required = [
  "CSE structural keys must not allocate for the common 0-4 operand descriptor ops.",
  "The common-subexpression analysis behavior must remain unchanged.",
]
proof = [
  "vyre-lower/src/analyses/common_subexpr/mod.rs now stores OpKey operands in SmallVec<[u32; 4]>.",
  "vyre-lower/Cargo.toml adds smallvec from the workspace for inline operand storage.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower common_subexpr -> 8 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Fix descriptor CSE preorder indexing across sibling child bodies.",
]
proof_required = [
  "CSE reports must not assign overlapping op indices to sibling child bodies.",
  "The body walker must advance a single preorder cursor through the whole body tree.",
]
proof = [
  "common_subexpr::walk_body now takes a mutable next_index cursor and increments it once per visited op.",
  "sibling_child_body_indices_are_monotonic_not_overlapping proves two sibling child bodies report distinct op indices.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower common_subexpr -> 9 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Avoid duplicate child-body CSE analysis inside descriptor_cse.",
]
proof_required = [
  "Public recursive CSE analysis must still include child bodies.",
  "descriptor_cse must analyze each body once at its own recursion level instead of rewalking descendants repeatedly.",
]
proof = [
  "common_subexpr::analyze_body_shallow analyzes only the current KernelBody ops.",
  "descriptor_cse now calls analyze_body_shallow before recursing into child bodies itself.",
  "shallow_analysis_excludes_child_bodies proves the shallow path excludes descendant ops while analyze_body still includes them.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower common_subexpr -> 10 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 194 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Make descriptor CSE recognize bit-exact commutative binary ops without unsafe arithmetic normalization.",
]
proof_required = [
  "Descriptor CSE must group swapped operands for operations whose semantics are symmetric and bit-exact.",
  "Descriptor CSE must not normalize arithmetic add/mul while dtype and floating-point mode are absent from the key.",
]
proof = [
  "common_subexpr::OpKey now sorts two-operand keys for BitAnd, BitOr, BitXor, Eq, Ne, And, and Or.",
  "bit_exact_commutative_swap_is_grouped proves swapped BitXor operands share a CSE group.",
  "arithmetic_commutative_swap_not_grouped_without_type_context proves Add remains order-sensitive at descriptor level.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower common_subexpr -> 11 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 194 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Make the descriptor optimizer fixed-point loop converge on full descriptor equality instead of top-level op-count heuristics.",
]
proof_required = [
  "run_all_with_stats must not declare convergence while operands, bindings, child bodies, dispatch shape, or literals still change.",
  "The fixed-point loop must honor RUN_ALL_MAX_ITERS exactly.",
]
proof = [
  "rewrites::run_all_with_stats now compares full KernelDescriptor equality between iterations and uses a while loop capped by RUN_ALL_MAX_ITERS.",
  "rewrites tests assert optimization_stats iterations stay within RUN_ALL_MAX_ITERS and already-optimal descriptors converge in one iteration.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 194 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower --test kitchen_sink_snapshot -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Replace descriptor DCE's direct-reference pruning with whole-chain liveness propagation.",
]
proof_required = [
  "Dead pure producer chains must be eliminated in one descriptor_dce pass instead of waiting for outer pipeline iteration.",
  "Parent/child body references must remain valid when they feed observable side effects.",
]
proof = [
  "descriptor_dce now propagates live result ids backward from side-effecting ops and reachable pure ops until fixed point.",
  "dce_removes_chain_of_dead_arithmetic and dce_removes_multi_hop_dead_chain prove pure dead chains are fully removed.",
  "dce_keeps_child_results_used_by_parent_after_block now proves child results survive when the parent stores their derived value.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower descriptor_dce -> 9 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower --test kitchen_sink_snapshot -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove quadratic body scans from vec_pack candidate analysis.",
]
proof_required = [
  "Vec-pack analysis must not rescan the full op list for every candidate load.",
  "Adjacent-load chain detection behavior must remain covered across parent and child bodies.",
]
proof = [
  "vec_pack analysis now builds a result-id-to-u32-literal table once per body and groups LoadGlobal candidates by slot before per-slot sorting.",
  "The documented complexity is now O(ops + candidates log candidates) per body instead of O(ops^2).",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower vec_pack -> 9 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Make global-memory coalescing analysis axis-aware for invocation id indexes.",
]
proof_required = [
  "Only x-axis LocalInvocationId/GlobalInvocationId indexes should be treated as unit-stride coalesced at descriptor level.",
  "y/z-axis invocation ids must not silently inherit x-lane coalescing assumptions.",
]
proof = [
  "coalesce::classify_index now routes invocation-id producers through classify_invocation_id and treats only axis 0 as CoalescedUnitStride.",
  "global_invocation_y_axis_is_not_unit_stride_x_coalesced proves GlobalInvocationId.y is Scattered.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower coalesce -> 31 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Make shared-memory bank-conflict analysis axis-aware for invocation id indexes.",
]
proof_required = [
  "Only x-axis invocation ids should be treated as no-conflict lane-contiguous shared-memory indexes.",
  "y/z-axis invocation ids must be classified conservatively so padding decisions are not based on false no-conflict assumptions.",
]
proof = [
  "bank_conflict::classify_index now routes invocation-id producers through classify_invocation_id and treats only axis 0 as NoConflict.",
  "local_invocation_y_axis_is_unknown_not_x_lane_no_conflict proves LocalInvocationId.y is Unknown.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower bank_conflict -> 32 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove repeated producer scans from shared-memory promotion candidate collection.",
]
proof_required = [
  "shared_mem_promote must not rfind through the full body for every candidate LoadGlobal index.",
  "Promotion behavior for repeated global/local x tile loads must remain covered.",
]
proof = [
  "shared_mem_promote now builds one result-id producer map per body and uses it in classify_tile_index.",
  "Readonly global slot membership now uses FxHashSet for candidate filtering.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower shared_mem_promote -> 25 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove repeated producer and operand-use scans from bank-conflict padding planning.",
]
proof_required = [
  "bank_conflict_pad must not rfind producers repeatedly while parsing shared-memory index expressions.",
  "bank_conflict_pad must not rescan every op to count literal uses for every candidate stride literal.",
]
proof = [
  "bank_conflict_pad now builds a PlanCtx per body with result-id producers and operand-use counts.",
  "parse_stride_index/parse_mul_stride and literal safety checks now use PlanCtx lookups.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower bank_conflict_pad -> 4 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Replace quadratic dead-store forward scans with a single-pass pending-store table.",
]
proof_required = [
  "dead_store must not scan forward from every store to find the next overwrite.",
  "Conservative invalidation at loads, barriers, control-flow, atomics, async ops, traps, returns, and opaque calls must remain intact.",
]
proof = [
  "dead_store now tracks pending StoreGlobal/StoreShared ops by address-space, slot, and index, marking the previous pending store dead when the same key is overwritten.",
  "Loads invalidate pending stores only for their address space; barriers/control-flow/atomics/async/trap/return/opaque calls invalidate all pending stores.",
  "interleaved_same_address_stores_keep_only_last_per_address proves the single-pass table eliminates overwritten interleaved stores while keeping other addresses.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower dead_store -> 8 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 196 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower --test kitchen_sink_snapshot -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove repeated producer scans from workgroup-uniform branch analysis.",
]
proof_required = [
  "Workgroup-uniform dependency closure must not rfind through the body for every operand in nested branch conditions.",
  "Uniform/divergent/unknown branch classification behavior must remain covered.",
]
proof = [
  "workgroup_uniform analysis now builds one result-id producer map per body and reuses it through condition dependency traversal.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower workgroup_uniform -> 11 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 196 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Use hash-set slot membership in constant-buffer promotion rewrite.",
]
proof_required = [
  "const_buffer_promote slot membership checks must not pay tree lookup costs when ordering is irrelevant.",
  "Promotion and write-veto behavior must remain unchanged.",
]
proof = [
  "const_buffer_promote now uses FxHashSet for candidate/promoted slot membership and LoadGlobal rewrite checks.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower const_buffer_promote -> 13 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 196 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Use hash-map access counters in shared-memory promotion analysis.",
]
proof_required = [
  "shared_mem_promote analysis must not pay ordered-map costs for per-binding load counts.",
  "Nested-body load counting and promotion candidate behavior must remain unchanged.",
]
proof = [
  "shared_mem_promote analysis now uses FxHashMap for binding-slot load counts and tests use FxHashMap for lookup-only assertions.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower shared_mem_promote -> 25 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 196 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "optimizer"]
scope = [
  "Remove repeated producer scans from coalescing and bank-conflict recursive index classification.",
]
proof_required = [
  "Memory-pattern analyses must not rfind through the full body for every recursive Add/Mul/index classification step.",
  "Existing classification behavior and axis precision must remain covered.",
]
proof = [
  "coalesce analysis now builds one result-id producer map per body and reuses it through recursive classify_index/classify_add/classify_mul.",
  "bank_conflict analysis now builds one result-id producer map per body and reuses it through recursive classify_index/classify_add/classify_mul.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower coalesce -> 31 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower bank_conflict -> 32 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower rewrites -> 195 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["lowering", "emit_naga", "driver_wgpu"]
scope = [
  "Q7 reopen: loop-carried outer-scope assignments produce dangling SSA references at the descriptor layer. The wgpu fixture buf_len_loop_bound_survives_pre_lowering and fnv1a64_shaped_loop_runs_three_times_for_twelve_byte_input regression-test this gap and currently fail (got 1, expected 3) on a 12-byte input.",
]
proof_required = [
  "vyre-lower must lower `Node::Assign { name }` inside a Loop body that targets an outer-scope name into a carrier the emitter can model as a mutable WGSL/PTX local: descriptor-level options are (a) a new `KernelOpKind::MutableLocal { slot }` + `LoadLocal` / `StoreLocal` pair that vyre-emit-naga maps to `Naga::LocalVariable` + `Statement::Store`, (b) reusing a 1-element workgroup `Shared` binding as the carrier when running on backends without function locals.",
  "After fix, descriptor emitted for `let seen=0; loop(i, 0..buf_len(input)) { seen = seen + 1 }; store(out[0], seen)` must NOT have the parent-body StoreGlobal referencing a result id defined in the loop child body  -  that is the dangling-SSA shape that produced got=1 instead of expected=3.",
  "Buf_len_array_length tests buf_len_loop_bound_survives_pre_lowering and fnv1a64_shaped_loop_runs_three_times_for_twelve_byte_input must pass on real wgpu dispatch (5090).",
]
proof = [
  "2026-05-09 sweep: buf_len_loop_bound_survives_pre_lowering ... ok",
  "2026-05-09 sweep: fnv1a64_shaped_loop_runs_three_times_for_twelve_byte_input ... ok",
  "2026-05-09 sweep: fnv1a64_shaped_loop_runs_once_for_one_byte_input ... ok",
  "vyre-lower/src/lower.rs lines 389-404: post-loop LoopCarrier read in parent body rebinds outer-scope name to a parent-body SSA id, ending the dangling-id chain.",
]

notes = [
  "Diagnosed by main-cc on 2026-05-02. Repro built in /tmp/bdump showed the descriptor body's StoreGlobal operand `id 8` is the result of `seen+1` inside the loop's child body, not the parent body.",
  "Pre-lowering pipeline (canonicalize/region_inline/const_fold/loop_strip_mine/loop_unroll/strength_reduce/normalize_atomics/CSE/DCE/IfConstantBranchEliminate) is innocent: original Program identical to post-pre-lowering Program at the IR level; bug is in vyre-lower's Node::Assign-inside-Loop scope mechanics rather than the optimizer pipeline.",
  "scope.rs `restore_loop_exit` propagates the inner-body result id of `seen` to the outer scope, but the inner result id has no parent-body definition  -  the descriptor needs an explicit carrier op kind, or lowering must hoist a Shared binding to materialise the carrier at the descriptor layer.",
  "Touches Codex's hold zone (vyre-lower/, vyre-emit-naga/). Flagged here so Q7 reopens; main-cc keeps the regression test green via this claim's proof_required gate.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "runtime", "performance"]
scope = [
  "Make compiled persistent bind-group cache keys stable across pooled GPU-buffer reacquire cycles.",
]
proof_required = [
  "Repeated dispatches through the compiled persistent path must hit the bind-group cache when the persistent pool reuses the same backing allocation.",
  "Bind-group cache keys must include the logical binding range so a reused allocation with a different requested size cannot reuse a stale bind group.",
]
proof = [
  "Persistent bind-group key construction now uses each handle's backing allocation identity plus padded logical byte length, matching the actual wgpu binding range instead of transient handle ids.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_reused_on_repeated_dispatches -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "runtime", "performance"]
scope = [
  "Remove per-group handle cloning and heap Vec binding-entry staging from pre-recorded persistent dispatch setup.",
]
proof_required = [
  "Pre-recording must build bind-group cache keys from borrowed handles without cloning GPU buffer handles.",
  "Bind-group entries for typical shader groups must stay inline instead of allocating a heap Vec.",
]
proof = [
  "prerecord_persistent_dispatch now builds SmallVec cache-key parts directly from borrowed handles and uses SmallVec bind-group entry storage.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["runtime", "megakernel", "performance"]
scope = [
  "Remove ordered-map costs from whole-megakernel cross-arm redundancy detection.",
]
proof_required = [
  "Cross-arm redundancy detection must not pay BTreeMap comparison costs for order-independent WorkItem triples.",
  "The detector must pre-size its lookup table from the arm workload instead of growing repeatedly.",
]
proof = [
  "detect_cross_arm_redundancy now uses an FxHashMap sized to the total WorkItem count and keeps the same first-seen semantics.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime whole_megakernel -> 10 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "pipeline", "performance"]
scope = [
  "Remove ordered-set costs from wgpu pipeline metadata derivation.",
]
proof_required = [
  "Public-output binding membership and trap-tag dedupe must not pay BTreeSet ordering costs in pipeline compile setup.",
  "Workspace commands must parse after the release bench target is declared.",
]
proof = [
  "Wgpu pipeline descriptor binding derivation and trap-tag dedupe now use FxHashSet for order-independent membership checks.",
  "Restored the declared release bench target with a Criterion registry-inventory benchmark so workspace parsing succeeds.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["core", "optimizer", "performance"]
scope = [
  "Make the public optimize fingerprint cache O(1) instead of a linear FIFO scan.",
]
proof_required = [
  "Repeated vyre::optimize calls must not linearly scan up to OPTIMIZE_CACHE_CAPACITY entries for every lookup.",
  "FIFO capacity behavior and fingerprint-equivalence behavior must remain unchanged.",
]
proof = [
  "optimize_cache now stores [u8; 32] fingerprints in a HashMap with a FIFO queue only for eviction order.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre optimize_ -> 3 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "dispatch", "performance"]
scope = [
  "Reuse output binding lookup state in the wgpu record/readback hot path.",
]
proof_required = [
  "Output buffer allocation during command recording must not linearly scan output metadata for every output binding.",
  "The lookup storage must reuse dispatch scratch capacity across calls.",
]
proof = [
  "DispatchScratch now owns an output_idx_by_binding BindingLookup, populated once per record call and reused across dispatches.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu scratch_retains_capacity_across_dispatches -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "pipeline", "performance"]
scope = [
  "Keep small compound-dispatch request staging inline.",
]
proof_required = [
  "Compound dispatch wrappers must not heap-allocate a Vec just to pass a short borrowed request slice into the recorder.",
  "Existing compound dispatch behavior must remain unchanged.",
]
proof = [
  "dispatch_coalesced and dispatch_compound_v2 now stage borrowed requests in SmallVec<[...; 8]> before calling dispatch_compound_borrowed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver", "cache", "performance"]
scope = [
  "Avoid full sorting in cache heat eviction when only a subset is dropped.",
]
proof_required = [
  "Cache eviction must not sort every cache entry when capacity pressure only needs the coldest K entries.",
  "Tied heat must remain deterministic by original input order.",
]
proof = [
  "entries_to_evict now select_nth_unstable partitions at the eviction count and sorts only the eviction slice with the same heat/index comparator.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver cache_eviction_heat -> 6 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver", "routing", "performance"]
scope = [
  "Make shape-prediction history allocation-free.",
]
proof_required = [
  "Predicted-next-shape history must not allocate a VecDeque for its fixed 16-entry window.",
  "Repeat, cycle, cap, and latest semantics must remain unchanged.",
]
proof = [
  "ShapeHistory now uses a fixed [ShapeFingerprint; MAX_HISTORY] circular buffer with start/len indices.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver shape_prediction -> 7 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "cache", "performance"]
scope = [
  "Wire shared heat-decay eviction into the real wgpu pipeline cache.",
]
proof_required = [
  "The wgpu runtime pipeline cache must not keep using the older gain-retention selector while the shared heat policy sits unused.",
  "Eviction must account for both entry-count and byte budgets and update recency on every cache hit.",
]
proof = [
  "LruPipelineCache entries now track last-hit time, refresh it on get(), and choose eviction keys through vyre_driver::cache_eviction_heat::entries_to_evict.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu pipeline_cache_eviction -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "pipeline", "performance"]
scope = [
  "Wire predicted-next-shape prefetch into the wgpu async dispatch wait path.",
]
proof_required = [
  "ShapeHistory must be consumed by a real dispatch path, not left as an unused prediction primitive.",
  "The GPU wait window must precompile the predicted next pipeline cache key before blocking on readback.",
]
proof = [
  "WgpuBackend now records shape fingerprints per async dispatch, keeps a fingerprint->Program/config cache, and attaches a PipelinePrefetch to WgpuPendingDispatch.",
  "WgpuPendingDispatch runs the predicted pipeline compile before awaiting readback, warming the existing wgpu pipeline cache while the submitted GPU work is in flight.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu pipeline_cache_eviction -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver", "driver_wgpu", "binding", "performance"]
scope = [
  "Wire shared backend-layout fingerprints into the wgpu bind-group layout cache.",
]
proof_required = [
  "Binding-set merging must use a shared descriptor-layout fingerprint, not a wgpu-only ad hoc cache key.",
  "Compatible programs must reuse wgpu BindGroupLayout objects across compiled pipeline artifacts.",
]
proof = [
  "vyre-driver now exposes BackendLayoutSlot/BackendLayoutClass/BackendLayoutFingerprint as the shared descriptor-layout cache key.",
  "WgpuBackend owns a DashMap<BackendLayoutFingerprint, Arc<[Arc<wgpu::BindGroupLayout>]>> and WgpuPipeline::compile_with_device_queue reuses layouts from it.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver backend_layout_fingerprint_sorts_slots -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "dispatch", "performance"]
scope = [
  "Collapse wgpu batched persistent-dispatch staging from three vectors to one item vector.",
]
proof_required = [
  "Batched compiled-pipeline dispatch must not allocate separate input-ref, output-ref, and item vectors just to describe one submission.",
  "Borrowed handle lifetimes must remain explicit and no handle clones may be introduced.",
]
proof = [
  "BorrowedDispatchItem now owns inline SmallVec handle-reference lists, so batched dispatch builds one Vec<BorrowedDispatchItem> instead of input_refs + output_refs + items.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "pipeline_cache", "performance"]
scope = [
  "Tighten disk pipeline-cache flush and metadata-read allocation behavior.",
]
proof_required = [
  "Pipeline-cache metadata reads must pre-size their string buffer from file metadata.",
  "Durability registration must not silently skip pending fsync paths on a poisoned cache lock.",
]
proof = [
  "read_metadata now uses String::with_capacity(metadata.len()), flush parent collection reserves paths.len(), and write_atomic propagates pending-durability lock errors.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu disk_cache -> 15 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "binding", "performance"]
scope = [
  "Avoid pre-probe bind-group cache key cloning on cache misses.",
]
proof_required = [
  "BindGroupCache must not clone the full handle key before knowing whether the entry exists.",
  "LRU generation updates must remain correct on cache hits.",
]
proof = [
  "BindGroupCacheInner::touch_existing now probes entries by &BindGroupCacheKey and clones only when pushing the hit into the LRU queue.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache -> 2 passed.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["libs_parsing", "foundation_optimizer"]
scope = [
  "GPU C compiler integration gate: vyre-frontend-c::compile_c11_sources end-to-end pipeline (lex -> digraph -> keyword -> preprocessor -> VAST -> sema -> AST->pg_nodes -> codegen -> ELF) currently fails at stage A (c11_lexer) with naga compile-time-eval underflow `0u - 1u` in WGSL line 65:62 (`haystack[(0u - 1u)]`). One downstream gate blocks the entire vs-CPU headline demo (compile real .c file -> ELF .o on GPU vs `clang -c -O0`).",
]
proof_required = [
  "Identify whether `0u - 1u` is emitted by the lexer Program builder (libs_parsing/c/lex/) or const-folded into existence by vyre-foundation::optimizer (foundation_optimizer pre_lowering passes folding `i - 1` when i is loop-invariant 0). If the second, the optimizer must guard against folding to a literal that fails Naga's static eval.",
  "After fix, vyre-frontend-c/tests/linux_tu_object_pipeline.rs::linux_shaped_tu_produces_elf_with_all_pipeline_sections must pass end-to-end on the 5090 wgpu backend and produce a valid ELF64 ET_REL.",
]
proof = [
  "2026-05-09: vyre-frontend-c::linux_tu_object_pipeline::linux_shaped_tu_produces_elf_with_all_pipeline_sections passes (3 passed in 1.26s on real wgpu).",
]

notes = [
  "Diagnosed by main-cc on 2026-05-02 while scoping the vs-CPU comparison demo. The discovery point matters: vyre-frontend-c is a near-complete GPU C compiler  -  lex / digraph / keyword / preprocess / VAST / sema / AST→pg_nodes / codegen / object emit / ELF link  -  wired through real backend dispatch via vyrec. Every stage has a Program; vyre::backend::acquire_preferred_dispatch_backend feeds them the live wgpu adapter. One naga validation gate at the lexer stage is the only thing between the current state and 'compile a real .c file end-to-end on GPU and link it into a runnable Linux binary.'",
  "Cleared ~/.cache/vyre/pipeline/ and re-ran; bug reproduces post-cache-clear so it is in the lexer Program builder or the optimizer pipeline, not stale disk-cached WGSL.",
  "Touches Codex's hold zone (vyre-libs/parsing/) plus CC's lane (vyre-foundation/optimizer/passes). Whichever side owns the underflow, the fix is small  -  likely 1-5 LOC plus a guard test.",
]

[[claim]]
owner = "main-cc"
date = "2026-05-02"
status = "done"
lanes = ["foundation", "libs_parsing", "lower"]
scope = [
  "GPU C compiler integration gate progress: stages A (c11_lexer) and B (c11_lex_digraphs) now pass after two real fixes. Stage C (c_keyword) hits the structural Q7 loop-carried outer-var SSA gap, the same bug class documented in the buf_len_loop_bound regression  -  keyword pass reassigns `hash` inside `loop_for(i, 0, len, [Node::assign(hash, ...)])` and reads it after the loop, producing dangling SSA refs at WGSL emit time (`no definition in scope for identifier _e37`).",
]
proof_required = [
  "Stage A fixed: vyre-foundation::Expr::saturating_sub helper added (compiles to `a - min(a, b)` so naga's static evaluator never sees a literal underflow inside an Expr::select arm). Five lexer/digraph call sites in vyre-libs/parsing/c/lex/ migrated.",
  "Stage B fixed: vyre-lower::rewrites::drop_unused_bindings now retains ALL ReadWrite bindings, not just those whose name matches the output_contract_name pattern. ReadWrite is part of the host dispatch contract: multi-stage pipelines pass tok_starts/tok_lens through stages that don't touch them. drop_unused_bindings::tests::unused_slot_dropped fixture migrated to ReadOnly for the unreferenced slot.",
  "Stage C blocked on Q7 (loop-carried outer-var SSA). Real structural fix: vyre-lower must allocate a Naga LocalVariable carrier per outer-scope name reassigned in a loop body OR emit explicit Load/Store ops to a dedicated Shared binding. Codex's lane (vyre-lower scope mechanics + vyre-emit-naga StructuredForLoop emit). Until Q7 closes, every multi-stage pipeline that uses fnv-style accumulator-in-loop patterns will fail at this same emit gate.",
]
proof = [
  "2026-05-09: vyre-frontend-c::linux_tu_object_pipeline::linux_shaped_tu_produces_elf_with_all_pipeline_sections passes (3 passed in 1.26s on real wgpu).",
]

notes = [
  "End-to-end progress: stage A reproduces the Q3 binding-mismatch fix (saturating_sub for index arithmetic in Expr::select arms)  -  applies to any future Program builder that uses guarded subtraction without a saturating helper.",
  "drop_unused_bindings change is a behavioral tighten: previously a multi-stage pipeline declaring ReadWrite bindings the body did not touch silently lost those slots through the rewrite stack, breaking host dispatch ABI on the FIRST cache-cold run (the WGSL-cache-bypassing path). The test rename + helper migration documents the new contract.",
  "Q7 is now blocking the headline GPU C compiler demo end-to-end. Reopening with this stronger evidence + the keyword-pass repro on top of the existing buf_len repro.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_wgpu", "readback", "performance"]
scope = [
  "Remove per-output metadata clones from submitted wgpu readback maps.",
]
proof_required = [
  "Submitted and pending readback queues must not clone OutputBindingLayout for every output buffer.",
  "Pending readbacks must keep output metadata alive without borrowing the dispatch request.",
  "dispatch_wgsl extension path must use the same shared-output metadata shape as compiled pipelines.",
]
proof = [
  "SubmittedMap/PendingMap now carry Option<usize> output indices; WgpuPendingReadback owns one Arc<[OutputBindingLayout]> shared across all output maps.",
  "RecordAndReadback accepts &Arc<[OutputBindingLayout]> so compiled pipelines reuse the existing output metadata Arc; dispatch_wgsl wraps its one-off output metadata in the same Arc shape.",
  "Removed the now-unused output_contract_name helper after ReadWrite binding retention became unconditional.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-wgpu bind_group_cache_shared_per_compiled_shader -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "autotune", "performance"]
scope = [
  "Close ROADMAP.md R7 and remove per-byte heap formatting from autotune keys.",
]
proof_required = [
  "Autotune DB writes must be cross-process fenced so concurrent dispatch workers do not tear the TOML file.",
  "Spec/autotune key construction must not allocate one temporary formatted string per digest byte.",
]
proof = [
  "AutotuneStore::save_if_dirty already takes an exclusive fs2 file lock and syncs the rewritten file under that lock; ROADMAP.md R7 now reflects the shipped code.",
  "AutotuneKey::new now uses write! into a pre-sized String instead of push_str(format!(...)) for every digest byte.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver autotune_store -> 10 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "dispatch_policy", "performance"]
scope = [
  "Remove tree allocations from dispatch arm-independence and async-copy overlap summaries.",
]
proof_required = [
  "Dispatch policy summaries must not allocate BTreeSet nodes for the common tiny binding-set case.",
  "Concurrent-dispatch, cross-pipeline fusion, and async-copy overlap verdicts must stay identical.",
]
proof = [
  "ArmBindingSummary now uses BindingSlotSet, an inline sorted SmallVec set with sorted unique insert, binary-search contains, and smaller-set intersection.",
  "async_copy_overlap, dispatch_policy, and pipeline_fusion continue constructing summaries through FromIterator without ordered-tree storage.",
  "The registry migration example was converted from an ignored stale doctest to a compiling public-API doctest.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver -- --ignored -> 0 ignored doctests remain and the previously ignored migration doctest compiles.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver -> 273 lib tests plus integration/doctests passed with 0 ignored.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "observability", "dispatch_policy"]
scope = [
  "Close ROADMAP.md R4 by wiring VYRE_TRACE=1 substrate audit events into real dispatch-policy decisions.",
]
proof_required = [
  "The trace surface must expose which optimization fired and the saved/expected nanoseconds.",
  "Normal dispatch must not allocate trace events unless VYRE_TRACE is enabled.",
  "The trace must be consumable as human-readable text and Prometheus metrics.",
]
proof = [
  "DriverObservability now includes bounded SubstrateAuditEvent snapshots plus to_audit_log() and vyre_driver_substrate_audit_saved_ns Prometheus output.",
  "record_substrate_audit_event returns immediately when VYRE_TRACE is not truthy, so the default hot path does not enqueue audit events.",
  "evaluate_dispatch_policy records audit events for persistent-kernel queue batching, CUDA/wgpu graph reuse, bindless descriptor arrays, and trace-JIT speculation.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver -> 274 lib tests plus integration/doctests passed with 0 ignored.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "pipeline", "reproducibility"]
scope = [
  "Close ROADMAP.md R3 by attaching reproducibility manifests to compiled pipeline builds and prewarm reports.",
]
proof_required = [
  "Every telemetry compile/prewarm result must carry a JSON-serializable manifest with backend id, pipeline id, normalized Program digest, dispatch policy, and cache status.",
  "The manifest must use canonical normalized pipeline digest bytes, not declaration-order raw wire bytes.",
]
proof = [
  "PipelineReproManifest added to vyre-driver::pipeline with schema, backend_id, pipeline_id, program_digest, dispatch_policy, cache_hit, and to_json().",
  "compile_shared_with_telemetry builds the manifest from normalized_program_cache_digest(program), dispatch_policy_cache_string(config), backend.id(), pipeline.id(), and cache telemetry.",
  "prewarm_shared forwards the same manifest through PipelinePrewarmReport.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver -> 274 lib tests plus integration/doctests passed with 0 ignored.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["vyre_core", "driver_shared", "optimizer"]
scope = [
  "Close ROADMAP.md R2 with a device-aware public optimization surface.",
]
proof_required = [
  "Consumers must have one public vyre API for conservative optimization and a live-device variant that consumes shared DeviceProfile facts.",
  "Device-aware optimization must not reuse the conservative cache key when adapter tuning can change the Program.",
]
proof = [
  "vyre::optimize_for_device(program, &DeviceProfile) runs Autotune::transform_for_adapter(profile.adapter_caps()) before vyre::optimize.",
  "vyre::optimize_for_backend(program, backend) derives the neutral DeviceProfile from VyreBackend::device_profile and calls optimize_for_device.",
  "Device optimize results are cached in a separate profile-keyed cache that includes Program fingerprint and capability/workgroup/subgroup/tuning fields.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre optimize_for_device -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["driver_shared", "foundation_optimizer", "performance"]
scope = [
  "Close ROADMAP.md N1 with shared device-conditioned e-graph extraction.",
]
proof_required = [
  "Extraction from one saturated EGraph must be parameterized by DeviceProfile, autotune records, trace-JIT counters, and hot-path state.",
  "The implementation must live in shared driver code so concrete backends do not duplicate extractor cost plumbing.",
  "The extractor must safely return no variant for an invalid root instead of panicking through the foundation extractor.",
]
proof = [
  "vyre-driver::device_extraction adds ExtractionDevice, DeviceExtraction, extract_best_for_device, and extract_best_for_devices.",
  "ExtractionDevice carries DeviceProfile, Option<&AutotuneRecord>, Option<TraceJitInputs>, and hot_path; extraction combines device_aware_cost with autotune/trace-JIT context bias.",
  "extract_best_for_devices extracts several backend variants from the same saturated EGraph without re-saturation.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver device_extraction -> 6 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_shared", "runtime_megakernel", "performance"]
scope = [
  "Close ROADMAP.md N2 by moving conservative-vs-speculative variant winner selection, autotune recording, and runtime paired-sample scheduling into shared driver/runtime code.",
]
proof_required = [
  "The decision must compare compile plus dispatch timing, not dispatch timing alone.",
  "The winning variant must write its AutotuneRecord under the matching side-cache SpecCacheKey.",
  "Ties must keep the conservative variant to avoid speculative cache churn.",
  "The megakernel runtime must feed paired conservative/speculative measurements into both the variant recorder and the accumulated N2 adoption verdict.",
]
proof = [
  "vyre-driver::speculate adds SpeculativeVariantKeys, SpeculativeVariantRace, SpeculativeVariantDecision, SpeculativeVariantKind, and record_speculative_variant_race.",
  "SpeculativeVariantRace totals compile/cache-miss cost plus dispatch cost for both variants before choosing a winner.",
  "record_speculative_variant_race writes the winner's AutotuneRecord to AutotuneStore using AutotuneKey::new(winning SpecCacheKey, adapter_id).",
  "vyre-runtime::megakernel::speculation adds PairedSpeculationWindow::record_sample, which records each paired timing sample through record_speculative_variant_race and feeds running means into decide_speculation.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver speculate -> 22 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime paired_window --lib -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-02"
status = "done"
lanes = ["foundation_optimizer", "lower_emit", "tooling"]
scope = [
  "Advance ROADMAP.md N3 by adding typed SMT-LIB proof obligations for optimizer rewrite contracts.",
]
proof_required = [
  "Rewrite proof artifacts must be deterministic and solver-consumable.",
  "The proof shape must check the negation of preconditions-imply-equivalence so UNSAT proves the rewrite.",
  "Variables must be declared with stable ordering and typed SMT sorts.",
]
proof = [
  "vyre-foundation::optimizer::rewrite_proof adds ProofSort, ProofExpr, and RewriteProofObligation.",
  "RewriteProofObligation::to_smt2 emits QF_BV SMT-LIB with sorted declarations, precondition assertions, negated equivalence, and check-sat.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation rewrite_proof -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["lower_emit", "performance"]
scope = [
  "Close ROADMAP.md N6 by adding the descriptor-level tail-mask rewrite consumed after launch-side power-of-two grid coercion.",
]
proof_required = [
  "The rewrite must predicate observable stores with index < logical element count when the launcher rounds dispatch lanes up.",
  "The rewrite must be backend-neutral and must not add a dependency from vyre-lower back to concrete drivers or vyre-driver.",
  "The rewritten descriptor must still pass the descriptor verifier.",
]
proof = [
  "vyre-lower::rewrites::tail_mask::apply_tail_mask wraps StoreGlobal/StoreShared ops indexed by GlobalInvocationId.x in a StructuredIfThen guarded by BinOp::Lt(index, logical_element_count).",
  "The pass is opt-in and takes the logical count directly, so vyre-driver remains the launch-policy owner while vyre-lower owns descriptor safety.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower tail_mask --lib -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "driver_shared", "performance"]
scope = [
  "Close ROADMAP.md N4 by adding runtime cross-pipeline fusion segment planning on top of the shared driver decision.",
]
proof_required = [
  "The shared driver layer remains the source of truth for pairwise fusion legality.",
  "The runtime layer must turn a full adjacent pipeline sequence into concrete launch segments and preserve break reasons for telemetry.",
  "The implementation must avoid backend-specific names and be reusable by wgpu, CUDA, and future emitters.",
]
proof = [
  "vyre-runtime::megakernel::planner::plan_cross_pipeline_fusion consumes &[ArmBindingSummary] and emits CrossPipelineFusionPlan { segments, breaks }.",
  "Each adjacent decision delegates to vyre_driver::pipeline_fusion::decide_cross_pipeline_fusion; runtime owns only grouping and telemetry shape.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime cross_pipeline --lib -> 4 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_shared", "lower_emit", "performance"]
scope = [
  "Close ROADMAP.md G1/G5 lower_emit contract by turning foundation precision hints into concrete shared strategy decisions.",
]
proof_required = [
  "F16 hints must only select F16 lowering when the backend capability exists.",
  "Transcendental polynomial hints must produce a deterministic backend-neutral emit plan with an explicit degree.",
  "DeviceProfile must project live backend capabilities into the strategy gate so emitters do not hardcode policy.",
]
proof = [
  "vyre-driver::strategy adds PrecisionLoweringPlan and select_precision_lowering for PrecisionHint::{F16Eligible, TranscendentalPolynomial}.",
  "DeviceProfile::strategy_capabilities maps supports_f16 to has_native_f16 and exposes has_transcendental_polynomial_emit through the shared capability record.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver strategy --lib -> 7 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["libs_parsing", "performance"]
scope = [
  "Close ROADMAP.md E2/L2 by wiring the shared ParsedSourceLru substrate into C11 and Python pipeline entry points.",
]
proof_required = [
  "C and Python pipelines must call get_or_parse through a language-owned entry point instead of leaving the cache as a generic unused substrate.",
  "Build flags/options must participate in the cache key so identical source bytes with different language settings do not alias.",
  "The cached value must be Arc-owned and reused by content, not by caller allocation identity.",
]
proof = [
  "vyre-libs::parsing::c::pipeline::source_cache adds C11PipelineCache and get_or_build_c11_pipeline(source, extra), caching the fused lexer/function/call Program bundle.",
  "vyre-libs::parsing::python::source_cache adds Python312PipelineCache and get_or_build_python312_pipeline(source, extra), caching lexer/structure/import/call Program bundles.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-libs --features parsing source_cache --lib -> 13 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["lower_emit", "driver_cuda", "performance"]
scope = [
  "Close ROADMAP.md B6 generic-loop visibility by exposing structured-loop matmul tile inference beside MatrixMma promotion.",
]
proof_required = [
  "The fragment matcher used by the rewrite and loop inference must be shared so CUDA/wgpu/future emitters do not duplicate tensor-core detection.",
  "StructuredForLoop bodies containing M16N8K16-shaped FMA fragments must be machine-auditable with stable location metadata.",
  "The inference path must avoid cloning op streams in the hot analysis path.",
]
proof = [
  "vyre-lower::rewrites::matmul_promote exports MatmulTileLoopPlan and infer_matmul_tile_loops.",
  "The matcher was factored into match_fragment_at, reused by try_promote_at and promotable_fma_starts without cloning the op slice.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower matmul --lib -> 8 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["foundation_optimizer", "lower_emit", "performance"]
scope = [
  "Close ROADMAP.md J1 by turning AoS-to-SoA analysis into a concrete descriptor binding split plan, not only advisory hints.",
]
proof_required = [
  "The split plan must allocate stable scalar component bindings after the existing descriptor slot range.",
  "Component types must come from explicit vector/tensor-shaped element metadata and must not be fabricated from raw byte-array sizes.",
  "The advisory hint path must remain available for emitters that cannot change ABI at that call site.",
]
proof = [
  "vyre-lower::rewrites::aos_to_soa_promote now exposes SoaBindingSplitPlan and plan_binding_splits(desc).",
  "plan_binding_splits emits deterministic component BindingSlot values with scalar element types and stable names derived from the source binding.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-lower aos_to_soa --lib -> 15 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["foundation_optimizer", "organization", "performance"]
scope = [
  "Close ROADMAP.md A3 by making Region/source metadata a side-table-backed optimizer view, not only recorded provenance.",
]
proof_required = [
  "Optimizer passes that do not care about provenance need a regionless preorder scan.",
  "Scope queries must skip Region wrappers without skipping real structural parents.",
  "The canonical wire enum must remain compatible while the optimizer view treats Region metadata as side-table payload.",
]
proof = [
  "ProgramFacts now exposes iter_regionless_nodes() and regionless_parent_of(NodeIndex).",
  "RegionMeta, region_at(), and regions_by_generator() remain the provenance restoration path for diagnostics and identity tracking.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-foundation regionless --lib -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["self_substrate", "runtime_megakernel", "performance"]
scope = [
  "Harden and optimize megakernel matroid self-substrate schedulers after the roadmap sweep.",
]
proof_required = [
  "User-shaped scheduler input must return structured errors instead of panicking on invalid vector lengths or n*n overflow.",
  "The fast bounded scheduler must avoid scanning every non-frontier row in the exchange graph.",
  "The exact certification scheduler must avoid double hashing packed states in its cycle detector.",
]
proof = [
  "matroid_megakernel_scheduler now returns Result<Vec<u32>, MatroidScheduleError> and max_fusion_subset_into returns Result<(), MatroidScheduleError>.",
  "matroid_exact_megakernel now returns Result through owned and scratch APIs with ExactMatroidError for adjacency/source/sink/seed shape violations.",
  "The bounded BFS loop iterates frontier rows first; the exact solver uses HashSet::insert as the single cycle check.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-self-substrate matroid --lib -> 19 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-test-harness --test self_consumer_matrix -> 12 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "performance"]
scope = [
  "Upgrade megakernel publication, scheduler metadata, and telemetry hot paths.",
]
proof_required = [
  "Window publication must avoid building an intermediate BatchDescriptor and must preflight bounds before mutating the ring.",
  "Priority partition offset callers need a fixed-array path so hot control-buffer writers do not allocate a Vec.",
  "Telemetry Count-Min sketches must support caller-owned reuse for high-frequency supervisor polling.",
]
proof = [
  "WindowDescriptor::publish_into streams required/lookahead payloads directly into Megakernel::publish_slot with one reusable args Vec and overflow preflight.",
  "scheduler::default_priority_offsets_array returns [u32; PRIORITY_LEVELS + 1] while default_priority_offsets remains the compatibility Vec wrapper.",
  "CountMinSketch gained clear/reset_shape and RingTelemetry::sketch_into fills SketchTelemetryScratch without reallocating unchanged counter tables.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime window --lib -> 8 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime default_offsets --lib -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime sketch --lib -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "performance"]
scope = [
  "Reduce megakernel protocol batch-publication validation cost.",
]
proof_required = [
  "Batch publication must not revalidate ring byte geometry for every slot in the batch.",
  "The single-slot API must retain the same validation and error behavior.",
  "Fence publication must use the same validated geometry as work-item publication.",
]
proof = [
  "protocol_api.rs now has validate_ring_publish_view and publish_slot_validated.",
  "publish_slot remains the public single-slot validator; batch_publish validates once and reuses RingPublishView for every item plus BATCH_FENCE.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime batch_publish --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel --lib -> 148 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "planner", "quality"]
scope = [
  "Make megakernel fusion selector shape failures explicit instead of silent empty selections.",
]
proof_required = [
  "Runtime callers need checked selector APIs that report malformed cost/adjacency inputs.",
  "Compatibility wrappers must preserve the historical empty-selection behavior on malformed inputs.",
  "The checked APIs must be exported from megakernel::planner so drivers can use them.",
]
proof = [
  "planner::fusion adds FusionSelectionError, select_fused_subset_checked_into, and select_fused_subset_compact_checked_into.",
  "planner::mod re-exports the checked APIs and error type.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime checked_selector --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel --lib -> 149 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "performance"]
scope = [
  "Upgrade megakernel slot publish and readback reuse paths.",
]
proof_required = [
  "Normal slot publication should clear the argument payload as one contiguous byte range instead of rewriting every word.",
  "Readback dispatch must offer caller-owned storage for supervisors that poll megakernel state repeatedly.",
  "The new readback path must preserve strict ABI validation.",
]
proof = [
  "publish_slot_validated now zero-fills the ARG0 payload byte range once and writes only supplied args.",
  "MegakernelReadback derives Default and exposes from_outputs_into.",
  "Megakernel::dispatch_with_io_queue_readback_borrowed_into dispatches through caller-owned OutputBuffers, validates into caller-owned MegakernelReadback, and returns stats.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime publish --lib -> 22 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime readback_borrowed_into --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel --lib -> 150 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "performance"]
scope = [
  "Second megakernel runtime upgrade slice: allocation-free barrier access sets and cleaner launch cache eviction.",
]
proof_required = [
  "Barrier elision independence checks must avoid heap hash sets for common small arm-access sets.",
  "Launch recommendation cache updates must not duplicate FIFO order entries for the same key.",
  "The megakernel-focused runtime suite must stay green after the changes.",
]
proof = [
  "planner::barriers AccessSet now uses SmallVec<[&Ident; 8]> with duplicate suppression instead of FxHashSet allocation per candidate barrier.",
  "policy::LaunchRecommendationCache::insert updates existing keys in place and only appends order entries for new keys.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime barrier --lib -> 4 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime launch_cache --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel --lib -> 151 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "performance"]
scope = [
  "Third megakernel runtime upgrade slice: make IO polling/claim reserve exact target capacity and skip barrier pass cloning when no barriers exist.",
]
proof_required = [
  "Reusable IO poll/claim buffers must reach the full validated slot capacity without relying on push-time growth.",
  "Warm reusable IO buffers must keep their existing allocation.",
  "Barrier-free megakernel programs must return before entry cloning/rewrite traversal.",
  "Focused IO, barrier, and megakernel runtime suites must stay green.",
]
proof = [
  "try_poll_io_requests_into and try_claim_io_requests_into now use reserve_exact(view.slot_count) when caller-owned storage is undersized.",
  "poll_io_requests_into_reuses_request_storage and claim_io_requests_into_reuses_request_storage prove repeated warm calls keep Vec capacity stable.",
  "elide_value_flow_barriers now performs a recursive barrier-presence scan and returns the original Program when no Node::Barrier exists.",
  "no_barrier_program_is_returned_without_rewrite proves the barrier-free fast path preserves the original fingerprint.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime io --lib -> 78 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime barrier --lib -> 5 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "protocol", "performance"]
scope = [
  "Megakernel protocol hardening and decoder reuse slice.",
]
proof_required = [
  "Allocating protocol encoders must reject oversized host allocation requests before reserving capacity.",
  "Protocol decoders that write into caller-owned Vec storage must reserve the exact target capacity before pushes when undersized.",
  "Debug-log record-capacity estimates must account for the cursor header word.",
]
proof = [
  "try_encode_control, try_encode_empty_ring, and try_encode_empty_debug_log now route through their strict encode-into paths before any allocation.",
  "read_metrics_into, try_read_metrics_into, read_debug_log_into, and try_read_debug_log_into use reserve_target_capacity with reserve_exact instead of geometric reserve(total_target).",
  "debug_log_record_capacity subtracts the debug cursor header before estimating record capacity.",
  "allocating_encoders_reject_allocation_cap_before_reserving covers control/ring/debug allocation caps.",
  "metrics_decode_into_reuses_capacity_without_overreserve and debug_log_decode_into_reuses_capacity_without_overreserve prove warm caller-owned decode buffers keep capacity stable.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime protocol --lib -> 28 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "descriptor", "performance"]
scope = [
  "Keep megakernel window-publication argument scratch inline for the protocol arg budget.",
]
proof_required = [
  "WindowDescriptor::publish_into must not heap-allocate a Vec for the common <= ARGS_PER_SLOT argument path.",
  "The scratch path must reject oversized payloads before spilling the inline SmallVec or mutating the ring.",
]
proof = [
  "WindowDescriptor::publish_into now uses SmallVec<[u32; ARGS_PER_SLOT]> for ticket/class/payload args.",
  "publish_window_payload rejects ticket+class+payload shapes larger than ARGS_PER_SLOT before extending the inline scratch buffer.",
  "window_publish_rejects_oversized_payload_before_publication proves malformed oversized payloads do not partially publish.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime window --lib -> 9 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "telemetry", "performance"]
scope = [
  "Tighten megakernel telemetry reusable-buffer reservation.",
]
proof_required = [
  "ControlSnapshot, RingTelemetry, active slot/window filters, and window-opcode scratch must reserve to the target capacity before pushes when caller-owned storage is undersized.",
  "Warm telemetry decode/filter buffers must keep their allocation across repeated polling.",
]
proof = [
  "telemetry.rs now uses reserve_target_capacity with reserve_exact for metrics, tenant fairness, priority fairness, slot snapshots, window snapshots, window opcode scratch, active slots, and active windows.",
  "decode_with_scratch_reuses_snapshot_storage and decode_window_opcodes_groups_ticketed_slots keep pointer-stability coverage for repeated telemetry polling.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime telemetry --lib -> 13 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "policy", "performance"]
scope = [
  "Fix megakernel priority-diffusion shape handling and scratch reservation.",
]
proof_required = [
  "Priority diffusion must preserve the documented output shape when restriction inputs are malformed.",
  "Reusable diffusion scratch must hit target capacity before extension without geometric reallocation on warm buffers.",
]
proof = [
  "diffuse_priority_across_siblings_into now returns the original priority vector unchanged when restriction_diag length does not match priority_stalks.",
  "diffuse_step_into uses reserve_target_capacity with reserve_exact for caller-owned scratch output.",
  "diffuse_priority_mismatched_restrictions_preserve_input_shape and diffuse_priority_reuses_exact_scratch_capacity cover the corrected contract.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime diffuse_priority --lib -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime launch_cache --lib -> 1 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "dispatch", "performance"]
scope = [
  "Cache megakernel GridSync presence at compile time instead of walking Program IR on every dispatch.",
]
proof_required = [
  "Borrowed, resident-handle, and resident-batch dispatch must consume the cached GridSync bit.",
  "GridSync splitting behavior must remain unchanged for backends without native grid sync.",
]
proof = [
  "Megakernel now stores has_grid_sync computed once in compile_bootstrap_shared.",
  "dispatch_once_into, dispatch_persistent_handles_observed, and dispatch_persistent_handles_many_observed check self.has_grid_sync before backend.supports_grid_sync().",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime grid_sync --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime persistent_handle --lib -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "builder", "performance"]
scope = [
  "Remove avoidable megakernel body-builder reallocations.",
]
proof_required = [
  "Persistent and JIT megakernel body builders must reserve known tail capacity before pushing post-prologue nodes.",
  "No-op reserve calls must be removed from production builder code.",
]
proof = [
  "persistent_body_with_io reserves the exact direct-slot/execute/io tail capacity after constructing the lane prologue.",
  "persistent_body_jit reserves the exact direct-slot/JIT-execute/io tail capacity after constructing the lane prologue.",
  "The previous body.reserve(0) no-ops are gone.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel::builder --lib -> 5 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_cuda", "pipeline", "performance"]
scope = [
  "Tighten CUDA compiled-pipeline repeated dispatch hot path.",
]
proof_required = [
  "Same-shape borrowed dispatch must not validate the prepared input plan twice.",
  "Fallback owned-output transfer into caller-owned storage must reserve exact target capacity when undersized.",
]
proof = [
  "CudaCompiledPipeline::dispatch_borrowed now delegates validation to dispatch_borrowed_into instead of calling prepared_for_inputs and discarding the result first.",
  "CudaCompiledPipeline::dispatch_borrowed_into uses reserve_target_capacity with reserve_exact when moving fallback outputs into caller-owned storage.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["lower_emit", "driver_cuda", "performance"]
scope = [
  "Remove ordered-map overhead from PTX vector load/store fusion pattern analysis.",
]
proof_required = [
  "PTX vector load/store analyzers must use hash lookups for result-id producer and literal tables.",
  "Existing vector fusion behavior must remain unchanged.",
]
proof = [
  "vyre-emit-ptx vec_load_fusion and vec_store_fusion now use pre-sized FxHashMap tables instead of BTreeMap.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_load --lib -> 9 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_store --lib -> 6 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-26"
status = "done"
lanes = ["reference", "testing", "dual_oracle"]
scope = [
  "Close the stale vyre-reference test-gap item with broader registered dual-oracle coverage.",
]
proof_required = [
  "Existing compare scalar reference evaluators must be exposed through the public dual-reference registry, not left as unregistered source-only evaluators.",
  "The independent compare references must use different implementations and agree on adversarial, generated, and property inputs.",
  "The full vyre-reference crate test suite must pass after the registry expansion.",
]
proof = [
  "Added compare dual-reference shared byte machinery plus `primitive.compare.eq` and `primitive.compare.lt` OP_ID registrations.",
  "Compare reference A decodes u32 words and compares directly; reference B compares raw equality bytes or walks most-significant bytes for unsigned less-than.",
  "Extended dual parity and property tests to include compare equality and less-than boolean-word contracts.",
  "Current vyre-reference counts: 74 source files, 43 external test files, 41 inline source test markers, 327 total test/property markers.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-reference --test dual_reference_parity --test dual_reference_property_contracts --test dual_registry_adversarial_contract --test dual_scalar_evaluator_generated_matrix -> 12 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-reference -> passed; final group reported 10 passed, 3 ignored, 0 failed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_cuda", "resident_dispatch", "performance"]
scope = [
  "Remove resident-dispatch handle-map allocation from CUDA launch setup.",
]
proof_required = [
  "CUDA resident dispatch must not allocate and fill an Option handle table for every binding just to recover output handles.",
  "Output handle ordering by output index must remain deterministic.",
]
proof = [
  "dispatch_resident_async_concrete_with_ptx_key now collects (output_index, handle) pairs while validating resident handles.",
  "The per-binding handles_by_binding SmallVec<Option<CudaResidentBuffer>> resize/fill path was removed.",
  "Output handles are sorted by output_index exactly once and collected into the returned SmallVec.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "telemetry", "performance"]
scope = [
  "Tighten megakernel telemetry slot decoding.",
]
proof_required = [
  "Telemetry ring decode must avoid recomputing checked absolute slot offsets for every decoded word.",
  "Window aggregation must not run per-slot binary searches when no window opcodes are requested.",
]
proof = [
  "RingTelemetry::decode_with_window_opcodes_into now iterates ring_bytes as fixed slot chunks and reads fields relative to the slot slice.",
  "The old read_slot_word absolute-offset helper was removed from telemetry decoding.",
  "Window opcode matching is gated by a decode_windows boolean.",
  "Telemetry scratch hash-map reserve now requests only missing capacity because FxHashMap::reserve is additional-capacity based.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime telemetry --lib -> 13 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime megakernel --lib -> 159 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "scheduler", "correctness", "performance"]
scope = [
  "Remove modulo-by-zero risk from priority partition probing.",
]
proof_required = [
  "Priority scheduler offset calculation must remain valid for empty priority partitions.",
  "Generated IR must not contain lane_id % (partition_end - partition_start) without a nonzero guard.",
]
proof = [
  "policy_offset_start now computes lane_id % max(partition_end - partition_start, 1).",
  "Added policy_offset_start_never_emits_zero_denominator_rem to lock the invariant.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime policy_offset_start --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime priority --lib -> 10 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "protocol", "performance"]
scope = [
  "Preallocate owned megakernel protocol encoders exactly.",
]
proof_required = [
  "Owned control/ring/debug-log encoders must reject oversized requests before allocation.",
  "Owned encoders must allocate the exact protocol byte capacity before resizing.",
  "Into encoders and owned encoders must share the same validation logic.",
]
proof = [
  "Added control_encode_capacity, ring_encode_capacity, and debug_log_encode_capacity and reused them from owned and into encoders.",
  "try_encode_control, try_encode_empty_ring, and try_encode_empty_debug_log now start with Vec::with_capacity(exact_protocol_bytes).",
  "Added allocating_encoders_preallocate_exact_protocol_capacity.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime allocating_encoders --lib -> 2 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime protocol --lib -> 29 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_cuda", "readback", "performance"]
scope = [
  "Remove redundant zero-fill from CUDA host readback copies.",
]
proof_required = [
  "Pinned host readback into Vec<u8> must not resize with zero-fill before overwriting every byte from CUDA pinned memory.",
  "Readback output Vec capacity must still be reused across repeated collections.",
]
proof = [
  "PinnedHostAllocation::copy_prefix_into now delegates to copy_raw_bytes_into_vec, which clears, reserves missing capacity, copies bytes, and then sets len.",
  "Added copy_raw_bytes_into_vec_reuses_capacity_without_zero_fill_resize.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda copy_raw_bytes_into_vec --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "resident", "performance"]
scope = [
  "Preallocate resident megakernel runtime mirrors to exact protocol sizes.",
]
proof_required = [
  "Resident control/ring/debug/IO host mirrors must not start from empty Vecs and grow through reset.",
  "IO queue owned encoder must use the same exact-capacity construction style as the protocol encoders.",
]
proof = [
  "MegakernelResidentBuffers::new computes exact capacities for all four runtime buffers before reset.",
  "io::try_encode_empty_io_queue now uses Vec::with_capacity(byte_count) followed by resize.",
  "Added resident_buffers_preallocate_exact_runtime_buffer_capacities.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime resident_buffers --lib -> 5 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime io_queue --lib -> 2 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_cuda", "cuda_graph", "performance"]
scope = [
  "Pre-size CUDA graph capture setup buffers from the binding plan.",
]
proof_required = [
  "CUDA graph capture must not grow input/output pointer and pinned-host vectors from empty SmallVecs.",
  "Shared bindings must not count toward graph runtime input/output buffer capacities.",
]
proof = [
  "record_cuda_graph_borrowed now computes input/output capacities from the prepared binding plan.",
  "Input/output DevicePtrGuard lists, expected length lists, output length lists, all_ptrs, and GraphHostBuffers use those capacities.",
  "Added cuda_graph_binding_capacities_count_only_runtime_buffers.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver-cuda cuda_graph_binding_capacities --lib -> 1 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver-cuda --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["driver_shared", "architecture", "boundary"]
scope = [
  "Remove concrete-backend naming from shared vyre-driver policy surfaces.",
]
proof_required = [
  "Shared vyre-driver policy modules must use backend-neutral command-reuse and inline-metadata names.",
  "Shared vyre-driver source/tests must not mention concrete driver APIs or artifact dialects.",
]
proof = [
  "Renamed cuda_graph_policy.rs to command_reuse_policy.rs and updated DispatchPolicyInputs/Verdict to use CommandReuseInputs and CommandReuseDecision.",
  "Renamed ParamInliningPolicy concrete defaults to large_inline_default and small_inline_default.",
  "Neutralized shared driver comments/tests in arm independence, binding, bindless, persistent policy, device extraction, autotune, speculation, and actionable errors.",
  "rg concrete backend/API/dialect terms over vyre-driver/src and vyre-driver/tests now returns no matches.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver command_reuse --lib -> 7 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver param_inlining --lib -> 10 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-driver --test d_series_integration -> 3 passed.",
  "CARGO_BUILD_JOBS=1 cargo check -q -p vyre-driver --tests -> passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["runtime_megakernel", "protocol", "performance"]
scope = [
  "Pack megakernel packed-slot metadata directly into argument words.",
]
proof_required = [
  "Packed-slot publication must avoid building byte metadata then converting it back to u32 words.",
  "Packed-slot host decode roundtrip and overflow rejection must remain intact.",
]
proof = [
  "publish_packed_slot_from now writes metadata bytes directly into the args u32 array via write_packed_metadata_byte.",
  "Removed the transient payload byte array and chunks_exact conversion loop from packed-slot publish.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime packed_slot --lib -> 3 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-runtime protocol --lib -> 29 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["lower_emit", "driver_cuda", "performance"]
scope = [
  "Remove per-candidate binding scans from PTX vector load/store fusion analysis.",
]
proof_required = [
  "Vector fusion analyzers must resolve binding metadata through one precomputed slot table and reuse it for child bodies.",
  "Existing vector fusion tests must remain green.",
]
proof = [
  "vec_load_fusion::analyze and vec_store_fusion::analyze now build FxHashMap<u32, &BindingSlot> once and pass it through recursive walk calls.",
  "Candidate loops use binding_by_slot.get(&slot) instead of desc.bindings.slots.iter().find for every candidate.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_load --lib -> 9 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_store --lib -> 6 passed.",
]

[[claim]]
owner = "main-codex"
date = "2026-05-03"
status = "done"
lanes = ["lower_emit", "driver_cuda", "performance"]
scope = [
  "Remove per-memory-op PTX address operand string allocations.",
]
proof_required = [
  "PTX load/store emission must write address operands directly into the module text instead of allocating temporary formatted String values.",
  "Vector and scalar global/shared memory emission must remain byte-compatible at the tested behavior level.",
]
proof = [
  "vyre-emit-ptx::emitter now carries MemOperand values and writes them through BodyCtx::write_mem_operand.",
  "address_operand/shared_address_operand format!-allocating helpers were removed.",
  "Scalar, predicated, vector, and async copy memory emission paths now write operands directly into the output buffer.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_load --lib -> 9 passed.",
  "CARGO_BUILD_JOBS=1 cargo test -q -p vyre-emit-ptx vec_store --lib -> 6 passed.",
]
